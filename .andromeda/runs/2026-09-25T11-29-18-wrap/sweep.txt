.andromeda/architecture.md:515:- Jobs wired today in `ci.yml` (8; 15 check-runs per push): `test` (per OS: one harness `run --coverage` through the OS's shim, i.e. the instrumented nextest run as suite `coverage` with the per-OS floors 85/95/80, followed by do
.andromeda/test-plan.md:437:| Mutation | Test-suite strength on changed code | Required (Founder Direction 1). cargo-mutants 27.1.0 runs `--in-diff` against the chunk diff through `--test-tool=nextest`. Any `missed` or `timeout` outcome, or more `unviable` tha
.andromeda/test-plan.md:555:     - Command: first `cargo build --package viola --features fake-agent` (the root package only: the harness tests spawn the root's `viola` and `viola-fake-agent`, cargo-mutants builds only the packages the diff touches, and a root
.andromeda/test-plan.md:557:     - Verdict (the `counted` arm): parse `mutants.out/outcomes.json` and require `missed == 0 && timeout == 0 && unviable <= caught`. A run with more unviable than caught mutants tested almost nothing, because its mutant builds fai
.andromeda/test-plan.md:563:- Exit code semantics: 0 only if every selected suite passes, there are no missed or timed-out mutants, and unviable mutants do not outnumber caught ones. Non-zero otherwise. With `--leg`, the mutants suite's survivors are deferred 
.andromeda/test-plan.md:566:  - A `--mutants` run adds a top-level `mutants` object: `{"tested":N,"verdict":"counted"}`, or `{"tested":0,"verdict":"no-rust-delta","diff":"target/agent-run/chunk.diff","files":N}` when the chunk diff names no `.rs` path (step 4)
.andromeda/test-plan.md:655:    - single-leg: the `mutants` suite's `survived > 0`. With `--mutants-legs`, the union: a mutant name no leg reports `caught` while some leg reports `missed` or `timeout` (at the union, unviable on every leg is reported, not red; 
.andromeda/test-plan.md:663:  - `run --mutants` `mutants.verdict`: `counted`, `no-rust-delta`. Leg-verdict `outcome`: `caught`, `missed`, `timeout`, `unviable`.
.andromeda/test-plan.md:796:  - zero missed or timed-out mutants, and no leg with more unviable than caught mutants
.andromeda/test-plan.md:1434:| Mutation | `mutants (<os>)`, a two-leg matrix (`ubuntu-latest`, `windows-2025`, `fail-fast: false`) on push and pull_request: `agent-run run --mutants --leg "$LEG"` with `AGENT_RUN_CHUNK_BASE` = `github.event.pull_request.base.sh
.andromeda/test-plan.md:1468:- Any missed or timed-out mutant in the chunk diff, or a mutation leg with more unviable than caught mutants
.andromeda/test-plan.md:1496:**Mutation gate (Founder Direction 1):** `mutants.out/outcomes.json` for the chunk diff must show `missed == 0`, `timeout == 0` and `unviable <= caught`. `unviable` mutants are reported, and a few do not fail the gate. When they ou
.andromeda/test-plan.md:1529:- Any missed or timed-out mutant, or unviable mutants outnumbering caught ones (`unviable-exceeds-caught`)
.andromeda/test-plan.md:1609:- NEVER trust the cargo-mutants exit code alone. Exit 3 masks exit 2, so count `missed`, `timeout`, `unviable` and `caught` in `outcomes.json`: any missed or timed-out mutant, or more unviable than caught, is red.
.andromeda/test-plan.md:1798:  - Mutation runs as two legs (`ubuntu-latest`, `windows-2025`). Each writes `mutants-verdict-<leg>.json` (`run --mutants --leg`), and the `mutants-verdict` job gates the union: a mutant is red only when no leg caught it and some l
.andromeda/test-plan.md:1829:  - `run --mutants` passes `--caught --unviable --build-timeout-multiplier=5` and streams cargo-mutants' stdout live to the harness's stderr. Every outcome line reaches the CI step log as it happens.
.andromeda/test-plan.md:1830:  - `mutants_suite` is red when `unviable > caught`: `failures[]` code `unviable-exceeds-caught`, and `failed` = survivors + 1. Under `--leg` such a leg is not deferred to the union.
.andromeda/test-plan.md:1836:  - CI run 36118112104 (windows): `8 caught, 135 unviable`, read `ok:true`. Under the `cleanup_cmd → Default` mutant the booted supervisor leaked and locked `viola-harness.exe` (relink `Access is denied. (os error 5)`, reproduced
.andromeda/test-plan.md:1837:  - The `unviable > caught` threshold was measured across all legs with an uploaded verdict (§10 Mutation gate).
.andromeda/test-plan.md:1839:  - Green witness: CI run 36126924953 on d14f234. ubuntu `148 … 144 caught, 4 unviable`, windows `148 … 142 caught, 6 unviable`, `mutants-verdict` success.
.andromeda/obs-plan.md:1253:| Mutation | obs code (`obs_event!` call sites, `MillisUtc`, panic hook, TraceLayer closures) under cargo-mutants like product code | per-leg `mutants-verdict-<os>.json` (ubuntu-latest, windows-2025), merged by the `mutants-verdict`
.claude/rules/testing.md:48:- **Coverage:** `run --coverage` (lines 85 / functions 95 / regions 80 per OS) · **mutants:** `run --mutants` (zero missed, zero timeout; verdict from a fresh `mutants.out/outcomes.json`, never the exit code alone; a diff with no `
.claude/docs/commands.md:31:- Internal (forwarded by the shims, not agent-facing): `supervise`, `ui-restart --session <id>`, `gate --require <suites> [--artifacts <dir>] [--mutants-legs a,b]` (the per-job verdict; a missing artifact is a breach; with `--mutant
.claude/docs/commands.md:41:- `cargo build --package viola --features fake-agent` then `NEXTEST_PROFILE=mutants cargo mutants --workspace --features fake-agent --in-diff target/agent-run/chunk.diff --test-tool=nextest --copy-target=true` — mutation gate (the
.claude/docs/tests-summary.md:42:| Mutation | 0 missed, 0 timeout in the chunk diff | cargo-mutants 27.1.0 `--in-diff`; a Rust delta reads a fresh `outcomes.json` (`verdict:"counted"`), a diff with no `.rs` path passes as `verdict:"no-rust-delta"`. CI runs two
