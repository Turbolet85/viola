proposals:
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Test Harness Contract → 5-command implementation → `run` step 4 Mutation (Verdict bullet)"
    change: "Verdict (the `counted` arm): parse `mutants.out/outcomes.json` and require `missed == 0 && timeout == 0 && unviable <= caught`; `unviable > caught` (an unviable swamp — mutant builds failing, e.g. a leaked process locking a relinked `.exe` on windows) makes `mutants_suite` red with `failures[]` code `unviable-exceeds-caught` and `failed` = survivors + 1. The exit-code rules that follow stay."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:557"
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Test Harness Contract → 5-command implementation → `run` step 4 Mutation (Command bullet)"
    change: "invocation becomes `… --test-tool=nextest --copy-target=true --caught --unviable --build-timeout-multiplier=5`; cargo-mutants' stdout streams live to the harness's stderr (`Stdio::from(std::io::stderr())`), not captured by `Command::output()`; harness stdout stays the one JSON document."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:555"
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 … `run` (Exit code semantics bullet)"
    change: "0 only if every selected suite passes, no missed or timed-out mutants and `unviable <= caught`; the non-survivor failures are `outcomes-missing` and `unviable-exceeds-caught`; a swamp leg is not deferred (its `failed != survived`)."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:563"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§10 Quality Gates & Coverage Targets → Mutation gate (Founder Direction 1)"
    change: "must show `missed == 0`, `timeout == 0` and `unviable <= caught`; unviable reported, but outnumbering caught is red (`unviable-exceeds-caught`)."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1496"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 … Internal harness subcommands → `gate` (breach list, mutants item)"
    change: "scope 'unviable on every leg is reported, not red' to the union; cross-reference the per-leg swamp rule."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:655"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§2 Test Strategy → Test pyramid table (Mutation row)"
    change: "Any `missed` or `timeout` outcome, or an unviable swamp (`unviable > caught`), fails harness `run`."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:437"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Test Harness Contract → Bootstrap phases → quality-gate-config-emit"
    change: "'zero missed or timed-out mutants' → '…, and no leg with `unviable > caught`'."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:796"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§9 CI Integration → stage table (Mutation row)"
    change: "append: each leg's own run is red, not deferred, when `unviable > caught`; keep the union sentence."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1434"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§9 CI Integration → Build failure conditions"
    change: "'Any missed or timed-out mutant in the chunk diff, or a mutation leg with `unviable > caught`'."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1468"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§10 … Build failure conditions"
    change: "'Any missed or timed-out mutant, or unviable mutants outnumbering caught ones (`unviable-exceeds-caught`)'."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1529"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§11 Test Anti-Patterns → Quality"
    change: "count `missed`, `timeout`, `unviable` and `caught`: missed/timeout > 0 or unviable > caught is red."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1609"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-obs-harness
    severity: warning
    section: "§12 Test Decisions Log"
    change: "new 2026-09-25 entry: streamed invocation + swamp rule + partial-verdict upload declined; rationale runs 36118112104 / 36046091888; impact list."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1796"
    dependent-of: D-tests-obs-harness
# (condensed transcription of the agent's return: `sidecar` and `rationale` fields elided here; every change/basis line kept.
#  The agent's closing comments: D-tests-coverage no drift; D-tests-framework no drift; :536/:647 nextest 0.9.133 attributions
#  historical, not proposed; :663 enums untouched; commands.md:41 / tests-summary.md:42 are distillations.)
