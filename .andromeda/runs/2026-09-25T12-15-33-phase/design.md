# design extract

## No domain coverage
This chunk builds no rendered or CLI-output surface. Its outputs are a Windows SQOS spike test, a pinned SHA-256 crate, licence files and manifests, and deny and CI checks. The `unable: the pinned viola copy failed its integrity check` hint in design-system.md §Surface: cli → Component Patterns belongs to the later Instance state (`bin/` writer and re-hash) chunk, which the scope's §3 Boundaries leaves out.
