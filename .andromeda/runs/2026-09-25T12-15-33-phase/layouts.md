# layouts extract

## No domain coverage
This chunk adds no web-spa or cli surface, region, component, focus stop or output line. It covers the SQOS pipe-handle spike, the SHA-256 crate pick, the MIT OR Apache-2.0 licence files and README licence line, and a possible CI fold. The layout plan's security-linked items (the 401 access strip and the credential content of the `viola ui` launch line, per layout-templates §Decisions Log → Notable surface-specific deferrals) belong to later implementation chunks, not this one. The amendment sidecar is missing, so the amendment history is empty.
