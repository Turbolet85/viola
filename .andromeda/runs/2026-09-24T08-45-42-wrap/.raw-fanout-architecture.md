proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources › Environment variables
    change: "Replace the test-harness bullet: `AGENT_RUN_CHUNK_BASE` (the mutation gate's diff base; read by `viola-harness`), `AGENT_RUN_KEEP_HOMES` (`1` keeps `target/e2e-home/` homes for the CI gates; read by `viola-harness` and by the root test chain `tests/support/home.rs`), and `AGENT_RUN_KEEP_FAILED` (NEW — keeps a failed test's home; read only by the root test chain `tests/support/home.rs`) — none of the three is ever read by `viola`."
    sidecar: "arch §Occupied Resources env vars — registered AGENT_RUN_KEEP_FAILED (test-only, tests/support/home.rs); AGENT_RUN_KEEP_HOMES now also read by the root test chain."
    rationale: "Report Changes › Env vars adds `AGENT_RUN_KEEP_FAILED` (NEW, test-only, read by tests/support/home.rs) and states `AGENT_RUN_KEEP_HOMES` is now also read by tests/support/home.rs, not only viola-harness; Spec claim disproved #2 names architecture.md:359. The 'never by `viola`' half still holds, so the Conventions naming rule (:170) needs no change."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:359"
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources › Repository
    change: "Replace the e2e-home bullet: `target/e2e-home/viola-session-*/home` = harness session homes (viola-harness); `target/e2e-home/viola-test-*/home` = root rstest homes (`tests/support/home.rs` TestHome; home dir never pre-created); both gitignored and kept per `AGENT_RUN_KEEP_HOMES` / `AGENT_RUN_KEEP_FAILED` for the obs gates."
    sidecar: "arch §Occupied Resources Repository — split e2e-home into harness `viola-session-*` and root-test `viola-test-*` homes."
    rationale: "Report Spec claim disproved #1: architecture.md:374 says `viola-session-*/home` is 'every harness and test home', but root rstest homes use the `viola-test-` prefix (Symbols › Root test support, TestHome; gate `git check-ignore -q target/e2e-home/viola-test-probe/home` green); only harness sessions use `viola-session-*`."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:374"
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources › Repository
    change: "Add: `fixtures/fake-scripts/*.json` — committed fake-agent step scripts (`{\"v\":1,\"steps\":[…]}`), validated against `schemas/fake-script.v1.json` (draft 2020-12, tolerant, `v` const 1, closed 9-event enum); `crates/viola-core/proptest-regressions/` — committed proptest regression seeds; `tests/support/` — the shared root test-support module (TestHome, rstest fixtures, Wrapper chain, fixture-hygiene checker)."
    sidecar: "arch §Occupied Resources Repository — registered fixtures/fake-scripts/, schemas/fake-script.v1.json, crates/viola-core/proptest-regressions/, tests/support/."
    rationale: "Report Changes › Files (New) lands fixtures/fake-scripts/gated-turn.json, schemas/fake-script.v1.json, crates/viola-core/proptest-regressions/lib.txt, tests/support/{mod,home,fake,hygiene}.rs; Changes › Schema/config describes the schema. None of these paths is in arch today (report: `proptest-regressions` architecture=0, `schemas/` architecture=0)."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:371-375"
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns › Project directory structure
    change: "Update the tree: under `fixtures/` add `fake-scripts/` (committed fake-agent step scripts) beside `claude/<cli-version>/`; add top-level `schemas/` holding `fake-script.v1.json`; annotate `tests/` with `support/` (shared rstest chain + hygiene checker); note `proptest-regressions/` (committed seeds) under `crates/viola-core/`."
    sidecar: "arch §Infrastructure Patterns directory tree — added fixtures/fake-scripts/, schemas/, tests/support/, viola-core proptest-regressions/."
    rationale: "Same new paths as the Repository registration (Changes › Files, New); the tree lists `fixtures/` with only `claude/<cli-version>/` and has no `schemas/`, so a Repository-only edit would leave the tree claiming fixtures/ holds only recorded hook payloads."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:423-437"
    dependent-of: D-arch-resources
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources › Filesystem
    change: "Add a test-only bullet: inside a test viola home only, `fake/<name>.control` (append-only control file; `\\n`-terminated lines release gated fake-agent steps) and `fake/<name>.receipt.ndjson` (the fake agent's receipt log — `\"v\":1` + kebab `kind` start·env·fds·key·prompt·hook·step, one write per line), passed via `--control`/`--receipt` by the root test chain (`Wrapper::boot`); `viola` never reads or writes them."
    sidecar: "arch §Occupied Resources Filesystem — registered test-only <home>/fake/<name>.control and <home>/fake/<name>.receipt.ndjson."
    rationale: "Report Changes › Symbols: `Wrapper::boot` spawns the fake agent with `--control <home>/fake/<name>.control --receipt <home>/fake/<name>.receipt.ndjson`, and the receipt format is listed; the Expected-amendments bullet names these two paths for arch §Occupied Resources. Neither path is registered today."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:361-369"
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies
    change: "Add a row — Layer: Testing (dev-dependencies only) · Technology: rstest 0.27.0 (default-features off), proptest 1.11.0 (default-features off, `std`), jsonschema 0.57.0 (default-features off), pinned in `[workspace.dependencies]` · Role: rstest fixtures for the root test chain, `ViolaName` property tests in viola-core, fixture-hygiene schema validation; dev-only, never in a product (normal) graph, no Tokio / reqwest / rustls / aws-lc / ring in the graph (choice owned by test-plan per [Deferred])."
    sidecar: "arch §Stack — registered dev-only test libraries rstest 0.27.0, proptest 1.11.0, jsonschema 0.57.0."
    rationale: "Report Changes › Dependencies adds jsonschema =0.57.0, proptest =1.11.0, rstest =0.27.0 to [workspace.dependencies] (root dev-deps jsonschema, rstest; viola-core dev-dep proptest); none is in §Stack. [Deferred] delegates the test framework to the tests specialist, so no locked decision is contradicted, but the new libraries are unregistered while Tokio containment is upheld ('No tokio added. No product (normal) dependency changed'; reqwest/rustls/aws-lc/ring probe = 0)."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:9-37"
