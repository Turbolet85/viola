[transport note: the return carried a harness-envelope-tag neutralization line and rewrote `<` as `<\` in one quoted prefix (`<task-notification>` in proposal 7); the parsed copy in fanout-results.md restores the literal `<`. Proposals as received, condensed to their fields:]

proposals:
  1. D-tests-obs-harness · warning · §3 `run` step 4 Mutation (Verdict) · classify the diff first; Rust-free (empty counts) → `{"tested":0,"verdict":"no-rust-delta","diff":"target/agent-run/chunk.diff","files":N}`, outcomes.json never read; Rust delta → delete `mutants.out/outcomes.json` before invoking, then counted verdict `{"tested":N,"verdict":"counted"}`; Rust delta with no outcomes stays red (`outcomes-missing`); exit-code rules unchanged · basis test-plan.md:556
  2. dependent · warning · §10 Mutation gate · replace the tests-only-diff `"mutants":{"tested":0}` claim with the two verdict arms · basis :1445
  3. dependent · warning · §3 `run` Output format · document the `mutants` object and qualify the outcomes.json bullet to the counted arm · basis :563-564
  4. dependent · warning · §3 Closed enums · add `run --mutants` `mutants.verdict`: `counted` | `no-rust-delta` · basis :637-640
  5. dependent · warning · §12 Decisions Log · new 2026-09-24 entry: the verdict enum, stale removal, operator P1 constraint, the P4 deferrals · basis :1704
  6. dependent · warning · §3 `run` step 2 · the chain exists today only as the sync root copy; the viola_e2e copy is deferred to its first consumer; root stamped_home interim (stamped:false, no stamps.json), interim readiness · basis :542
  7. dependent · warning · §7 Fake agent · rewrite hooks/receipt/modes bullets to the built contract (hooks from `<plugin-dir>/hooks/hooks.json`, matchers not evaluated, absolute-only exec, payload `<fixtures>/<cli-version>/<Event>.<variant>.json` with `prompt` set for UserPromptSubmit, variant `default`, script schema, receipt kinds, modes built incl. `--inject-harness-turn` `<task-notification>`-prefixed harness prompt; three modes deferred as CARRYs; unknown args ignored) · basis :1321-1325
  8. dependent · warning · §7 seed table `claude agents --json` row · mark the stub + FAKE_CLAUDE_AGENTS_MODE deferred · basis :1312
  9. dependent · warning · §3 `boot` command body (`--agents-mode` / `--statusline-echo`) · mark both deferred; "usage errors until consumer lands" · basis :514
  10. dependent · warning · §6 E1 step 2 · qualify FAKE_CLAUDE_AGENTS_MODE=recorded as the deferred stub switch this scenario pins · basis :1146
  11. dependent · ESCALATE · §3 `boot` step 5 fake-agent argv · `--fixtures` should pass the parent `fixtures/claude` (the fake agent joins `<cli-version>`), not `fixtures/claude/<ver>`; name the `--plugin-dir` source · basis :519
  12. dependent · ESCALATE · §2 fixture file naming · fake agent reads PascalCase `<Event>.<variant>.json`; §2 mandates kebab `<hook-event>.<variant>.json` — reconcile before the first recorded fixture · basis :467
  13. dependent · ESCALATE · §7 seed table recorded-payload row · same naming reconciliation as 12 · basis :1313
  14. D-tests-framework · warning · §7 Fixture hygiene · the built walk over fixtures/fake-scripts against schemas/fake-script.v1.json, classes absolute-path|username|schema, planted inputs; fixtures/claude walk deferred · basis :1328-1333
