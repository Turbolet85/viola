# security-plan.md fan-out return (condensed record: detector · severity · section · basis)
- D-security-deps · escalate · Dependency Security → `deny.toml` additions · fuzz/ separate workspace, own lock with libfuzzer-sys (cc) + arbitrary OUTSIDE cargo deny (bans/advisories/sources); exemption holds only as test-only, never linked; must never join root [workspace] · security-plan.md:314
- D-security-deps (dep) · escalate · Dependency Security → Pinning · both lockfiles committed; fuzz pins exact · :317
- D-security-deps (dep) · escalate · Dependency Security → CI integration · cargo deny covers root lock only; nightly.yml gains fuzz job · :326-327
- D-security-deps (dep) · escalate · Threat Model → Supply chain (Trust boundary) · cargo deny bounds root lock only; fuzz workspace outside · :134
- D-security-deps (dep) · escalate · Threat Model → Infrastructure → CI/CD · nightly + fuzz; ci.yml job list · :160-161
- D-security-deps (dep) · escalate · Security Decisions Log · dated entry: fuzz workspace exemption + toolchain + viola_name synthetic corpus · :574-639
- D-security-deps · escalate · Dependency Security → CI integration (SHA-pinned actions) · + download-artifact@3e5f45b… v8.0.1 (4→5) · :329-333
- D-security-deps (dep) · escalate · Threat Model → Supply chain (Entry point) · + download-artifact; toolchains from rust-toolchain.toml + fuzz/rust-toolchain.toml + MSRV 1.96 via RUSTUP_TOOLCHAIN, no toolchain action · :131
- D-security-deps · escalate · Dependency Security → Pinning (toolchain) · + fuzz nightly-2026-09-20 file; MSRV 1.96 rustup install · :320
- D-security-deps (dep) · escalate · Dependency Security → CI integration (toolchain) · toolchain sources incl. fuzz file + MSRV rustup · :334
- D-security-auth · warning · Bootstrap → secret-scanning-ci-gate · mutants.out/ upload removed (CARRY closed); names-only verdict upload + nightly fuzz/artifacts upload, neither scan-gated · :381
# D-security-input: no drift (harness inputs closed-enum / charset validated; fuzz target exercises ViolaName::try_new).
# Note: the zizmor concurrency-limits decline is an expected amendment that no detector here covers → the orchestrator raises it.
