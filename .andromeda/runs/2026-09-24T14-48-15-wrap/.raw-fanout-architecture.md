# architecture.md fan-out return (condensed record: detector · section · basis; each change is re-derived at apply)
- D-arch-resources · Occupied Resources → Repository · `fuzz/` workspace bullet (viola-fuzz, own [workspace] + Cargo.lock, exclude, rust-toolchain.toml nightly-2026-09-20, fuzz_targets, corpus, artifacts) · architecture.md:375-388
- D-arch-resources · Occupied Resources → Repository · artifacts bullet + `llvm-cov-summary.json`, `mutants-verdict-<leg>.json`; + `target/lcov.info` · :381
- D-arch-resources · Occupied Resources → Workspace crates · note `viola-fuzz` NOT a member (excluded, own lockfile, path dep viola-core) · :348-350
- D-arch-resources (dependent) · Established Decisions → [Module Boundaries] · fuzz/ excluded workspace; nightly confined there · :96
- D-arch-resources (dependent) · Infrastructure Patterns → Project directory structure · Cargo.toml comment + `exclude = ["fuzz"]`, `fuzz/` subtree · :424
- D-arch-decisions · Stack → Language / runtime row · MSRV CI-checked by `msrv` (rustup 1.96 + RUSTUP_TOOLCHAIN); fuzz-only nightly-2026-09-20 · :13
- D-arch-decisions · Stack → Code quality row · cargo-llvm-cov 0.9.1 (llvm-tools-preview; floors via gate) · :36
- D-arch-decisions · Stack → Code quality row · cargo-fuzz 0.13.2 CI-only; fuzz/ pins libfuzzer-sys =0.4.13 (cc) + arbitrary =1.4.2 outside the deny root graph · :36
- D-arch-decisions (dependent) · Infrastructure Patterns → Build system (Dependency policy) · fuzz lock outside cargo deny / deny-sync; its cc is the only C build, never in `viola` · :401
- D-arch-decisions · Stack → CI/CD row · + download-artifact v8.0.1 (5 actions); MSRV via rustup 1.96 + RUSTUP_TOOLCHAIN · :35
- D-arch-decisions (dependent) · CI/CD approach → Setup steps · + download-artifact, llvm-tools-preview + cargo-llvm-cov@0.9.1, rustup 1.96, rustup in fuzz/, cargo install cargo-fuzz@0.13.2 · :476
- D-arch-decisions (dependent) · CI/CD approach → workflows bullet · nightly.yml = advisories + fuzz; no `concurrency:` block (declined, why) · :474
- D-arch-decisions (dependent) · CI/CD approach → Jobs wired today · test (coverage + gate), mutants 2-leg matrix + verdict upload only, mutants-verdict, msrv, fuzz-replay; 7 jobs · :477
- D-arch-decisions (dependent) · Project directory structure · workflow comments (nightly fuzz; ci.yml jobs) · :467-468
