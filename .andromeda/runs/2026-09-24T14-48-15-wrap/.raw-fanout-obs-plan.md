proposals:
  - detector: D-obs-pii
    severity: escalate
    section: "## 9. CI Integration → Pipeline integration (Mutation row)"
    change: "Mutation row Consumer: replace `mutants.out/outcomes.json` with the per-leg `target/agent-run/artifacts/mutants-verdict-<leg>.json` (mutant names + outcomes only) merged by the `mutants-verdict` job's `gate --require mutants --mutants-legs ubuntu-latest,windows-2025` (a mutant is red only when no leg caught it and some leg missed/timed it out); `mutants.out/` is never uploaded."
    sidecar: "§9 Mutation consumer: mutants.out/outcomes.json → mutants-verdict-<leg>.json + union gate; mutants.out upload removed (outcomes.json absolute argv paths, log/ test output)."
    rationale: "report.md:67 + :68 + :126 + :42; §9:1249 still names the retired artifact; sole mutants.out hit in obs-plan is §9:1249 (not §8 item 6)."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1249"
  - detector: D-obs-pii
    severity: escalate
    section: "## 8. PII Scrubbing & Compliance → PII Scrubbing → Integration points, item 6 (Verification)"
    change: "Add a sub-bullet: two CI uploads are outside the secret scan and admissible only by content — (1) mutants-verdict-<leg>.json (names + outcomes only; mutants.out/ never uploaded); (2) nightly fuzz job's fuzz/artifacts/ on failure() (fuzzer inputs mutated from the synthetic corpus; a non-synthetic seed must first drop that upload)."
    sidecar: "§8 item 6: records the two unscanned CI uploads added by quality-gates and why each is admissible; mutants.out never uploaded."
    rationale: "report.md:67, 109, 74; names-only verdict (report.md:42, 145); synthetic corpus (report.md:10, 107, 148); §8 item 6 justifies unscanned-content uploads only for E2E detail files and harness-<os> (obs-plan.md:1205-1207)."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1200-1207"
    dependent-of: D-obs-pii
  - detector: D-obs-pii
    severity: escalate
    section: "## 9. CI Integration → Platform"
    change: "Platform: nightly.yml runs the weekly cargo deny check advisories AND a fuzz job (per target, 120 s, fuzz/rust-toolchain.toml nightly, fuzz/artifacts/ on failure() only) — replacing 'which runs only the weekly cargo deny check advisories'."
    sidecar: "§9 Platform: nightly.yml no longer 'only' deny advisories — adds the fuzz time-box with a failure-only fuzz/artifacts upload."
    rationale: "report.md:49, 74, 100; the retired 'only' also hides a new upload from the §8/§9 inventory."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1229"
    dependent-of: D-obs-pii
# D-obs-instrumentation / D-obs-stack: no drift (test-only viola-e2e, CI YAML, separate fuzz workspace; root graph unchanged).
# Sweep: mutants\.out|outcomes\.json → only 1249; nightly|advisories → only 1229; §10:1339 still holds under the union verdict.
