The portable-pty line is recorded beside its pin (the `Cargo.toml` `[workspace.dependencies]` portable-pty comment) and in the `viola-pty` crate doc. The record names 0.8.1 as the line in use and why 0.9.0 is excluded: wezterm#6783 (Windows `pty.read` returns garbage from 0.9.0) was re-checked open on 2026-09-25, and 0.9.0 is still crates.io's newest release. It also states that child exit is detected on the process handle and that the spawn passes `bInheritHandles = 0` (portable-pty 0.8.1 `src/win/psuedocon.rs:141`). Under that line the seam's exit-on-handle tests pass: the `--exit-no-eof` fake child's exit is reported by `wait()` on a real PTY, and a never-EOF mock reader returns on `wait()`.
