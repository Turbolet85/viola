## Analysis

**Cross-reference walk (iteration-1 residue check first)**
- Most iteration-1 fixes are in place: the G1 PCRE2 form, G2 `-n -e`, the DEBUG ceiling, the `(conn, corr)` join key, `cli` precedence, crate ownership, the `harness-<os>` artifact, the canary scan, the detail schema, the exemptions and D-23 to D-25.
- **Residue:** the §10 perf table, `viola hook session-end` row, still reads `jq '.results[0].max < 1.0'` with no `-e`. The iteration-1 changelog says "perf jq now `-e`", but the fix never landed there. Without `-e`, jq exits 0 on `false`, so the only perf gate cannot fail.
- No stray "1.89" or "pending" text outside Section 1 and the historical entries. The trigger map, the `<area>` list and the Vector 4 D-16 override are all correct now.

**1. Downstream Readiness**
- G1 scans only `crates/`. The root bin `viola`, the "root bin" in arch Workspace, holds the `run` pump, the readiness gate and the hook/`run` dispatch, and it is not under `crates/`. A bare `#[instrument]` there passes CI green, which is exactly the anchor's adversarial case. Patch: search from the workspace root with `--type rust`.
- G2 reads every file through `cat`, so it breaks on a torn line:
  - A process killed mid-write (a chaos case) leaves a last line with no newline. `cat` glues it to the next file's first line, and jq then fails with a parse error (exit 2). The job goes red for the wrong reason, or a panic line at the start of the next file is lost.
  - Patch: `awk 1` ends every file's last line with a newline, and `-R` + `fromjson?` skips torn lines.
- Route: the plan says `pii-scrubbing-wire` "merge with security's `logging-redaction-wire`", but gives no position and no owner of the merged gates. Patch.
- a11y: the plan names the state words but not which component shows each one, or which Problem URNs reach the page. upstream-context has both (`<viola-atis>`, `<viola-readback>`, the rack strips, the 401/403/404/405/503 URNs). Patch adds the boundary with no ARIA content.

**2. Obs Scope Faithfulness**
- `ledger/stamps.json` (listed as instrumentable in Section 1) has no span, `subject` or `parser` value. `verify` and `plugin install` exist only as span names. Nothing says they usually have no instance, so under D-06 they write no file. Patches: an Edge-flow entry, plus a `ledger-stamps` value in the §6 `parser` enum.
- Section 1's pending items are stale: the `mcp` stderr sink (D-09), the four open items, and a join key of `process` + `instance`. Patch adds one pointer line after the verbatim block. Section 1 itself is not edited.

**3. Tool Anchoring**
- The init sketch now matches the catalog line (`with_max_level(DEBUG)`). The direct `chrono` dependency is present and the D-12 fallback is closed (D-24).
- The tracing-subscriber `chrono` feature is still listed but nothing uses it. It is harmless, so no patch this round; flagged for next iteration.
- Versions agree across all three sites.

**4. Perf / SLO**
- The `-e` residue above. Patch, merged with the dimension 7 table fix.
- §7 panic exit behaviour: no catch site turns a main-thread panic into exit 1 plus `process-exit`, and nothing handles a `run` worker-thread panic.
  - A hook that panics before init exits 101 instead of 0.
  - Nothing forbids `panic = "abort"`, which turns every `catch_unwind` into an abort.
  - Patch in §7, plus a G3 gate added to the §9 block. This also covers the dimension 8 ban on `panic = "abort"`.

**5. Log Format Alignment**
- The detail schema and the counting rule are fixed.
- Still open (deferred for budget):
  - The 4 KiB append-atomicity claim covers `hook` only; `mcp.ndjson` and `cli-<name>.ndjson` are also shared by several processes.
  - The schema's null-vs-absent typing for `corr` / `instance` on the panic line.

**6. PII**
- Canary scan, synthetic-input rule and scan-failure fallback are present. No patch.

**7. Tier Calibration**
- The §10 p99 column has no computation anywhere in the plan. Patch drops the column in the same table patch and adds a note.
- The liveness cross-reference without `ui` is present. Carve-outs trace to D-13, §7 and the initial entry.

**8. Anti-Patterns**
- `panic = "abort"` is covered through the §7 patch and G3.
- Not patched (low priority): the duplicate Universal ban (screen/transcripts), the `print_stdout` / `dbg_macro` lints, and a §11 `LogTracer` ban (D-23 already says it).

## Patches

### Patch 1: G1 scans the whole workspace, G2 handles torn lines, add G3 (no panic=abort)
**Reason:** Downstream Readiness. G1's `crates/` scope misses the root bin, which holds the `run` and hook dispatch, so a bare `#[instrument]` there ships green. G2's `cat` joins a torn last line to the next file's first line and fails with a jq parse error. There is also no gate for the `panic = "abort"` case that Patch 7 bans.
**Old:**
```
set +e; rg -n -U --pcre2 '#\[(tracing::)?instrument\b(?!\(\s*skip_all\b)' crates/; test $? -eq 1
find target/e2e-home -path '*/diagnostics/*.ndjson' ! -name 'detail-*' -exec cat {} + | jq -n -e '[inputs|select(.event=="panic")]|length==0'
```
**New:**
```
# G1: whole workspace from its root (root bin `viola` + crates/* + tests/ + fuzz/); target/ is skipped through .gitignore
set +e; rg -n -U --pcre2 --type rust '#\[(tracing::)?instrument\b(?!\(\s*skip_all\b)' .; test $? -eq 1
# G2: `awk 1` ends every file's last line with a newline; -R + fromjson? skip a torn line (not a panic record) instead of failing with jq exit 2
find target/e2e-home -path '*/diagnostics/*.ndjson' ! -name 'detail-*' -exec awk 1 {} + | jq -R -n -e '[inputs|fromjson?|select(.event=="panic")]|length==0'
# G3: no `panic = "abort"` in any Cargo profile or cargo config (§7); as with G1, only rg exit 1 passes
set +e; rg -n --hidden -g 'Cargo.toml' -g 'config.toml' 'panic\s*=\s*"abort"' .; test $? -eq 1
```

### Patch 2: Route position and ownership of the merged pii-scrubbing / logging-redaction phase
**Reason:** Downstream Readiness (route). The merge is stated, but its position relative to the other phases and the owner of the merged gates are not.
**Old:**
```
Route order: otel-sdk-install (no-op record) → logger-stack-install → service-identity-wire → log-format-schema-emit → trace-context-propagate-wire → heartbeat-tick-wire → pii-scrubbing-wire → obs-ci-gate-wire.
```
**New:**
```
Route order: otel-sdk-install (no-op record) → logger-stack-install → service-identity-wire → log-format-schema-emit → trace-context-propagate-wire → heartbeat-tick-wire → pii-scrubbing-wire → obs-ci-gate-wire.
- **Merged phase position:** `pii-scrubbing-wire` + security's `logging-redaction-wire` run as **one** phase at the `pii-scrubbing-wire` slot. It must follow logger-stack-install, because the sinks, `obs_event!` and the detail-file routing target must exist. It must precede obs-ci-gate-wire, because G1, the secret-scan/canary step and the veil `toggle` ban verify it. `logging-redaction-wire` is security's only logging contribution at Minimal tier, so obs imposes no order on security's other, non-logging phases.
- **Ownership:** obs owns the merged phase and its mechanical gates: `#[instrument(skip_all)]` + G1, the veil `toggle` cargo-deny ban (D-19), the TraceLayer `make_span_with`, and the `detail-<process>.ndjson` routing. Security owns the NEVER-log floor list that those gates enforce. The secret-scan test body and canary value stay tests-owned.
```

### Patch 3: State the frontend observable boundary for a11y (components, state words, Problem URNs)
**Reason:** Downstream Readiness (a11y). The plan names the DOM state words but not which component shows each one, or which Problem URNs reach the GUI. a11y needs both to derive state and error surfaces. The facts come from the upstream-context design excerpt. No ARIA content is added.
**Old:**
```
Skipped as not-instrumentable per obs-scope §1: `viola-core`
```
**New:**
```
**Frontend observable boundary** (boundary-only; read by tests' Playwright and used by a11y to derive state and error surfaces; attribute names and announcements stay design/a11y-owned):
- `<viola-atis>`: the tape connection word `TAPE connecting|live|stopped` and the `skipped N · N · N` counters. Process-log counterparts: `sse-opened` (→ `live`), `sse-closed{close_cause}` (→ `stopped`), `http-request{route:"/api/sessions", skipped}`.
- `<viola-readback>` (on `<viola-event-feed>` send lines and `<viola-transfer>` markers): `open|read back|unable|unconfirmable`, with `unable · <reason> · <detail>` using the §6 refusal codes. Process-log counterparts, joined by the send `cursor` in `corr`: `send-issued` (open), `send-confirmed{confirmed:true}` (read back), `send-confirmed{confirmed:false}` (unconfirmable), `send-refused{refusal, detail}` (unable).
- Problem URNs that reach the page, each mirrored by `http-request{status, problem}`:
  - `urn:viola:problem:state-unreadable` (503, rack strip);
  - `urn:viola:problem:unauthorized` (401, access strip);
  - `not-found` (404) and `method-not-allowed` (405), defensive rack strips.

  `host-not-allowed` (403) never renders the page (the browser shows the raw Problem JSON). `cross-origin-forbidden` is v1.x-only.

Skipped as not-instrumentable per obs-scope §1: `viola-core`
```

### Patch 4: Cover the capability ledger and the verify / plugin install verbs
**Reason:** Obs Scope Faithfulness. Section 1 lists `ledger/stamps.json` as instrumentable, but it has no span, `subject` or `parser` value. `verify` and `plugin install` appear only as span names, and the plan never says they usually run with no instance, so under D-06 they write no file.
**Old:**
```
- **E5** (snapshot corruption): `state-recovered{detail:"snapshot-replayed"|"snapshot-unsupported-v", file:"snapshot.json", v_seen}` (D-03).
```
**New:**
```
- **E5** (snapshot corruption): `state-recovered{detail:"snapshot-replayed"|"snapshot-unsupported-v", file:"snapshot.json", v_seen}` (D-03).
- **Capability ledger** (`ledger/stamps.json`, obs-scope §1 persistent stores):
  - Read inside `run.version_gate` as the child span `state.ledger_read`. Its outcome is the existing `cli_verified` on `process-start{subject:"claude-child"}`. A missing stamp gives `cli_verified:false` and the Scenario 4 `unverified-cli` branch.
  - An unreadable or unparsable ledger gives `parse-rejected{parser:"ledger-stamps", detail:"unreadable"|"malformed", count}` in `run-<name>.ndjson` and is treated as unverified.
  - The path and stamp contents never appear.
- **`verify` / `plugin install`:**
  - Dispatch spans: `cli.verify`, with `cli.verify_step` per step and `state.ledger_write` for the stamp, and `cli.plugin_install`.
  - With `VIOLA_NAME` set they log `process-start` / `process-exit{exit_code, detail}` to `cli-<name>.ndjson`. Run from a plain terminal with no instance, they write no process-log file (D-06). Their agent-readable outcome is then the exit code plus the `stamped <version>  N pass  N fail` stderr summary (design).
  - `viola verify` never runs in CI (arch Pipeline note), so no CI gate depends on it.
```

### Patch 5: Add `ledger-stamps` to the closed `parser` enum
**Reason:** Obs Scope Faithfulness, together with Patch 4. Without it, the new ledger `parse-rejected` line fails the §8 `additionalProperties: false` / closed-enum conformance check.
**Old:**
```
| `parse-rejected` (D-05) | `parser` (`claude-agents-json|vt100-feed|config-json|last-event-id|hook-stdin|channel-frame|prompt-submitted|paste-text`), `detail`, `count` |
```
**New:**
```
| `parse-rejected` (D-05) | `parser` (`claude-agents-json|vt100-feed|config-json|ledger-stamps|last-event-id|hook-stdin|channel-frame|prompt-submitted|paste-text`), `detail`, `count` |
```

### Patch 6: Pointer that §3/§6/§12 supersede Section 1's pending items
**Reason:** Obs Scope Faithfulness. The verbatim Section 1 still states the `mcp`-to-stderr sink (D-09), four items as open, and a merged-log join key of `process` + `instance` (D-25). The meta-prompt allows one pointer line after the verbatim block; Section 1 is not edited.
**Old:**
```
This tier is the SINGLE SOURCE OF TRUTH for how much observability
```
**New:**
```
_Section 1 is a verbatim copy of obs-scope.md and keeps its pending wording. Where they differ, §3, §6 and §12 win: diagnostics root (D-08, D-22), the `mcp` sink (D-09: always `mcp.ndjson`, stderr only when the file cannot be opened), `event` enum extensions (D-01…D-05, D-21), `hook-invoked` `corr` timing (D-07), the channel join key (D-25: `(conn, corr)`, each side logs its own `instance`), and the MSRV floor (D-22: 1.96)._

This tier is the SINGLE SOURCE OF TRUTH for how much observability
```

### Patch 7: Catch sites for panic exits, worker-thread panics, and require unwinding
**Reason:** Perf / SLO (plus Anti-Pattern dimension 8).
- §7 promises that a panicking `cli` exits 1 and that `run` writes `process-exit{exit_code:1, detail:"internal-error"}`, but `catch_unwind` is specified only for hook dispatch and vt100. An uncaught unwind exits 101 with no `process-exit` line.
- A `run` PTY-pump or handle-wait thread panic leaves `main` running with no signal.
- A hook that panics before init exits non-zero.
- `panic = "abort"` would silently turn every catch site into an abort.
**Old:**
```
- Per-role behaviour after the line:
  - `hook`: `catch_unwind` around dispatch, then exit 0 with empty stdout;
  - short-lived `cli`: exit 1 with the fixed stderr `error: internal error`;
  - `run`: `process-exit{exit_code:1, detail:"internal-error"}` and nothing on the terminal while the child runs;
```
**New:**
```
- The panic hook only writes. The exit is decided at a catch site:
  - **Main-thread catch site (every role):** everything after `set_hook` (clap parse, init and role dispatch) runs inside one `std::panic::catch_unwind` in the root bin's `main`. An `Err` maps to the role's exit below, so no role ends through an uncaught unwind (Rust's default exit 101). The role is taken from `argv[1]` before clap runs, so a pre-init panic in a `hook` process still exits 0.
  - **`run` worker threads:** the PTY pump and handle-wait thread bodies each run inside `catch_unwind`. An `Err` is sent to `main`, which takes the `run` exit path below, because the session cannot continue without either thread. A worker panic that leaves `main` running with no `process-exit` is forbidden. A panic in one channel server connection worker is contained: the `channel.dispatch` `Drop` guard logs `channel-response{result_class:"error", error_code:-32603}`, the connection closes, and `run` continues.
  - **Unwinding is required:** every Cargo profile keeps `panic = "unwind"`. `panic = "abort"` turns every `catch_unwind` into an abort, so `hook` would die by SIGABRT instead of exiting 0. Gate G3 (§9) enforces this.
- Per-role behaviour after the line:
  - `hook`: `catch_unwind` around dispatch, then exit 0 with empty stdout;
  - short-lived `cli`: exit 1 with the fixed stderr `error: internal error`;
  - `run`: `process-exit{subject:"self", exit_code:1, detail:"internal-error"}`, then exit 1, and nothing on the terminal while the child runs;
```

### Patch 8: Perf table: add the missing `jq -e` and drop the uncomputed p99 column
**Reason:**
- Perf / SLO: iteration-1 residue. The table still has `jq '.results[0].max < 1.0'` without `-e`; it exits 0 on `false`, so the only perf gate cannot fail. The spine-hook row names no assertion command and no deadline owner.
- Tier Calibration: no command in the plan computes p99 (the §5 snippet gives p50/p95/max), so the p99 column reads as unfinished TBD cells.
**Old:**
```
| Path / operation | p50 | p95 | p99 | Budget assertion |
|------------------|-----|-----|-----|------------------|
| `viola hook session-end` | report-only (jq over `duration_ms`) | report-only | report-only | hyperfine `max < 1.0 s` → `jq '.results[0].max < 1.0'` sets the exit code |
| Spine hooks (`session-start`, `user-prompt-submit`, `stop`) and `pre-tool-use` on an unverified CLI | report-only | report-only | report-only | hyperfine `max <` spine deadline (provisionally 1.0 s) |
| SSE keep-alive | n/a | n/a | n/a | emitted at `advance(15 s)` and not before (tests-owned) |
| Heartbeat staleness flip | n/a | n/a | n/a | 4.9 s → `live`, 5.1 s → `stale`/`gone`; `liveness-changed.heartbeat_age_ms > 5000` |
| Obs overhead on `hook` | n/a | n/a | n/a | logger init = 1 `open` + appends, inside the same `max < 1.0 s` gate |
```
**New:**
```
| Path / operation | p50 | p95 | Budget assertion |
|------------------|-----|-----|------------------|
| `viola hook session-end` | report-only (§5 snippet over `duration_ms`) | report-only | hyperfine `max < 1.0 s` → `jq -e '.results[0].max < 1.0' perf/hook-session-end.json` sets the exit code (`-e` maps `false` to exit 1; plain `jq` exits 0 on `false`) |
| Spine hooks (`session-start`, `user-prompt-submit`, `stop`) and `pre-tool-use` on an unverified CLI | report-only | report-only | hyperfine `max <` spine deadline → `jq -e '.results[0].max < <deadline_s>' perf/hook-<event>.json`. The deadline value is tests-owned (provisionally 1.0 s); obs requires only the `-e` assertion |
| SSE keep-alive | n/a | n/a | emitted at `advance(15 s)` and not before (tests-owned) |
| Heartbeat staleness flip | n/a | n/a | 4.9 s → `live`, 5.1 s → `stale`/`gone`; `liveness-changed.heartbeat_age_ms > 5000` |
| Obs overhead on `hook` | n/a | n/a | logger init = 1 `open` + appends, inside the same `max < 1.0 s` gate |

p99 is not computed in v1. hyperfine `max` is the gated tail, and the §5 snippet reports p50/p95/max only. Section 1's "p95/p99" wording is met by `max` as an upper bound on every quantile.
```

## Changelog
[Iteration 2] [substantive] Fixed the perf gate that still lacked `jq -e` (iteration-1 residue) and dropped the uncomputed p99 column. Widened G1 to the whole workspace so it covers the root bin, made G2 tolerate torn lines, and added a G3 gate against `panic = "abort"`. Specified panic catch sites for the main thread and `run` workers. Set the merged pii-scrubbing / logging-redaction phase's route position and owner. Stated the frontend component, state-word and Problem URN boundary for a11y. Covered the capability ledger (`ledger-stamps` parser value) and the `verify` / `plugin install` verbs. Added a pointer from Section 1 to the decisions that supersede its pending items. Still open for iteration 3: the shared-file append rule for `mcp.ndjson` and `cli-<name>.ndjson`, null vs absent `corr` / `instance` in the panic line's schema, the unused tracing-subscriber `chrono` feature, and the duplicate Universal ban.
