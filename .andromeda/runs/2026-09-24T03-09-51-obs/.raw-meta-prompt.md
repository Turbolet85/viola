## Output Protocol

You are an iteration agent improving `D:/dev/projects/viola/.andromeda/runs/2026-09-24T03-09-51-obs/obs-plan-draft.md`, the viola obs plan: Standard (1) tier with Minimal-tier exporter carve-outs, Rust workspace, tracing 0.1.44 + tracing-subscriber 0.3.23 JSON file sink, no OTel SDK in v1. To check claims, cross-reference `obs-scope.md`, `obs-research.md`, `upstream-context.md` and `review-feedback-1.md` in the same run directory.

Rules:
1. **What you output:** patches (old → new) plus a changelog. Do NOT reproduce the full document.
2. **Patch format:**
   ### Patch N: <one-line description>
   **Old:**
   ```
   <exact text copied from the current draft; it must match exactly one location>
   ```
   **New:**
   ```
   <replacement text>
   ```
   You may output at most 8 patches per iteration. Spend them in this priority order:
   1. Obs Scope Faithfulness
   2. Log Format Alignment to Tests
   3. PII Scrubbing & Compliance Coverage
   4. Downstream Readiness
   5. Tool Anchoring
   6. Tier Calibration
   7. Performance Budget & SLO Discipline
   8. Anti-Pattern Relevance

   Within a dimension, fix a factual or command bug (a gate that cannot set an exit code, a regex that cannot run, a contradiction between sections) before you add missing detail.
3. **Changelog:** write exactly ONE line per iteration that classifies the iteration as a whole: `[Iteration N] [substantive|cosmetic] description`. Never write one line per patch.
4. **Prohibited:**
   - reproducing the full document;
   - restructuring without a reason;
   - labelling cosmetic changes as substantive;
   - paraphrasing the Section 1 Obs Scope Summary, which is a VERBATIM copy of obs-scope.md. Patch it only to restore verbatim fidelity;
   - editing the tests-owned log format block reproduced in Section 3. It is binding;
   - editing historical Section 12 Decisions Log entries. New decisions go in new entries appended at the bottom as `D-23`, `D-24`, …;
   - adding instrumentation code blocks longer than 5 lines, or test bodies. Those belong to `/andromeda-implement` and tests.
5. **If you find no issues:** output "No patches" and a cosmetic changelog line.

## Analysis Dimensions

### 1. Obs Scope Faithfulness [priority: high]
- Check each obs-scope §1 instrumentable or boundary-only entity for a named span or `event` in plan Sections 4 and 6. Three are suspect:
  - the `ledger/stamps.json` capability ledger in `viola-agent-claude`;
  - the short-lived verbs `verify`, `plugin install` and `pause`, which get only the generic `cli.<verb>` span;
  - notify 8.2.0 tailing errors in `viola-state`.

  Name any entity that obs-scope lists but that has no span attribute, `subject` value or `detail` code, and patch Section 4's surface table or Section 6's catalog. Do not invent coverage for the not-instrumentable `viola-core`, `config.json`/plugin files, or CI infrastructure.
- Section 1, copied verbatim, still describes superseded states:
  - `mcp` "stderr when `VIOLA_DIR` is unset" (now D-09: always `mcp.ndjson`);
  - gaps (a), (b) and (c) as "pending" (now D-01…D-07);
  - "Obs aligns to tests … directory root must be reconciled" (now D-08/D-22).

  Does the plan tell a downstream reader that Sections 3, 6 and 12 supersede these, without paraphrasing Section 1? One short pointer note outside the verbatim block is acceptable. Rewriting the copied text is not.
- D-16 overrides the Vector 4 trigger's "missing `VIOLA_NAME`" logging. Does the Section 2 trigger coverage map row for Vector 4 still claim full coverage? Is the unrealizable part stated explicitly, so the 19-row map does not over-claim?

### 2. Tier Calibration [priority: high]
- Standard tier needs all 12 sections, and all are present. Check that each Minimal carve-out traces to an upstream ban and a Decisions Log entry, not just to prose. The carve-outs are:
  - no exporter (file sink only);
  - no error reporter (sentry 0.49.3 rejected);
  - no periodic heartbeat tick lines (D-13).

  Is Section 5 Metric Coverage (jq-derived, no instruments) marked as satisfying the Standard requirement through derivation, rather than silently downgraded?
- Section 10's perf budget table marks p50/p95/p99 as "report-only" for every row. Does that match Standard depth given the perf-budget-instruments trigger? Or should the table state explicitly that hyperfine `max` is the only gated statistic and that no percentile is gated, so it does not look like unfinished "TBD" cells?
- The Standard+ `Heartbeat ticks` subsection replaces 10–30 s tick lines with `liveness-changed` emitted only by `ui`. If no `ui` process is running, is `run`'s stall still agent-detectable (`agent-run status` / `list --json` `liveness`)? Does the plan say so in Section 10's Liveness invariant?

### 3. Tool Anchoring (Catalog ↔ Plan) [priority: high]
- obs-research.md recommends the tracing-subscriber 0.3.23 `tracing-log` bridge. Its reason: portable-pty 0.8.1 and notify 8.2.0 log through the `log` crate, and "without this bridge, two failure classes would be silently dropped". D-11 turns `tracing-log` off and sets those targets `OFF`, claiming failures are "captured from returned `Result`s". Does the plan name which portable-pty/notify failures surface only through `log` and never as a `Result`? Does the D-11 rationale answer the research finding directly? If not, patch D-11's Impact through a new appended entry.
- Two more research recommendations are overridden:
  - rmcp 3.4.1 native `tracing` to `mcp.ndjson`: overridden by D-11 (target `OFF`);
  - web-vitals 6.2.2 vendored: overridden by D-17.

  Does each override cite the specific research trade-off? Is json-subscriber 0.3.0, the named fallback in D-11 and D-12, still a live option after D-21 accepted key absence? Or should the plan say it is no longer needed?
- Check every version in the plan against the obs-research catalog:
  - tracing 0.1.44, tracing-subscriber 0.3.23 (features `fmt,json,chrono,registry,std`);
  - tower-http 0.7.1 `trace`, veil 0.3.0, thiserror 2.0.20, anyhow 1.0.104;
  - hyperfine 1.20.0, cargo-nextest 0.9.146, actions/upload-artifact v7.0.1, sysinfo 0.39.6;
  - logroller 0.1.12, axum-tracing-opentelemetry 0.39.1, opentelemetry 0.33.0, tracing-opentelemetry 0.34.0.

  `MillisUtc` depends on chrono, but the plan never pins the chrono version (research: 0.4.45 already in tree). Flag any tool whose catalog `Agent-readable` mechanism the plan contradicts.

### 4. Anti-Pattern Relevance [priority: medium]
- Section 11 must carry the bans that the viola-specific pitfalls imply. Check for these gaps:
  - a Cargo profile `panic = "abort"` would bypass the `catch_unwind` that keeps `hook` at exit 0, and would skip unwinding in `run`;
  - `println!`/`eprintln!`/`dbg!` in `hook`, `run` and `mcp` code paths (the stdout/stderr reservations) are not guarded by a clippy lint (`print_stdout` / `print_stderr`) alongside `disallowed-macros`;
  - installing `LogTracer` manually after D-11 removed `tracing-log`.

  Each would need a ban grounded in the Hook contract and the `run` terminal rule.
- Section 11 Universal has 7 bans. Two are viola-specific rather than cross-stack: "NEVER parse Claude Code transcripts or the rendered screen", which duplicates the Telemetry Strategy ban, and "NEVER self-instrument as if the product were telemetry". Should the first move or merge so that Universal holds only agent-driven cross-stack bans? Proprietary APM as sole exporter, dashboards-only consumption, human-review-gated analysis, env-resolved service identity and unscrubbed egress must stay.
- Sections 3, 4, 6 and 8 repeat bans inline instead of cross-referencing Section 11. Examples:
  - "never read from `RUST_LOG`" in §3 Logging stack;
  - "Never `DefaultMakeSpan` and never `.include_headers(true)`" in the §4 surface table;
  - "`DefaultMakeSpan` and `.include_headers(true)` banned" in the §8 table.

  Where a duplicate adds no section-specific fact, replace it with a pointer to the Section 11 domain.

### 5. Downstream Readiness [priority: high]
- **setup-project.** Can it materialize Section 3 without guessing? Check these points:
  - The `viola_obs_init(role)` signature and the `cli` role's file name: for `viola send --to B` run by a driver whose `VIOLA_NAME=A`, is `cli-<name>.ndjson` named after A or B?
  - How `obs_event!`, which is defined in viola-core and expands to `::tracing::event!`, stays exempt from the clippy `disallowed-macros` gate at its caller-crate expansion sites.
  - Who owns `schemas/diag-line.v1.json`: the plan says both "hand-maintained" by obs and checked by a "tests-owned check".
- **setup-project, CI commands.** Can the Section 9 lint gate `rg -n '#\[(tracing::)?instrument(\]|\((?!.*skip_all))' crates/` run at all? Its lookahead needs `rg -P` (PCRE2). Would a multi-line `#[instrument(` attribute evade it? Patch the command so a copy-paste into `ci.yml` actually fails the job.
- **route and a11y.**
  - route: `pii-scrubbing-wire` "merges with security's `logging-redaction-wire`". Does the route order state which phase runs first, and that `log-format-schema-emit` must precede `obs-ci-gate-wire`?
  - a11y: Section 4 says the frontend is observed through "DOM state words" (`TAPE connecting|live|stopped`, readback `open|read back|unable|unconfirmable`) read from "DOM attributes", but never names the attribute or element (e.g. `<viola-atis>`). Can a11y derive stable, testable error and state surfaces from this? Also, does Section 7 list which Problem URNs (e.g. `state-unreadable`) reach the GUI, so accessible error UX can be derived?

### 6. Log Format Alignment to Tests [trigger: tests excerpt in upstream-context Section 5 has explicit log format JSON schema (NOT "N/A")] [priority: high]
- The hand-written panic line writes a literal `"corr":null`, while `obs_event!` lines encode null as key absence (D-12, accepted in D-21). Does `schemas/diag-line.v1.json` (`additionalProperties: false` per event) accept both encodings for `corr` and `instance`? Does the panic line set `instance` for the `ui` role, where the instance is null? Patch Section 3 "Null encoding" or Section 7 so the two emitters produce schema-identical lines.
- Section 3 lists the tests-required fields: `timestamp` (RFC 3339 UTC, ms, `Z`), `level`, `target`, flattened `message`, `event`, `corr`, `process`, `instance`. Does every emitter satisfy all of them, including the raw panic line, which bypasses the subscriber, and the `mcp` stderr fallback (D-09)? Is the panic line's `timestamp` also written with `MillisUtc`'s exact format, and is its `level` uppercase `ERROR`? None of the additions may rename a field; for example `service_name`/`version` stay on `process-start` only, and nothing becomes `trace_id` or `event_type`.
- Tests binds the file list `{run-<name>,hook-<name>,mcp,ui-<port>}`, and D-21 adds `cli-<name>`, `process="cli"` and the `instances/*/diagnostics/detail-*.ndjson` glob. Is D-21's "blocking for `/andromeda-implement` until test-plan.md carries it" status reflected wherever those additions are used (Section 3 Log file location, Section 4 surface table, Section 9 artifact paths)? Is the zero-flakiness ↔ zero-unlogged-panics parallel in Section 10 consistent with tests' `retries = 0`?

### 7. PII Scrubbing & Compliance Coverage [trigger: Critical/High data classifications in upstream-context Section 2 Security Plan Excerpt OR security_tier=Hardened with compliance triggers] [priority: high]
- For each Critical class in Section 8 (GUI token / `?t=` / `Cookie` / `ui/<port>.url`, `CLAUDE_CODE_MESSAGING_TOKEN` and other R8-stripped `CLAUDE*` values, v1.x signing credentials), the secret-scan test is the verification layer. Does it cover the detail files `instances/<name>/diagnostics/detail-<process>.ndjson` too? Those can hold anyhow chains, drift reports and panic payloads, which could quote an env map or a URL. For High classes (prompt text, `last_assistant_message`, tool `input`), what proves that no home-level file contains them? The scan list names only token, `Cookie`, `?t=` and `CLAUDE*`.
- Section 9 uploads `target/e2e-home/**/diagnostics/*.ndjson`, which includes content-bearing detail files, to GitHub artifacts with `retention-days: 7`. Security permits user content only in 0600 instance `diagnostics/`. Does the plan state that E2E content is synthetic fixtures only? Or should detail files be excluded from upload, or run through the scan with a stricter rule?
- The scrubbing layers are:
  - the source allow-list via `#[instrument(skip_all, fields(...))]` and `obs_event!`;
  - veil 0.3.0 `#[redact]`, with `toggle` banned in cargo-deny;
  - the tower-http custom `make_span_with` recording `uri.path()` only;
  - fixed thiserror `Display`;
  - sink separation.

  Is each layer mapped to the exact payload types it protects (`SendParams.text`, `HookDialogParams.data`, `AnswerParams.response`, `LastResult.last_assistant_message`, `HookEventParams.data`)? Is the Compliance Trace Fields omission justified by security Minimal and "Compliance triggers: None", with a cross-reference to the security plan?

### 8. Performance Budget & SLO Discipline [trigger: perf-budget-instruments trigger in obs-scope Sec 5 OR error-budget-SLO trigger] [priority: medium]
- Section 10 and Section 9 claim that `jq '.results[0].max < 1.0'` "sets the exit code". Plain `jq` exits 0 whether it prints `true` or `false`; only `jq -e` fails on `false`/`null`. The zero-panic gate `jq -e '[inputs|select(.event=="panic")]|length==0'` without `-n` consumes the first line as `.` and never inspects it. Patch both commands so that each budget and SLO assertion really is exit-code-setting.
- Is the zero-unlogged-panics invariant ("every panic in `run`, `hook`, `mcp`, `ui` and `cli` produces exactly one … record") consistent with the plan's own exceptions?
  - Section 7: "Before step 4 of init … the panic hook writes nothing" (clap parse and home resolution);
  - D-06: `cli` without an instance writes no file;
  - D-16: `hook` without `VIOLA_NAME` writes no line.

  State these windows as explicit, bounded exemptions in Section 10. Also check whether the "any panic evidenced by an exit code without its line" failure condition can actually be detected for them.
- Section 3 claims that concurrent `hook` appends are atomic because lines are "well under 4 KiB". Two points to check:
  - POSIX `O_APPEND` guarantees the write offset; the size-atomicity rule (`PIPE_BUF`) applies to pipes, not regular files. Is this documented as an assumption the multi-platform-exporter-compat trigger must verify on windows-2025 / macos-latest / ubuntu-latest?
  - Multiple concurrent `viola mcp` processes (one per Claude session) all append to a single home-level `mcp.ndjson`, and concurrent short-lived verbs share `cli-<name>.ndjson`. Do those files get the same one-`write_all` guarantee and a torn-line tolerance statement in `agent-run logs`?

Read the document, analyze along all dimensions, output patches and changelog.
