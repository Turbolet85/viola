## Analysis

**1. Downstream Readiness**
- §9 Lint row: the `rg` gate uses `(?!.*skip_all)` without `--pcre2`. ripgrep's default engine rejects that with exit 2 and empty stdout, so a "must be empty" check passes on every commit. The gate also matches line by line, so a multi-line `#[instrument(` whose `skip_all` is on the next line is reported as a violation. **Patch 1.**
- §9 artifact table: the `if: failure()` harness output is uploaded "in the same `diag-<os>` artifact". upload-artifact v4 and later cannot upload the same name twice (the second step fails with a conflict, and `overwrite: true` would replace the diagnostics). **Patch 1**, which gives it its own artifact name.
- §3 logger-stack-install: it lists `MillisUtc`, `viola_obs_init` and the panic hook under "(viola-core)", but viola-core is tracing-free and tracing-subscriber is root-bin only. There is no direct `chrono` dependency, and nothing says how `obs_event!` expansions avoid the `disallowed-macros` ban. **Patch 2.**
- route merge ownership and the a11y state-word boundary: real but lower-ranked. Deferred.

**2. Obs Scope Faithfulness**
- The join key `(instance, conn, corr)` cannot join the two sides of a call: the mcp/cli side logs A, the wrapper side logs B. `cli` instance precedence is also unstated. **Patch 3.**
- The canonical `--instance builder | select(.record.corr==412)` query drops the caller's side, can match an unrelated JSON-RPC id 412, and misses the hook line (its `corr` is null). **Patch 3** (the same contiguous block).
- The §2 `<area>` list lacks `last` and `cli`, which §4 uses. The Vector 4 trigger-map row ignores the D-16 override. **Patch 4.**
- `ledger/stamps.json` has no coverage, and there is no pointer for the Section 1 D-09 residue. Deferred to the next iteration.

**3. Tool Anchoring**
- The init sketch drops the catalog's `.with_max_level(...)`. The fmt builder's default ceiling is INFO, so `diagnostics_level:"debug"` never emits anything (no `with_max_level` anywhere in the plan). **Patch 5.**
- D-11 never answers the obs-research concern that portable-pty and notify failures reach only `log`. D-12's json-subscriber fallback is closed by D-21 but never recorded as closed. **Patch 6** (new D-23 to D-25). The chrono direct dependency is handled in Patch 2.
- Pinned versions agree across the plan body, the Section 1 copy and the catalog. No patch.

**4. Performance Budget & SLO Discipline**
- §10 `jq '.results[0].max < 1.0'` has no `-e`, so it exits 0 on `false`. **Patch 7.**
- §9 `jq -e '[inputs|…]'` has no `-n`, so it skips the first line and exits 4 on empty input. **Patch 1** (gate G2).
- The zero-panic invariant contradicts the §7 pre-init window, D-06 and D-16. **Patch 7** adds these as bounded exemptions.
- No catch site is specified for main-thread or `run` worker-thread panics. Deferred.

**5. Log Format Alignment to Tests**
- Detail-file fields (`panic_payload`, `backtrace`, `chain`, `drift_report`) fail the default-deny `additionalProperties:false` check. **Patch 8** adds a detail-file schema scope.
- There are two `event:"panic"` lines per panic. **Patch 7** adds the counting rule, and **Patch 3** dedupes the ERROR query.
- The null-encoding mismatch between emitters and the append-atomicity claims for `mcp.ndjson` / `cli-<name>.ndjson` are deferred.

**6. PII Scrubbing & Compliance**
- Detail files are uploaded, but E2E inputs are never stated to be synthetic. There is no High-class verification layer, and the conflict between the two §11 CI bans is unresolved. **Patch 8** adds a canary requirement, the synthetic-input precondition and a hit-report-only fallback.

**7. Tier Calibration**
- §10 Liveness does not cover the case where no `ui` runs. **Patch 7** adds a cross-reference.
- The spine deadline's owner is now named in **Patch 7**.
- p99 is in the §10 table but never computed. Deferred (§5 snippet).

**8. Anti-Pattern Relevance**
- The `panic = "abort"`, `println!`/`dbg!` and `LogTracer` bans are missing, and one Universal ban duplicates another. Out of budget. D-23 in **Patch 6** states the "never install `LogTracer` by hand" rule.

## Patches

### Patch 1: Fix the §9 rg and zero-panic gate commands and give the harness output its own artifact name
**Reason:** Dimension 1 (the rg look-around needs PCRE2, the gate breaks on multi-line attributes, and an "empty = pass" check passes on every commit; the same artifact name cannot be uploaded twice) and Dimension 4 (the zero-panic jq lacks `-n`: it skips the first line and exits 4 on empty input; the file scope is unstated). The commands move below the table because `|` inside the table breaks the cells.
**Old:**
```
| `agent-run logs` merged ndjson + `agent-run status` JSON | on test failure (`if: failure()`) | uploaded in the same `diag-<os>` artifact | paste-to-AI directly; `jq` over `record` |
| nextest JUnit XML (`[profile.ci.junit] path = "junit.xml"` → `target/nextest/ci/junit.xml`) | every test job | uploaded as `junit-${{ matrix.os }}` | failing test ID joined to `diag` lines by time and `instance` |
| hyperfine `--export-json perf/*.json` | every perf-gate job | uploaded as `perf-${{ matrix.os }}` | `jq '.results[0].max'` |
| OTel trace export | N/A (no exporter in v1) | — | — |

**Pipeline integration:**

| Stage | Telemetry produced | Consumer |
|-------|---------------------|----------|
| Lint / typecheck | clippy `-D warnings` incl. `disallowed-macros` (raw `tracing::*!` outside `viola_core::obs`); grep gate: `rg -n '#\[(tracing::)?instrument(\]|\((?!.*skip_all))' crates/` must be empty; `cargo check` of sync crates without Tokio; `cargo deny check` (ubuntu) incl. veil `toggle` ban and bans on `opentelemetry-otlp` / `opentelemetry-stdout` / `sentry` / `tracing-appender` / tracing-subscriber `env-filter` feature | CI annotations + job exit code |
| Unit tests | In-memory `Vec<u8>` writer tests parse emitted lines with serde_json against `schemas/diag-line.v1.json` (tests-owned bodies) | nextest JUnit |
| Integration tests | `diagnostics/*.ndjson` from fake-agent runs; perf: hyperfine JSON with `max` assertions | uploaded artifacts + budget assertion exit code |
| E2E tests | `agent-run logs` / `status`; Playwright DOM-attribute reads; zero-`event:"panic"` gate (`jq -e '[inputs|select(.event=="panic")]|length==0'`) | uploaded artifacts |
| Mutation | obs code (`obs_event!` call sites, `MillisUtc`, panic hook, TraceLayer closures) under cargo-mutants like product code | `mutants.out/outcomes.json` |
```
**New:**
````
| `agent-run logs` merged ndjson + `agent-run status` JSON | on test failure (`if: failure()`), after the secret-scan step passes | its **own** artifact `harness-${{ matrix.os }}` (upload-artifact v7.0.1 SHA-pinned, `retention-days: 7`). Not `diag-<os>`: upload-artifact v4+ artifacts are immutable, so a second step uploading the same name fails with a conflict, and `overwrite: true` would replace the raw diagnostics | paste-to-AI directly; `jq` over `record` |
| nextest JUnit XML (`[profile.ci.junit] path = "junit.xml"` → `target/nextest/ci/junit.xml`) | every test job | uploaded as `junit-${{ matrix.os }}` | failing test ID joined to `diag` lines by time and `instance` |
| hyperfine `--export-json perf/*.json` | every perf-gate job | uploaded as `perf-${{ matrix.os }}` | `jq '.results[0].max'` |
| OTel trace export | N/A (no exporter in v1) | — | — |

**Pipeline integration:**

| Stage | Telemetry produced | Consumer |
|-------|---------------------|----------|
| Lint / typecheck | clippy `-D warnings` incl. `disallowed-macros` (raw `tracing::*!` outside `viola_core::obs`; exemption mechanism in §3 logger-stack-install); bare-`#[instrument]` gate G1 (below); `cargo check` of sync crates without Tokio; `cargo deny check` (ubuntu) incl. veil `toggle` ban and bans on `opentelemetry-otlp` / `opentelemetry-stdout` / `sentry` / `tracing-appender` / tracing-subscriber `env-filter` feature | CI annotations + job exit code |
| Unit tests | In-memory `Vec<u8>` writer tests parse emitted lines with serde_json against `schemas/diag-line.v1.json` (tests-owned bodies) | nextest JUnit |
| Integration tests | `diagnostics/*.ndjson` from fake-agent runs; perf: hyperfine JSON with `max` assertions | uploaded artifacts + budget assertion exit code |
| E2E tests | `agent-run logs` / `status`; Playwright DOM-attribute reads; zero-`event:"panic"` gate G2 (below) | uploaded artifacts + gate exit code |
| Mutation | obs code (`obs_event!` call sites, `MillisUtc`, panic hook, TraceLayer closures) under cargo-mutants like product code | `mutants.out/outcomes.json` |

**Gate commands.** Each gate is its own `run:` step with `shell: bash` on all three OSes, and is copied verbatim into `ci.yml`:
- **G1, bare `#[instrument]`.**
  - Needs ripgrep built with PCRE2 (`rg --pcre2-version` succeeds).
  - `-U` lets `\s*` span lines, so an `#[instrument(` with `skip_all` on the next line passes. `skip_all` must be the first argument (the §4 form).
  - rg exit 1 (no match) passes. Exit 0 (a violation) and exit 2 (a regex or I/O error) both fail the step. This replaces the earlier `(?!.*skip_all)` form: without `--pcre2` it exits 2 with empty stdout, so a "must be empty" check would pass on every commit.
- **G2, zero panics.**
  - `-n` makes `inputs` see every line, including the first, and makes empty input a pass (`true`) rather than exit 4. `-e` turns `false` into exit 1.
  - Scope is the home-level role files only. `detail-*.ndjson` repeats the panic line and is excluded (§10).

```sh
set +e; rg -n -U --pcre2 '#\[(tracing::)?instrument\b(?!\(\s*skip_all\b)' crates/; test $? -eq 1
find target/e2e-home -path '*/diagnostics/*.ndjson' ! -name 'detail-*' -exec cat {} + | jq -n -e '[inputs|select(.event=="panic")]|length==0'
```
````

### Patch 2: Say which crate owns each obs init piece, add the direct chrono dependency, and define the disallowed-macros exemption
**Reason:** Dimension 1, setup-project crate wiring: `MillisUtc`, `viola_obs_init` and the panic hook need tracing-subscriber, which is root-bin only, while viola-core stays tracing-free. `obs_event!` call sites need a stated exemption from the ban. Dimension 3: `MillisUtc` calls `chrono::Utc::now()` directly, so the root bin needs a direct chrono entry.
**Old:**
```
  - Add `MillisUtc`, `obs_event!`, `ObsEvent` / `ObsProcess` enums (viola-core), `viola_obs_init`, and the panic hook.
```
**New:**
```
  - viola-core (`viola_core::obs`): `obs_event!` and the `ObsEvent` / `ObsProcess` enums. The macro expands to `::tracing::event!` at the caller, so viola-core itself gains no `tracing` dependency.
  - Root bin (`viola::obs`), the only crate that depends on tracing-subscriber: `MillisUtc`, `viola_obs_init` and `viola_panic_hook`.
    - Add a direct `chrono = { version = "0.4.45", default-features = false, features = ["clock", "std"] }` (already in the tree). `MillisUtc` calls `chrono::Utc::now()` itself, and tracing-subscriber's `chrono` feature does not re-export chrono.
  - `disallowed-macros` exemption:
    - `obs_event!` expands to a block whose inner `::tracing::event!` statement carries `#[allow(clippy::disallowed_macros)]`, so caller crates pass `-D warnings`.
    - The ban lives in one workspace `clippy.toml`.
    - The gate must be proven both ways: an `obs_event!` call in a caller crate lints clean, and a raw `tracing::info!` in the same crate fails.
```

### Patch 3: Take `instance` out of the cross-process join key, state the `cli` instance precedence, and fix the canonical send query
**Reason:** Dimension 2, cross-surface-trace-propagation. Each side of a channel call logs its own `instance` (the mcp/cli side logs A, the wrapper logs B), so `(instance, conn, corr)` returns only one side. The `--instance builder` / `corr==412` query drops the caller's lines, can match an unrelated JSON-RPC id 412, and cannot reach the hook line, whose `corr` is null. The ERROR query also counts the detail-file panic duplicate twice.
**Old:**
```
  - `agent-run logs --instance builder | jq -c 'select(.record.corr==412)'` joins one send across CLI, MCP, wrapper and hook;
  - `jq -c 'select(.record.level=="ERROR")'` lists every error and panic.
- The harness `logs` glob must also cover `instances/*/diagnostics/detail-*.ndjson` (D-08). This additive harness change is part of tests amendment D-21.

### Trace context propagation

- **HTTP boundaries:** no W3C `traceparent` in v1. The browser sends no propagation header (obs-scope §3), and axum-tracing-opentelemetry 0.39.1 is rejected: it needs the deferred SDK, its W3C `traceparent` extraction has no sender, and its optional `/metrics` route would be a new listener. (Its MSRV 1.91 fits the raised 1.96 workspace floor, D-22, so MSRV is no longer a reason.)
  - Correlation on the `ui` side runs on the SSE `id:` cursor `<ViolaName>:<offset>` and `Last-Event-ID`.
  - `sse-opened` records `last_event_id_present` / `last_event_id_valid` and `resume_instances` (a count).
- **gRPC boundaries:** N/A (there is no gRPC in the stack).
- **IPC boundaries** (viola-channel JSON-RPC over interprocess 2.4.4 local sockets):
  - `corr` carries the JSON-RPC `id` on channel lines, `dialog_id` on dialog and hook lines, and the send `cursor` on send lines (tests).
  - **Additive `conn` field (D-10).** The client generates `conn = "<process>-<pid>-<n>"`, with `n` a per-process connection counter, and sends it in `params` alongside `v` / `sender` / `from`. Both sides log it on `channel-request` / `channel-response`. It is wire-safe under arch Mixed-version tolerance, because readers skip unknown fields. A server receiving a frame without `conn` from an older peer logs `srv_conn = "srv-<accept counter>"` instead.
  - The merged-log join key is `(instance, conn, corr)`.
```
**New:**
```
  - To follow one send across every side: `agent-run logs | jq -c 'select((.record.event|IN("send-issued","send-confirmed","send-refused")) and .record.corr==412)'`.
    - There is no `--instance` filter, because each side logs its own instance (see Trace context propagation).
    - The `event` filter keeps out an unrelated `channel-request` whose JSON-RPC `id` happens to be 412.
    - The `channel-*` lines of the same send join on `(conn, corr)`.
    - The hook side (`hook-invoked{hook_event:"user-prompt-submit"}`, `corr` null) joins by the wrapper's `instance` + `hook_event` + `timestamp` inside `run.confirm_window`;
  - `jq -c 'select(.record.level=="ERROR" and (.file|startswith("detail-")|not))'` lists every error and panic once. The detail-file duplicate of a panic line is excluded (§10).
- The harness `logs` glob must also cover `instances/*/diagnostics/detail-*.ndjson` (D-08). This additive harness change is part of tests amendment D-21.

### Trace context propagation

- **HTTP boundaries:** no W3C `traceparent` in v1. The browser sends no propagation header (obs-scope §3), and axum-tracing-opentelemetry 0.39.1 is rejected: it needs the deferred SDK, its W3C `traceparent` extraction has no sender, and its optional `/metrics` route would be a new listener. (Its MSRV 1.91 fits the raised 1.96 workspace floor, D-22, so MSRV is no longer a reason.)
  - Correlation on the `ui` side runs on the SSE `id:` cursor `<ViolaName>:<offset>` and `Last-Event-ID`.
  - `sse-opened` records `last_event_id_present` / `last_event_id_valid` and `resume_instances` (a count).
- **gRPC boundaries:** N/A (there is no gRPC in the stack).
- **IPC boundaries** (viola-channel JSON-RPC over interprocess 2.4.4 local sockets):
  - `corr` carries the JSON-RPC `id` on channel lines, `dialog_id` on dialog and hook lines, and the send `cursor` on send lines (tests).
  - **Additive `conn` field (D-10).** The client generates `conn = "<process>-<pid>-<n>"`, with `n` a per-process connection counter, and sends it in `params` alongside `v` / `sender` / `from`. Both sides log it on `channel-request` / `channel-response`. It is wire-safe under arch Mixed-version tolerance, because readers skip unknown fields. A server receiving a frame without `conn` from an older peer logs `srv_conn = "srv-<accept counter>"` instead.
  - **Join key.** The merged-log join key for channel lines is `(conn, corr)`, and `instance` is deliberately **not** part of it (D-25).
    - `conn` is already globally unique (`<process>-<pid>-<n>`, D-10).
    - Each side logs its **own** instance. `cli` and `mcp` log the caller's `VIOLA_NAME`, which is also `from`. `run` logs the wrapper's `ViolaName`. So the two sides of one call carry different `instance` values.
    - `srv_conn` lines from older peers have no client-side counterpart. They join only within the server's own file.
  - **`cli` instance precedence** (init step 3): the caller's `VIOLA_NAME` comes first. The explicit target `ViolaName` argument becomes `instance`, and names the `cli-<name>.ndjson` file, only when `VIOLA_NAME` is unset. So `viola send --to B` run by driver A logs `instance:"A"` to `cli-A.ndjson`.
  - `agent-run logs --instance B` therefore shows only B's side of a call. To reach the caller, take `conn` from B's `channel-request` and query with no `--instance` filter, for example `agent-run logs | jq -c 'select(.record.conn=="cli-4812-1")'`.
```

### Patch 4: Add `last` and `cli` to the span-area list and mark the D-16 override on the Vector 4 row
**Reason:** Dimension 2. The closed `<area>` list omits `last` and `cli`, which §4 uses (`last.client`, `cli.<verb>`). The Vector 4 trigger-map row still claims coverage of the "missing `VIOLA_NAME`" logging, which D-16 made unrealizable.
**Old:**
```
`<area>` is one of `run`, `pty`, `channel`, `state`, `hook`, `statusline`, `send`, `wait`, `answer`, `mcp`, `ui`. Examples: `run.version_gate`, `pty.paste_write`, `ui.sse_stream`. Span names are code-internal, not wire contract.
- **Event naming:** the `event` field is a closed kebab-case enum (the tests list plus the Section 12 extensions). It is compiled in viola-core as `ObsEvent` with a kebab-case `Display`.
- **Metric naming:** derived metrics (jq-computed, never instruments) are named `viola.<area>.<measure>`, for example `viola.hook.duration_ms` (Section 5).
- **Log fields:** snake_case field names. Enum values are kebab-case (arch JSON / enum naming). Instants are `*_at` in RFC 3339 UTC with milliseconds and `Z`. Durations are `duration_ms` as an integer.
- **Resource attributes:** `service.name` = `"viola"` (a viola-core constant, the arch-fixed binary name). `service.version` = `viola_core::VERSION` = `env!("CARGO_PKG_VERSION")`. `deployment.environment` is N/A (local-only). Both are emitted once per process on `process-start` as `service_name` / `version`.

**Trigger coverage map** (every obs-scope Section 5 trigger → plan section):

| Trigger | Covered in |
|---------|-----------|
| logging-sensitive Vector 1 (IPC) | §4 surface table (channel), §6 channel field allow-list, §8 table |
| logging-sensitive Vector 2 (paste text) | §4 Scenario 2 (`send-refused` `side`), §8 |
| logging-sensitive Vector 3 (GUI) | §4 Scenario 7, §6 HTTP wrapper, §8, §11 Spans |
| logging-sensitive Vector 4 (hook stdin) | §3 Logging stack (hook sink), §4 Scenario 4, §6 `hook-decision` `detail` codes |
```
**New:**
```
`<area>` is one of `run`, `pty`, `channel`, `state`, `hook`, `statusline`, `send`, `wait`, `last`, `answer`, `cli`, `mcp`, `ui`. `cli.<verb>` names the dispatch span of a short-lived verb that has no dedicated client span (for example `cli.list`, `cli.verify`, `cli.plugin_install`). Examples: `run.version_gate`, `pty.paste_write`, `ui.sse_stream`, `last.client`. Span names are code-internal, not wire contract.
- **Event naming:** the `event` field is a closed kebab-case enum (the tests list plus the Section 12 extensions). It is compiled in viola-core as `ObsEvent` with a kebab-case `Display`.
- **Metric naming:** derived metrics (jq-computed, never instruments) are named `viola.<area>.<measure>`, for example `viola.hook.duration_ms` (Section 5).
- **Log fields:** snake_case field names. Enum values are kebab-case (arch JSON / enum naming). Instants are `*_at` in RFC 3339 UTC with milliseconds and `Z`. Durations are `duration_ms` as an integer.
- **Resource attributes:** `service.name` = `"viola"` (a viola-core constant, the arch-fixed binary name). `service.version` = `viola_core::VERSION` = `env!("CARGO_PKG_VERSION")`. `deployment.environment` is N/A (local-only). Both are emitted once per process on `process-start` as `service_name` / `version`.

**Trigger coverage map** (every obs-scope Section 5 trigger → plan section):

| Trigger | Covered in |
|---------|-----------|
| logging-sensitive Vector 1 (IPC) | §4 surface table (channel), §6 channel field allow-list, §8 table |
| logging-sensitive Vector 2 (paste text) | §4 Scenario 2 (`send-refused` `side`), §8 |
| logging-sensitive Vector 3 (GUI) | §4 Scenario 7, §6 HTTP wrapper, §8, §11 Spans |
| logging-sensitive Vector 4 (hook stdin) | §3 Logging stack (hook sink), §4 Scenario 4, §6 `hook-decision` `detail` codes. The "missing `VIOLA_NAME`" item is **overridden**: no file and no line, asserted by absence (D-16, §4 E1) |
```

### Patch 5: Open the fmt builder's level ceiling to DEBUG so that `Targets` is the only level gate
**Reason:** Dimension 3. The sketch drops the catalog's `.with_max_level(...)`. The fmt `SubscriberBuilder` defaults to an INFO ceiling, and the outer `Targets` layer can only narrow it, so `diagnostics_level:"debug"` would never emit a DEBUG line. The sketch stays at 5 lines.
**Old:**
```
      .with_span_list(false).with_ansi(false).log_internal_errors(false)
      .with_timer(MillisUtc).with_writer(diag_writer /* BoxMakeWriter over Arc<File> | sink */)
      .finish().with(viola_targets(cfg.diagnostics_level)); // filter::Targets, built in code
```
**New:**
```
      .with_span_list(false).with_ansi(false).log_internal_errors(false).with_max_level(tracing::Level::DEBUG)
      .with_timer(MillisUtc).with_writer(diag_writer /* BoxMakeWriter over Arc<File> | sink */)
      .finish().with(viola_targets(cfg.diagnostics_level)); // filter::Targets = the only configurable level gate
```

### Patch 6: Append D-23 to D-25 (lost `tracing-log` failures, D-12 fallback closed, iteration-1 corrections)
**Reason:** Dimension 3.
- The obs-research `tracing-log` finding (portable-pty and notify failures dropped without the bridge) is not answered by D-11, which addressed field shape.
- D-21 met D-12's fallback condition, but the fallback was never recorded as closed.
- The join-key, ownership and artifact changes need a Decisions Log trail.

Historical entries are unchanged.
**Old:**
```
`2026-09-24` — Review 1: A/B/C accepted as tests amendment D-21; D-08 and the 1.96 floor (D-18) accepted as arch amendment D-22; tier Standard with Minimal exporter carve-outs confirmed (overseer, founder-delegated).
```
**New:**
```
`2026-09-24` — Review 1: A/B/C accepted as tests amendment D-21; D-08 and the 1.96 floor (D-18) accepted as arch amendment D-22; tier Standard with Minimal exporter carve-outs confirmed (overseer, founder-delegated).

`2026-09-24` — D-23 `tracing-log` off: what portable-pty / notify failures still surface
- **Decision:** With the bridge off (D-11) and no hand-installed `LogTracer`, `log` records from portable-pty 0.8.1 and notify 8.2.0 are dropped. The failure classes that obs-research flags are still captured from returned values or handles:
  - `pty.spawn` / ConPTY spawn failures are returned as `Err` from the seam. They become `process-exit{subject:"self", exit_code:1, detail:"internal-error"}`, with the chain in `detail-run.ndjson`.
  - Child exit without EOF is detected by the handle-wait thread (`exit_source`), independent of `log`.
  - notify watcher errors reach the event handler as `Err(notify::Error)`. They surface through `state.tail_notify` as `sse-closed{close_cause:"tail-error"}`.
  - Accepted loss: only third-party `log` context emitted without a returned error. It has no `event` / `corr` / `process`.
- **Rationale:** obs-research, tracing-subscriber `tracing-log` finding. D-11 answered the field-shape and rmcp-content concern, not the dropped-failure concern.
- **Impact:**
  - The chaos cases "child exit without EOF" and "notify watcher error" must each find a viola line.
  - If one does not, that is a seam gap, handled through D-11's revisit path. Never re-enable `tracing-log` and never install `LogTracer` by hand.
  - Affects §3 Logging stack and §6 per-module levels.
- **By:** `/andromeda-obs` iteration 1

`2026-09-24` — D-24 D-12 json-subscriber fallback closed
- **Decision:** D-12's fallback ("tests must confirm absent == null, or obs switches to json-subscriber 0.3.0") is closed. D-21 (accepted) states "absent = null" for `corr` / `instance`. D-11's separate seam-gap revisit stays open.
- **Rationale:** the condition named in D-12's Impact is met.
- **Impact:** §3 Null encoding, §6.
- **By:** `/andromeda-obs` iteration 1

`2026-09-24` — D-25 Iteration-1 contract corrections
- **Decision:**
  - The cross-process channel join key is `(conn, corr)`, not `(instance, conn, corr)`. Each side logs its own instance.
  - `cli` `instance` / file precedence is the caller's `VIOLA_NAME`, then the target argument.
  - `obs_event!` and the enums live in viola-core. `MillisUtc`, `viola_obs_init` and the panic hook live in the root bin, with a direct `chrono` dependency. The `disallowed-macros` exemption is an allow attribute inside the macro expansion.
  - The fmt builder ceiling is `Level::DEBUG`, and `Targets` is the only level gate.
  - The CI harness output goes to its own `harness-<os>` artifact.
- **Rationale:**
  - `(instance, conn, corr)` could not join MCP- or CLI-originated calls.
  - The default INFO ceiling made `diagnostics_level:"debug"` a no-op.
  - upload-artifact v4+ rejects a second upload of the same artifact name.
- **Impact:** §3 (Init body sketch, Snapshot / paste-to-AI, Trace context propagation, Bootstrap phases), §9.
- **By:** `/andromeda-obs` iteration 1
```

### Patch 7: Add bounded panic exemptions, a counting rule, the no-`ui` liveness gate, and `jq -e` perf assertions to §10
**Reason:** Dimension 4.
- `jq '.results[0].max < 1.0'` exits 0 on `false`, so the budget gate never fails.
- The "exactly one" panic invariant contradicts the §7 pre-init window, D-06 and D-16.
- With two `event:"panic"` lines per panic, the count is ambiguous.

Dimension 7: without a `ui` process there is no liveness gate, and the spine deadline reads like an obs TBD.
**Old:**
```
- **Zero unlogged panics:** every panic in `run`, `hook`, `mcp`, `ui` and `cli` produces **exactly one** single-line `level:"ERROR", event:"panic"` record in its role file, with no multi-line backtrace in home-level files, and `hook` still exits 0. Retry-once policies for panic handling are forbidden, in parallel with the tests' `retries = 0`.

**Standard+ invariants:**
- **Liveness signal** (replaces 10–30 s tick lines, D-13): `run` touches `heartbeat` every 1 s. `liveness-changed` fires on every live↔stale↔gone transition the `ui` observes. The flip happens at >5 s (4.9 s live / 5.1 s stale or gone).
```
**New:**
```
- **Zero unlogged panics:** every panic in `run`, `hook`, `mcp`, `ui` and `cli` **after init step 4** produces **exactly one** single-line `level:"ERROR", event:"panic"` record in its home-level role file, with no multi-line backtrace in home-level files, and `hook` still exits 0. Retry-once policies for panic handling are forbidden, in parallel with the tests' `retries = 0`.
  - **Counting rule:** "exactly one", the `viola.panic` counter and gate G2 (§9) count home-level role files only. The companion line in `detail-<process>.ndjson` also carries `event:"panic"` and is excluded. In merged `agent-run logs` output, count `select(.record.event=="panic" and (.file|startswith("detail-")|not))`.
  - **Bounded exemptions** (no file exists, so no line can be written):
    1. the pre-init window before step 4 (§7: clap parse and home resolution only);
    2. `cli` without a resolved instance (D-06);
    3. `hook` without `VIOLA_NAME` (D-16).

    The failure condition "a panic evidenced by an exit code without its line" applies only to processes that wrote `process-start`. For the exemptions the only evidence is the exit status. `hook` still exits 0, so a D-16 panic is unobservable by design (E1).

**Standard+ invariants:**
- **Liveness signal** (replaces 10–30 s tick lines, D-13): `run` touches `heartbeat` every 1 s. `liveness-changed` fires on every live↔stale↔gone transition the `ui` observes. The flip happens at >5 s (4.9 s live / 5.1 s stale or gone). When no `ui` runs, no `liveness-changed` line exists. The chaos class "stale heartbeat vs live pid" is then gated on the short-lived readers' `liveness` (`list --json`, `/api/sessions`) through `agent-run status` (`state: degraded`) (§3 Heartbeat ticks).
```

### Patch 8: Add a detail-file schema scope, a High-class canary scan, and the behaviour when the secret scan fails
**Reason:** Dimension 5: `panic_payload`, `backtrace`, `chain` and `drift_report` are not in the §6 catalog, so the detail lines D-21 brings into the harness glob fail `additionalProperties:false`. Dimension 6:
- Content-bearing detail files are uploaded to GitHub, but inputs are never stated to be synthetic.
- The pre-upload scan checks only Critical-class strings, with no High-class check.
- The two §11 CI bans conflict when the scan itself fails.
**Old:**
```
6. **Verification:** the tests secret-scan test (token, `Cookie`, `?t=`, `CLAUDE*` values, 0600) over every `diagnostics/` file, run in CI before artifact upload.

**Default-deny posture:** a field is logged only if it is named in this plan's Section 6 catalog. Unknown fields are a review failure, caught by the `schemas/diag-line.v1.json` conformance check (`additionalProperties: false` per event). No config key, flag or env var can widen the allow-list or disable redaction (security Anti-Patterns).
```
**New:**
```
6. **Verification:** the tests secret-scan test (token, `Cookie`, `?t=`, `CLAUDE*` values, 0600) over every `diagnostics/` file, run in CI before artifact upload.
   - **High classes** (prompt and send text, `last_assistant_message`, tool `input`, dialog answers, drift-report echoes):
     - Every E2E and integration input is synthetic and embeds a fixed canary string.
     - The scan also asserts that the canary never appears in a home-level `diagnostics/*.ndjson`. It may appear only in `detail-*.ndjson`.
     - The canary value and the test body are tests-owned.
   - **Detail-file upload** (§9) is admissible only because E2E inputs are synthetic. Any non-synthetic input source must first drop `detail-*.ndjson` from the upload path.
   - **Scan failure:**
     - The pre-upload ban wins: no `diag-<os>` or `harness-<os>` artifact is uploaded.
     - Instead, the scan step uploads its own hit report as `secret-scan-${{ matrix.os }}`: `file`, line, byte offset and pattern class, never the matched bytes.
     - So the failing job still leaves agent-readable evidence (§11 CI).

**Default-deny posture:** a field is logged only if it is named in this plan's Section 6 catalog. Unknown fields are a review failure, caught by the `schemas/diag-line.v1.json` conformance check (`additionalProperties: false` per event). No config key, flag or env var can widen the allow-list or disable redaction (security Anti-Patterns).
- **Detail-file scope:** `schemas/diag-line.v1.json` validates home-level lines only. `detail-<process>.ndjson` lines are validated by a sibling `schemas/diag-detail.v1.json`, with `additionalProperties: false`. It allows:
  - the same required fields as home-level lines: `timestamp`, `level`, `target`, `message`, `event`, `process` and `instance`, plus `corr` / `conn` when the home-level counterpart carries them;
  - exactly these content fields: `panic_payload` and `backtrace` (panic, §7), `chain` (§7 layer 3) and `drift_report` (§8 table).

  Both schemas count toward the §10 budget of zero conformance failures.
```

## Changelog
[Iteration 1] [substantive] Fixed the CI gates that could not fail (rg now PCRE2 and multiline, zero-panic jq now `-n -e`, perf jq now `-e`) and moved the harness output to its own artifact name. Set the fmt builder's level ceiling to DEBUG. Removed `instance` from the channel join key and fixed the canonical send query. Named the crate that owns each obs init piece and the `disallowed-macros` exemption. Added `last` and `cli` to the span areas and marked the D-16 override on the Vector 4 row. Added bounded panic exemptions and a counting rule, a detail-file schema scope, and a canary scan plus scan-failure fallback. Appended D-23 to D-25.

**Deferred to later iterations:** p99 derivation in the §5 snippet, ledger coverage, a pointer for the Section 1 D-09 residue, null encoding across the two emitters, append-atomicity claims for the shared files, panic catch sites, route/a11y boundaries, and the Section 11 ban additions.
