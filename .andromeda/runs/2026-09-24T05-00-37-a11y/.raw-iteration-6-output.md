## Analysis

**1. Downstream Readiness**
- Checked the Section 3 closed `violation_type` list against every gating non-axe check in Sections 3 and 5–10. The scrub-leak, csp-console and axe-rule-not-run ids are settled (Iterations 1 and 3). State-strip positions, lint paths, html-validate `elements`, the `always()` Aggregation step and the applicable-SC rule are all consistent.
- **Defect:** several gating checks have no id, so the row writer would have to make one up. The missing checks are:
  - the animation-loop / 5 s walk (SC 2.2.2 / 2.3.1);
  - the auto-follow viewport check (SC 2.2.2);
  - the Enter/Space activation contracts;
  - URL/title/scroll unchanged on focus, toggle or SSE (SC 3.2.1 / 3.2.2);
  - pinned copy / title / 401 instruction line / URN absence (SC 1.3.3 / 2.4.2 / 3.3.1 / 3.3.3);
  - readback-word and caption parity (SC 3.2.4);
  - the tape `lang` assertion (SC 3.1.2);
  - the session-cookie expiry / auto-dismiss check (SC 2.2.1).

  The Section 10 rule "every non-axe check (Section 3 `violation_type` list) passing" is therefore underspecified → Patch 1.
- **Defect:** the Section 6 Motion heading "Verification (`media-emulation`)" puts a `check_source` value where the other Section 6 headings put the `violation_type`. A writer could emit `violation_type:"media-emulation"`, which is not in the list → Patch 2.

**2. A11y Scope Faithfulness**
- The three tui boundary clauses each have their own nextest case (Section 3 Keyboard → Tooling, P4, Section 9). The CLI C0/C1, no-spinner and word-beside-SGR behaviours are pinned in P6 on all three OS legs. The v1.x brake note is excluded from coverage. The trim notice, `unverified-cli`, `unconfirmable` and the ATIS states have verification paths.
- P1 and P3–P6 SC lists match Section 1 (only the accepted `1.4.10 (≥ 760)` qualifier differs).
- **Defect:** Section 4 P2 adds SC 3.2.4, which Section 1 P2 does not have, and nothing says so. Section 1 cannot be patched (D-A11Y-15) → Patch 3 annotates P2.

**3. Tool Anchoring**
- Versions agree across Section 1, the body and the Decisions Log. The `options()`-ordering claim is labelled an assumption and backed by the `axe-rule-not-run` check. `jq` is labelled catalog-less. SC 2.4.6 is no longer credited to axe. The `target-size` / `wcag22aa` claim is anchored in research (a).
- **Defect:** the Section 3 per-SC map row for 4.1.2 still says "axe 4.1.2 rule set". Iteration 4 fixed this in Section 2 but not here. A reader can take "4.1.2" for an axe version (axe is 4.13.x) → Patch 4.

**4. Tier Calibration**
- The Section 10 state list matches the P1–P5 per-state axe verdicts and the Section 5 layouts, and each state has one owning spec. The budgets are tied to `duration_ms` and `stats.duration` and evaluated in the Aggregation step. Section 8 depth is within Standard. No patch.

**5. WCAG Mapping Discipline**
- SC 2.4.11 triggers agree between Section 3 and Section 5, including combined growth and `#tape-end`. The SC 2.5.8 spacing exception is defined. A/AA levels are present. SC 2.2.2 agrees across Sections 3, 5 and 6.
- The "single page" vs "single-page exception" wording for 3.2.3 is only cosmetic. Section 9 already handles both values, so no patch.

**6. Visual Design Verification to Design Tokens**
- The skip link and anchor are bound to `--ink`/`--surface-bay`. `--focus-ring`/`--surface-strip` is marked token-ratio only. `--strip-h` is scoped to the clip check. The token-sized element selector list is defined. No raw values found.
- **Defect:** `--radius` is listed as a focus-ring token (upstream: "corner token applied to the focus indicator (square everywhere)"), but the SC 2.4.7 check reads only `outlineColor/Width/Offset`. Nothing says whether `--radius` is asserted → Patch 5.

**7. Anti-Pattern Relevance**
- § CI and § Universal now name the `@axe-core/playwright` pick.
- § ARIA "NEVER overuse `aria-label` (use visible `<label>` first…)" is a template ban. v1 has no inputs and no `<label>` → one-line grounded rewrite to SC 2.5.3 label-in-name (Patch 6).
- § Visual repeats the § Motion "NEVER autoplay motion…" ban word for word. § Visual keeps ≥ 3 grounded bans without it → remove the copy (Patch 7).
- § Universal still holds three viola-specific items. Moving them is a restructure with no downstream breakage, so deferred.

## Patches

### Patch 1: §3 Structured violation JSON schema → closed `violation_type` list gives no id for several gating non-axe checks
**Reason:** Dimension 1 (downstream readiness: closed vocabularies). Gating checks for SC 1.3.3, 2.1.1/2.4.1 activation, 2.2.1, 2.2.2/2.3.1, 3.1.2, 3.2.1/3.2.2, 2.4.2/3.3.1/3.3.3 copy and 3.2.4 have no closed-list id. The row writer would have to invent ids, which breaks the closed list and the per-`violation_type` counts.
**Old:**
```
`surface-absence`, `csp-console`, `scrub-leak`, `axe-rule-not-run`. Each id has exactly one meaning:
```
**New:**
```
`surface-absence`, `motion-policy`, `auto-follow-scroll`, `keyboard-activation`, `context-change`, `copy-mismatch`, `identification-mismatch`, `lang-guessed`, `timed-expiry`, `csp-console`, `scrub-leak`, `axe-rule-not-run`. The ids from `motion-policy` to `timed-expiry` cover gating checks that would otherwise have no id: `motion-policy` = an `infinite` or looped animation, or one running longer than the SC 2.2.2 five-second threshold (SC 2.2.2, 2.3.1); `auto-follow-scroll` = auto-follow scrolls the focused `<summary>` out of the tape viewport (SC 2.2.2, 2.4.11; Section 5 → Auto-follow); `keyboard-activation` = an Enter / Space contract fails (skip link not landing on `#tape-end`, anchor jump not instant, `<summary>` not toggling; SC 2.1.1, 2.4.1); `context-change` = URL, title, scroll outside the tape, or focus changes on focus, on `<details>` toggle or on SSE arrival (SC 3.2.1, 3.2.2); `copy-mismatch` = pinned page text other than a state word is wrong: a fixed-copy string, the document title, an error strip or the 401 instruction line differs from its pinned value, or page text shows a token, `?t=`, URL, `.url` path or Problem URN (SC 1.3.3, 2.4.2, 3.3.1, 3.3.3); `identification-mismatch` = the readback word differs between the tape line and `<viola-transfer>`, or the web `columnheader` order differs from the CLI captions (SC 3.2.4); `lang-guessed` = a tape descendant carries a `lang` attribute (SC 3.1.2); `timed-expiry` = the session cookie has a timed expiry or a notice auto-dismisses (SC 2.2.1). Region-order differences across states (SC 1.3.2, 3.2.3) are `aria-tree`. Each id has exactly one meaning:
```

### Patch 2: §6 Motion tokens → verification label names a `check_source` value where a `violation_type` is expected
**Reason:** Dimension 1 (closed vocabularies). The other Section 6 verification headings put the `violation_type` in parentheses (`text-spacing-clip`, `token-placement`). Here `media-emulation` is a `check_source` value, not a closed-list id, so a writer could emit an id outside the list.
**Old:**
```
- **Verification (`media-emulation`):**
```
**New:**
```
- **Verification (`check_source:"media-emulation"`; `violation_type:"reduced-motion-override"` for the override checks, `motion-policy` for the computed-style walk):**
```

### Patch 3: §4 P2 SC list → SC 3.2.4 is added beyond Section 1 P2 without a note
**Reason:** Dimension 2 (paths and pointers). The only allowed difference between the Section 4 and Section 1 path SC lists is the `1.4.10 (≥ 760)` qualifier. Section 1 stays verbatim (D-A11Y-15), so the addition has to be declared where it is made.
**Old:**
```
  - **SCs:** 1.1.1, 1.3.1, 1.4.1, 1.4.11, 3.2.4, 3.3.1, 4.1.2, 4.1.3.
```
**New:**
```
  - **SCs:** 1.1.1, 1.3.1, 1.4.1, 1.4.11, 3.2.4, 3.3.1, 4.1.2, 4.1.3. SC 3.2.4 is a Section 4 addition beyond the Section 1 P2 list, which stays verbatim per D-A11Y-15. The transfer-marker word parity is a P2 surface, and its coverage comes from the Section 3 per-SC map row 3.2.4 and its `@sc-3.2.4` tag, not from the Section 1 list.
```

### Patch 4: §3 per-SC map row 4.1.2 → "axe 4.1.2 rule set" reads as an axe version
**Reason:** Dimension 3 (tool/version consistency). axe is 4.13.x. Iteration 4 fixed the same wording in Section 2 but not in the map row that `sc-coverage.json` is built from.
**Old:**
```
| 4.1.2 Name, Role, Value | yes | axe 4.1.2 rule set (
```
**New:**
```
| 4.1.2 Name, Role, Value | yes | axe 4.13.0 rules tagged for SC 4.1.2 (
```

### Patch 5: §6 Focus ring tokens → `--radius` is listed but its assertion status is not stated
**Reason:** Dimension 6 (token-named checks can be implemented by name). `--radius` is a bound focus-indicator token (upstream: corner token, square everywhere), but the SC 2.4.7 check compares only outline colour, width and offset. As written, the implementer cannot tell whether `--radius` must be asserted.
**Old:**
```
  - The token checker rows cover the ring contrast.
```
**New:**
```
  - The token checker rows cover the ring contrast.
  - `--radius` is not asserted. Corner shape carries no SC 2.4.7 or SC 1.4.11 requirement, and SC 2.4.13 (AAA) is not claimed.
```

### Patch 6: §11 ARIA → template `aria-label` ban refers to `<label>` in a plan with no inputs
**Reason:** Dimension 7 (generic bans with no viola grounding). v1 has no inputs or `<label>`. The viola risk is an `aria-label` that overrides visible text, which is the SC 2.5.3 check the plan already runs.
**Old:**
```
- NEVER overuse `aria-label` (use visible `<label>` first; `aria-label`
  only when visible label not feasible)
```
**New:**
```
- NEVER add `aria-label` where visible text already names the element (skip link, `N new lines below` anchor, `<summary>` lines, rack `<caption>`s). The accessible name must equal the visible text (SC 2.5.3; `toHaveAccessibleName`).
```

### Patch 7: §11 Visual → duplicate of the § Motion autoplay ban
**Reason:** Dimension 7 (duplicate bans across domains). § Motion carries the same ban with the grounded `--cock-dur` / `--fade-dur` note. § Visual keeps ≥ 3 viola-grounded bans without it.
**Old:**
```
- NEVER autoplay motion without `prefers-reduced-motion: reduce` respect
- NEVER place `--ink-dim`
```
**New:**
```
- NEVER place `--ink-dim`
```

## Changelog
[Iteration 6] [substantive] Added closed-list `violation_type` ids (`motion-policy`, `auto-follow-scroll`, `keyboard-activation`, `context-change`, `copy-mismatch`, `identification-mismatch`, `lang-guessed`, `timed-expiry`) for gating non-axe checks that had none, and mapped region order to `aria-tree`; changed the §6 Motion verification label from the `check_source` value to its violation ids; noted SC 3.2.4 in P2 as a Section 4 addition beyond Section 1 P2; changed the §3 map row "axe 4.1.2 rule set" so it no longer reads as a version; stated that `--radius` is not asserted; replaced the template `aria-label` ban with a grounded SC 2.5.3 ban and removed the duplicate § Visual autoplay ban.
