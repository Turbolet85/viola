## Output Protocol

You are an iteration agent improving the viola a11y plan draft at `D:/dev/projects/viola/.andromeda/runs/2026-09-24T05-00-37-a11y/a11y-plan-draft.md`. Check it against these upstream sources in the same run directory: `upstream-context.md` (binding excerpts), `a11y-scope.md` (Phase 1), `a11y-research.md` (Phase 2 tool catalog) and `review-feedback-1.md` (overseer decisions, which are binding).

Follow these rules:

1. **Output patches and a changelog only.** Never reproduce the full document or a full section.
2. **Patch format:**

   ### Patch N: <one-line description naming the section, e.g. "§4 State strips row — invalid `<p>` placement inside table">
   **Old:**
   ```
   <exact text copied from the draft, long enough to be unique>
   ```
   **New:**
   ```
   <replacement text>
   ```

   - Write at most 8 patches per iteration. Spend them in this order:
     1. Scope faithfulness and downstream-readiness defects (dimensions 1 and 5);
     2. tier or WCAG mapping errors (dimensions 2 and 7);
     3. tool anchoring errors (dimension 3);
     4. design-token binding violations (dimension 6);
     5. anti-pattern relevance (dimension 4).
   - Every **Old:** block must match the draft byte for byte.
   - Keep token names, SC IDs, versions and paths exactly as upstream spells them.
3. **Changelog:** write exactly ONE line for the whole iteration, never one line per patch:
   `[Iteration N] [substantive|cosmetic] <description summarising all patches>`
4. **Prohibited:**
   - reproducing the full document;
   - restructuring or renumbering sections without a defect-driven reason;
   - labelling wording, formatting or synonym changes "substantive";
   - reopening decisions that `review-feedback-1.md` or Decisions Log entries marked "accepted (overseer)" already settled. This covers D-A11Y-02/03/04/06/09/14/19, the SC 1.4.10 < 760 CSS px exception, the 401 copy binding, the CSS-only ellipsis, the ESLint lockfile pin and the deferral of the v1.x brakes;
   - adding content owned by other specialists:
     - test code blocks over 5 lines (`test(...)`, `describe(...)`, `#[test]`, `fn test_*`);
     - instrumentation code;
     - threat-model or auth-flow detail;
     - raw design token values (hex, px, ms);
     - a11y implementation code blocks over 5 lines (`onKeyDown` handlers, `element.focus()` / FocusTrap blocks, `axe.run` / `axe.configure` blocks, `aria-*` injection markup);
   - recommending ARIA on non-semantic hosts (e.g. `role="row"` on `<viola-session-row>`). Semantic HTML comes first;
   - recommending Lighthouse, pa11y, WAVE, Stark or any manual-only or vendor-locked method as primary verification.
5. **If you find no issues:** output "No patches" followed by `[Iteration N] [cosmetic] No defects found along dimensions 1–7`.

## Analysis Dimensions

### 1. A11y Scope Faithfulness [high]

- **Section 1 fidelity.** Diff Section 1 (A11y Scope Summary) line by line against `a11y-scope.md`. D-A11Y-15 allows only `[resolved: …]` pointers in place of deferral clauses. Has anything else been paraphrased, dropped or reordered?
  - Look at the tier justification, the 11 web-spa entities, the three not-assertable groups, the tui boundary-only reason, the six paths and the three triggered and six not-triggered triggers.
  - Does every `[resolved: …]` pointer target a section and D-A11Y entry that actually resolves it? Examples:
    - "Section 5 → Focus order per layout" for initial focus;
    - "Section 7 → Language of page and parts" for SC 3.1.2;
    - "Section 4 → component catalog and Section 7 → Live regions" for the 401 access strip role.
- **Entity and path coverage.** Does every assertable entity have a row in the Section 4 per-component catalog and a verification path in Sections 5, 6 or 7? Entities to check:
  - page shell;
  - `<viola-atis>` (including `budget-paused`, `skipped N · N · N` and `expired`);
  - WRAPPED rack with cocked / stale / `cli_verified:false` states;
  - UNWRAPPED · READ-ONLY rack;
  - `<viola-transfer>`;
  - `<viola-readback>` with `open` / `read back` / `unable · <reason> · <detail>` / `unconfirmable`;
  - `<viola-event-feed>`, including the trim notice, the `N new lines below` anchor and `#tape-end`;
  - status announcer;
  - skip link;
  - state strips;
  - global token layer.

  Is the tui `viola run` boundary covered as a boundary assertion, never a conformance claim? The boundary is zero viola bytes, keystrokes never blocked past the atomic paste, and focus/mouse/resize sequences ignored. Are the 403 page, v1.x brakes, `/api/*`, plugin/IPC, internal crates and phone view fully excluded, and not quietly asserted anywhere (e.g. a Section 7 "Form labels" note on brakes that reads like a v1 requirement)?
- **Paths and triggers.** Are P1–P6 each carried into Section 4 "Must-be-accessible path scenarios" with roles, focus order and SC list consistent with Section 1?
  - Section 1 P1 lists SC 1.4.10 without a qualifier, while Section 4 P1 says "1.4.10 (≥ 760)". Is that drift?
  - Does Section 4 P4 drop SCs that Section 1 P4 lists?
  - Is each triggered trigger answered by its owning section: visual-discrimination → Section 6 contrast plus state-colour tokens; keyboard-only → Section 5; screen-reader-priority (scoped) → Section 7 with the `role="log"` implicit-live resolution?

### 2. Tier Calibration (WCAG Mapping) [high]

- **Depth versus tier.** The tier is Standard (1), web-spa only. Are all 12 sections present at Standard depth without drifting into Comprehensive?
  - Is Section 8 correctly scoped to SC 2.2.1 / 3.3.1 / 3.3.3 / 3.2.1–3.2.4 / 3.3.7 / 3.3.8 absence, with SC 2.2.6, 3.1.5 and 3.3.9 stated as not claimed, given no cognitive trigger fired?
  - Is Section 7 scoped to announcement correctness, as Section 1 says for screen-reader-priority, rather than a full AT matrix? Is gating on @guidepup/virtual-screen-reader 0.33.0 proportionate to that scope?
- **Conformance label.** Is "WCAG 2.1 AA + SC 2.4.11 + SC 2.5.8" with the SC 1.4.10 < 760 CSS px exception used identically in Sections 2, 3, 10 and 11 and D-A11Y-04? Does any section drift to "WCAG 2.2 AA", which is banned in Section 11 Strategy? Do the cli and tui rows anywhere imply a conformance claim instead of "output-discipline stand-ins for SC 1.3.2 / 1.4.1 / 3.3.1"?
- **SLO tier defaults.** Does Section 10 carry both Standard-tier defaults: zero AA violations on the P1–P5 states, and zero new violations per PR?
  - The binary `violations: []` verdict with no fingerprint baseline is the stated regression mechanism. Is it actually sufficient?
  - Are "axe `analyze()` < 30 s per state render", "< 10 min total" and "> 50 % over budget fails" measurable from artifacts the plan names (`duration_ms` in the axe attachment, Playwright JSON)?

### 3. Tool Anchoring (Catalog ↔ Plan) [high]

- **Version match.** Does every tool named in Sections 2, 3, 4, 7 and 9 appear in `a11y-research.md` with the same version?
  - `@axe-core/playwright` 4.13.0 / axe-core 4.13.0; Playwright Test 1.63.0;
  - colorjs.io 0.7.1; tabbable 6.5.0; @guidepup/virtual-screen-reader 0.33.0;
  - html-validate 11.16.0; eslint-plugin-lit-a11y 5.1.1; Lit 3.3.3;
  - assert_cmd 2.2.2 / predicates 3.1.4 / trycmd 1.2.1;
  - NVDA 2026.2 / Orca GNOME 50; @guidepup/guidepup 0.34.0 / @guidepup/playwright 0.19.1;
  - `actions/upload-artifact` v7.0.1.

  Research lists portable-pty at 0.9.0 while the plan uses the inherited `=0.8.1`. Is that reconciled consistently through D-A11Y-13 everywhere portable-pty appears? Are `jq` (Section 9 aggregation) and ESLint core (lint stage, lockfile-pinned) catalog-less? If so, is that stated explicitly instead of left as unanchored picks?
- **Rationale for rejections.** For each multi-candidate category, does the Decisions Log give the research trade-off?
  - colorjs.io vs `culori` 4.0.2 (D-A11Y-10);
  - no runtime focus library vs `focus-trap` 8.2.2;
  - native HTML vs `@lion/ui` 0.21.1 / Material Web;
  - `lighthouse` 13.5.0 / `pa11y` 10.0.0 / `pa11y-ci` 4.1.1 / `@lhci/cli` 0.15.1 / `playwright-lighthouse` 4.0.0 / WAVE API (D-A11Y-05).

  Does any rejected tool still show up as an active pick elsewhere?
- **SC claims per tool.** Is each tool's research `**WCAG SCs covered:**` honoured, so the plan never credits a tool with an SC research did not assign to it?
  - axe's 24 SCs / 70 rules under `['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']`;
  - tabbable → SC 2.4.3 / 2.1.1;
  - VSR → SC 4.1.3 / 4.1.2 / 1.3.1 / 1.3.2.

  Is the claim that `.options({rules})` enabling the five best-practice rules survives the later `withTags()` `runOnly` anchored in research? Or is it an unverified assumption that should be flagged?

### 4. Anti-Pattern Relevance [medium]

- **Contradictions with viola's stack.** Do any Section 11 bans contradict decisions made for this stack? For example, § CI says "NEVER ship lint-only a11y without runtime axe-core / pa11y / Lighthouse", but D-A11Y-05 and § Strategy ban pa11y and Lighthouse as a second browser stack. Do any bans name tools viola does not use, and so need rewording to the actual picks (`@axe-core/playwright` 4.13.0 inside `scripts/agent-run.* run --browser`)?
- **Universal bans.** Does § Universal hold only genuinely cross-stack bans? Items that look viola-specific include:
  - "never parse the rendered `claude` TUI" (R7);
  - "never write rows into `<home>/diagnostics/`";
  - "never overclaim in the GUI".

  Should those move to § Screen Reader, § CI or § Strategy, leaving the ≥ 4 universal bans intact (manual-only, vendor-locked, inaccessible captcha/modal, conformance without machine evidence)?
- **Duplicates and grounding.** Are bans duplicated across domains, such as "NEVER autoplay motion without `prefers-reduced-motion: reduce` respect" in both § Visual and § Motion? Are bans repeated inline in section bodies instead of cross-referenced? Does each domain have ≥ 3 bans grounded in viola pitfalls, e.g.:
  - § ARIA: `display: contents` hosts, no Shadow DOM, `aria-hidden` on `<viola-readback>`;
  - § Keyboard: no focus theft on SSE / E4 / E5, no smooth scroll to `#tape-end`;
  - § CI: no `bypassCSP` or axe `locale`, scrub `nodes[].html`.

  Or do any domains still hold generic template bans only (§ Strategy "NEVER skip POUR principle coverage")?

### 5. Downstream Readiness [high]

- **Scaffolding from Section 3 alone.** Can `setup-project` build `e2e-web/fixtures/a11y.ts` from the Section 3 "Bootstrap phases" plus the harness bodies alone? Is each of these concretely specified, or implied only?
  - `makeAxeBuilder`, with its five best-practice rule ids enumerated where `BP_RULES` is referenced;
  - the scrubber;
  - the violation-row writer;
  - the VSR route/import helper and the MutationObserver fallback.

  Is the html-validate config that "declares the `viola-*` elements" specified enough to set content models? `<viola-session-row>` and `<viola-transfer>` sit as `display: contents` hosts inside `<tbody>` rendering `<tr>`/`<td>`, so without a permitted-content declaration, `prefer-native-element` and the content-model rules may reject them.
- **SC coverage gate.** Is `e2e-web/a11y/sc-coverage.json` derivable without guessing? Section 3's per-SC table mixes "yes", "no … absence", "single-page exception" and "indeterminate-language content". Does Section 9 or 10 state which of those rows need ≥ 1 passing `@sc-*`-tagged test? The `surface-absence` spec would carry `@sc-1.2.x` / `@sc-3.3.2` tags. Does the Aggregation stage, which runs "after `gate`", still run and fail CI when `gate --require playwright` has already failed the job?
- **Route and CI inputs.** Can `route` order the seven bootstrap phases, and can setup-project wire `ci.yml`, without undefined inputs?
  - The Lint-stage targets are "the Lit sources" and "the embedded `assets/index.html`". Is the crate or path (`viola-ui`) named?
  - Is the `violation_type` closed list consistent with its uses? Section 3 scrubbing classifies an unscrubbed token as "`csp-console`-class severity `critical`", which contradicts the rule that non-axe checks emit `serious` and misuses `csp-console`. Does that need its own id in the closed list?
  - Is `event:"a11y-violation"` (D-A11Y-09) carried consistently with `level:"ERROR"` for gating rows and `level:"WARN"` for non-gating `incomplete` rows?

### 6. Visual Design Verification to Design Tokens [high]

- **Token names match upstream.** Does every token named in Sections 3, 5, 6 and 11 exist verbatim in `upstream-context.md` Section 3 A11y-Relevant Design Tokens?
  - `--ink`, `--ink-dim`, `--ink-on-paper`, `--attention`, `--handoff`;
  - `--surface-bay`, `--surface-strip`, `--surface-inset`;
  - `--rb-fill`, `--rb-rule`, `--rb-strike`, `--rule-info`, `--rule-field`, `--rule-deco`;
  - `--focus-ring`, `--focus-w`, `--focus-offset`, `--radius`;
  - `--line-h`, `--strip-h`, `--rb-size`;
  - `--cock-dur`, `--cock-ease`, `--cock-offset`, `--fade-dur`, `--fade-ease`;
  - the `--fs-*`, `--fw-*`, `--track-*`, `--stretch-label`, `--lh-*`, `--strip-cols*` and `--tape-cols` families.

  Beyond that:
  - Are the two derived pairs (`--rule-field`/`--surface-bay` for `expired`, and `--rb-strike`/`--surface-inset`) traced to the design text quoted in D-A11Y-18, with no invented pairing?
  - Is `color-border-focus` (the layout name) confined to D-A11Y-19 and Section 6's rename note?
- **Raw values.** Scan Sections 3, 5, 6 and 10 for raw values bleeding in: hex, `Npx`, `Nms`/`Ns`, rgb/hsl, cubic-bezier.
  - Allowed: WCAG thresholds 4.5:1, 3:1 and 24×24 / 44×44, and the SC 2.2.2 five-second threshold, as long as it is framed as the SC threshold and not a motion token value.
  - Are the viewport widths "1024", "760–1023", "< 760 CSS px" and "1536 CSS px at 200 %" attributed to the tests/layout plans as test viewports, not presented as a11y-owned layout values?
  - Does the SC 1.4.12 override stay described "exactly as SC 1.4.12 defines them", without numeric spacing values?
- **Pair table.** For every row of the Section 6 "Color contrast pairs" table, is the SC (1.4.3 text / 1.4.11 non-text) and ratio correct for the context?
  - Is `--ink-dim`/`--surface-inset` truly text (SC 1.4.3)?
  - Does `--rule-deco` correctly carry "none (below 3:1 by design)" with a state-word compensating check?
  - Are the negative placements (`--ink-dim`, `--attention` as text, `--handoff` as text on `--surface-strip`) exactly the three bans design states?
  - Do the forced-colors expectations match the design's fallback wording? That wording is: borders survive, strike → dashed outline + `unable`, cock band → system highlight.

### 7. WCAG Mapping Discipline [high]

- **Levels in the per-SC table.** Section 3's per-SC verification map has no conformance-level column. Should each row state A / AA, plus the 2.2 origin for SC 2.4.11, 2.5.8, 2.5.7, 3.2.6, 3.3.7 and 3.3.8, so the "WCAG 2.1 AA + SC 2.4.11 + SC 2.5.8" label is auditable? Is SC 4.1.1 Parsing flagged as applying only under the 2.1 label (obsolete in 2.2)? Does the Section 4 path SC lists and Section 2 pyramid state levels where ambiguous?
- **Verification paths hold up.** Is each "yes" row backed by a verification path that actually proves that SC?
  - SC 1.4.4 Resize text is proxied by the 760–1023 reflow check as "a 1536 CSS px desktop at 200 %". Is that proxy valid, or does it need a stated assumption?
  - SC 2.4.11 is checked as "not fully covered by the `<viola-atis>` rect". Does that match the Minimum definition, and is it also asserted after skip-link activation lands focus on the zero-height `#tape-end`?
  - SC 2.5.8 uses `boundingBox()` on `<summary>` rows sized by `--line-h`. Does the spacing-exception path say how it is computed?
  - SC 3.1.2 is claimed under the indeterminate-language provision. Is that provision correctly cited?
- **Specific SC IDs everywhere.** Do all WCAG-relevant bodies in Sections 4, 5, 6, 7, 8, 10 and 11 cite specific SC IDs rather than generic "WCAG AA"? Candidates to check:
  - Section 11 § Strategy "NEVER skip POUR principle coverage";
  - § ARIA "NEVER overuse `aria-label`";
  - § Screen Reader "NEVER add excessive ARIA noise";
  - Section 10 "Zero WCAG AA violations".

  With no regime in the security excerpt (no Section 508 / EAA / EN 301 549), does the plan state consistently in Sections 1, 3 and 12 that no regime-mandated level overrides the label?

Read the document, analyze along all dimensions, output patches and changelog.