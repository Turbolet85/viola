## Analysis

**1. Downstream Readiness**
- The closed `violation_type` list, `scrub-leak`/`csp-console`, the absent `wcag_criterion`, the state-strip and trim-notice positions, the lint paths and configs, and the html-validate `elements` (tbody hosts, phrasing `viola-readback`) are all consistent now. No patch needed for these.
- **Defect (Aggregation gate, §2 Test tags):** Section 9 Aggregation fails CI when any `sc-coverage.json` row has zero passing tagged tests. §2 Test tags assigns owners only for the path SCs and the re-asserted reflow and route checks. Nothing says which spec carries `@sc-*` for the rows covered only by axe (for example 1.3.4 `css-orientation-lock`), or for the non-axe rows with no path owner (1.3.3, 2.1.4, 2.2.1, 2.4.4, 2.5.3, 3.1.2, 3.2.1, 4.1.1, and the 2.5.1/2.5.2/2.5.4/2.5.7 tabbable-oracle actuator test). A literal materialization produces a failing gate. → Patch 1.
- **Defect (Bootstrap, §3 a11y-ci-gate-wire):** the bullet says only "post-gate `jq` SC-coverage check against `sc-coverage.json`". It does not say that this phase creates `sc-coverage.json`, and it omits `if: always()` and the budget evaluation that Section 9 and Section 10 require. setup-project reads this list, so it would wire the step so it gets skipped once `gate` fails. → Patch 2.

**2. A11y Scope Faithfulness**
- The three tui boundary clauses each have a nextest case (§3 Keyboard harness, P4). The CLI C0/C1 escaping, static-output and word-beside-SGR checks are pinned in P6. No patch needed.
- **Defect (ATIS state words):** the §3 SC 1.4.1 map row says `TAPE connecting`, `TAPE live`, `TAPE stopped` and `skipped N · N · N` are "each asserted in its ATIS `<dd>` by `a11y-path1` / `a11y-path5`". However, the P1 test list names only `budget-paused`, and P5 never asserts the ATIS `TAPE stopped` word. → Patches 3 and 4.
- **Pointers:** "[resolved: Section 4 → landmarks]" does not match the heading "Per-surface landmark roles inventory". That pointer and the focus-order pointer both omit their existing D-A11Y IDs (02 and 07). The P5 role pointer omits D-A11Y-06 and the catalog row. → Patches 6 and 7. These are `[resolved: …]` pointers, which D-A11Y-15 allows.

**3. Tool Anchoring**
- These claims match research: `target-size` `enabled:false` (item (a)), axe 4.13.0 selector escaping (#5273), 24 SCs / 70 rules, NVDA 2026.2, Orca GNOME 50, the guidepup and upload-artifact versions, and culori/focus-trap/@lion.
- The `options()` ordering is labelled an assumption and checked by `axe-rule-not-run`. `jq` is labelled catalog-less, and there is no SC 2.4.6 credit to axe.
- No patch needed.

**4. Tier Calibration**
- **Defect (`TAPE connecting`):** this is a distinct rendered ATIS state. It is the §6 info state (`--ink-dim`), and the §3 map says the harness holds it before `sse-opened`. It gets no axe verdict in P1 and is missing from the §10 SLO state list, so rendered contrast and `region` are never evaluated for it. → Patches 3 and 5.
- The `duration_ms` / `stats.duration` budgets are tied to the Aggregation step. No patch needed.

**5. WCAG Mapping Discipline**
- SC 2.4.11 triggers match across §3 and §5, including combined growth and `#tape-end`. The SC 2.5.8 spacing exception is defined, and the SC 2.2.2 wording is aligned. Levels are listed.
- One wording inconsistency remains: "single-page exception" vs "single page". Section 9 handles both, so it does not break downstream. Not patched (cosmetic).
- The generic § Strategy "POUR" ban cites no SCs. → Patch 8.

**6. Design Token Binding**
- Section 6 pairs match upstream token names. The D-A11Y-18 derived pairs are traced. The skip link and anchor are bound to `--ink`/`--surface-bay`. `--focus-ring`/`--surface-strip` is marked token-ratio only. The selector list for token-sized elements is defined.
- Forced-colors wording matches the design. No raw values, only SC thresholds and test viewports.
- No patch needed.

**7. Anti-Pattern Relevance**
- § CI and § Universal now name the `@axe-core/playwright` pick.
- The § Strategy POUR ban is a template ban with no viola grounding. It is rewritten as a one-line ban grounded in `sc-coverage.json` (Patch 8).
- § Universal still holds three viola-specific bans (TUI parsing, diagnostics rows, overclaim). Moving them would take several patches. It is a restructure with low functional value, so it is not patched.

## Patches

### Patch 1: §2 Test tags — name the `@sc-*` owner for axe-only rows and for non-axe rows with no path owner
**Reason:** Dimension 1 (coverage gate derivable). The Aggregation step fails CI on any row with zero tagged tests, and several §3 map rows have no named tagging spec.
**Old:**
```
  this, those rows have zero tagged tests and the Section 9 Aggregation
  gate fails.
```
**New:**
```
  this, those rows have zero tagged tests and the Section 9 Aggregation
  gate fails. The same rule covers every other map row. A test that
  asserts a `makeAxeBuilder` verdict carries the `@sc-*` tag of each
  map row whose path names an axe rule, because one `analyze()`
  verdict verifies all of them (for example `@sc-1.3.4` through
  `css-orientation-lock`). Map rows whose non-axe check has no path
  owner are asserted and tagged in `a11y-path1`: 1.3.3 (empty-rack
  instruction copy; `a11y-path5` also tags it for the 401 / 503
  instruction copy), 2.1.4 (character keys), 2.2.1 (session-cookie
  expiry), 2.4.4 and 2.5.3 (accessible names), 3.1.2 (tape `lang`),
  3.2.1 (keyboard-walk context checks), 4.1.1 (rendered-DOM
  html-validate), and the 2.5.1 / 2.5.2 / 2.5.4 / 2.5.7
  tabbable-oracle actuator test. `surface-absence` carries the
  absence-row tags (Section 9 → Aggregation).
```

### Patch 2: §3 Bootstrap a11y-ci-gate-wire — create `sc-coverage.json` and run Aggregation with `if: always()`
**Reason:** Dimension 1 (CI wiring inputs defined). setup-project materializes from this list, but it omits creating the file, the `always()` run condition and the budget evaluation that §9 and §10 require.
**Old:**
```
  - A post-gate `jq` SC-coverage check runs over the Playwright JSON against `e2e-web/a11y/sc-coverage.json` (Section 9).
```
**New:**
```
  - This phase creates `e2e-web/a11y/sc-coverage.json` with one entry per Section 3 per-SC map row (ranges expanded; SC id, level and "Applies in v1" value). A post-gate `jq` SC-coverage and budget step runs over the Playwright JSON and `e2e-web/test-results/a11y/*.ndjson` against that file. It runs with `if: always()`, as the `e2e-web/test-results/` upload step does, so it still runs and reports after `gate --require playwright` has failed the job. It writes `sc-coverage-report.json` (Section 9 → Aggregation row; Section 10 budget failure condition).
```

### Patch 3: §4 P1 Tests — add the `TAPE connecting` axe verdict and the ATIS `<dd>` words the SC 1.4.1 row assigns to `a11y-path1`
**Reason:** Dimension 2 (entity coverage: the ATIS states named in the §3 SC 1.4.1 row are not in P1) and Dimension 4 (a rendered state without an axe verdict).
**Old:**
```
    - axe verdict in each of those states, and in the other `a11y-path1`-owned Section 10 states: tape-line-expanded, scrolled-up with the `N new lines below` anchor present, skip link focused, after skip-link activation, tape trimmed past the DOM cap, and the boxed ATIS states (`budget-paused`, `expired`, nonzero `skipped`; each analyzed after its word renders);
    - the tests' reflow check (`scrollWidth <= clientWidth` at 1024 and 760–1023) and route check (only `/` serves HTML), re-asserted under a11y tags (Section 2 → Test tags);
    - E3 transfer rows as `row` with one `cell`;
    - E4 `Last-Event-ID` resume keeping the tree and focus;
    - the `budget-paused` word in the ATIS `<dd>`;
```
**New:**
```
    - axe verdict in each of those states, and in the other `a11y-path1`-owned Section 10 states: tape-line-expanded, scrolled-up with the `N new lines below` anchor present, skip link focused, after skip-link activation, tape trimmed past the DOM cap, `TAPE connecting` (held by the harness before `sse-opened`, never reached by a timeout), and the boxed ATIS states (`budget-paused`, `expired`, nonzero `skipped`; each analyzed after its word renders);
    - the tests' reflow check (`scrollWidth <= clientWidth` at 1024 and 760–1023) and route check (only `/` serves HTML), re-asserted under a11y tags (Section 2 → Test tags);
    - E3 transfer rows as `row` with one `cell`;
    - E4 `Last-Event-ID` resume keeping the tree and focus;
    - the ATIS `<dd>` words `budget-paused`, `expired`, `TAPE connecting`, `TAPE live` and `skipped N · N · N` (SC 1.4.1 map row; `TAPE stopped` is asserted by `a11y-path5`);
```

### Patch 4: §4 P5 Tests — assert the ATIS `TAPE stopped` word that the SC 1.4.1 row assigns to `a11y-path5`
**Reason:** Dimension 2 (the §3 SC 1.4.1 row credits `a11y-path5` with the ATIS `TAPE stopped` word, but P5 never asserts it).
**Old:**
```
    - for 401: the access strip is shown
```
**New:**
```
    - for `TAPE stopped`: the ATIS `<dd>` shows `TAPE stopped` and the tape-top `<p>` is present (SC 1.4.1 map row);
    - for 401: the access strip is shown
```

### Patch 5: §10 SLO state list — add `TAPE connecting` and its owning spec
**Reason:** Dimension 4 (the SLO list must be the union of the path axe verdicts; Patch 3 adds `TAPE connecting` to P1).
**Old:**
```
ATIS boxed `budget-paused` / `expired` / nonzero `skipped`, cocked, readback-refused, readback-`unconfirmable`, WHEEL `human`, stale, 503, 404 / 405 rack strips after first render, `TAPE stopped`, 401 access strip). This list is the union of the per-state axe verdicts in Section 4 P1–P5 and the Section 5 layouts; a state added to either is added here. Each state's `analyze()` belongs to exactly one owning spec: `a11y-path1` (steady, bay-narrow, first-reading/empty, tape-line-expanded, scrolled-up, skip link focused, after skip-link activation, tape trimmed, ATIS boxed states)
```
**New:**
```
`TAPE connecting` (harness-held before `sse-opened`), ATIS boxed `budget-paused` / `expired` / nonzero `skipped`, cocked, readback-refused, readback-`unconfirmable`, WHEEL `human`, stale, 503, 404 / 405 rack strips after first render, `TAPE stopped`, 401 access strip). This list is the union of the per-state axe verdicts in Section 4 P1–P5 and the Section 5 layouts; a state added to either is added here. Each state's `analyze()` belongs to exactly one owning spec: `a11y-path1` (steady, bay-narrow, first-reading/empty, tape-line-expanded, scrolled-up, skip link focused, after skip-link activation, tape trimmed, `TAPE connecting`, ATIS boxed states)
```

### Patch 6: §1 P1 `[resolved: …]` pointers — land on the real subsection and name the D-A11Y IDs
**Reason:** Dimension 2 (pointers). "Section 4 → landmarks" does not match the heading "Per-surface landmark roles inventory", and neither pointer names its existing Decisions Log ID. These are `[resolved: …]` pointers, which D-A11Y-15 allows patching.
**Old:**
```
[resolved: Section 4 → landmarks]), `table` / `row` / `columnheader` / `cell`, `log`, `link` (skip link and new-lines anchor), native `<details>`/`<summary>`.
- **Required focus order:** initial focus at document start ([resolved: Section 5 → Focus order per layout]).
```
**New:**
```
[resolved: Section 4 → Per-surface landmark roles inventory; Decisions Log D-A11Y-02]), `table` / `row` / `columnheader` / `cell`, `log`, `link` (skip link and new-lines anchor), native `<details>`/`<summary>`.
- **Required focus order:** initial focus at document start ([resolved: Section 5 → Focus order per layout; Decisions Log D-A11Y-07]).
```

### Patch 7: §1 P5 `[resolved: …]` pointer — name the catalog row and D-A11Y-06
**Reason:** Dimension 2 (pointers). "component catalog" should name the "Per-component pattern catalog → State strips" row and the existing D-A11Y-06 announcer-scope decision.
**Old:**
```
[resolved: Section 4 → component catalog and Section 7 → Live regions]
```
**New:**
```
[resolved: Section 4 → Per-component pattern catalog → State strips row and Section 7 → Live regions; Decisions Log D-A11Y-06]
```

### Patch 8: §11 § Strategy — replace the generic POUR ban with one grounded in SC IDs and `sc-coverage.json`
**Reason:** Dimension 7 (generic template ban with no viola grounding) and Dimension 5 (generic wording should cite specific SCs). One-line fix. This is the last iteration, so it cannot be deferred.
**Old:**
```
- NEVER skip POUR principle coverage at chosen tier (e.g.,
  Operable without Perceivable verification)
```
**New:**
```
- NEVER leave a POUR row of the Section 2 pyramid without gating machine evidence (for example Operable SC 2.1.1 / 2.4.3 / 2.4.7 / 2.4.11 resting only on the manual keyboard pass, or Perceivable SC 1.4.3 / 1.4.11 resting on visual review). Every SC in `sc-coverage.json` needs a passing `@sc-*` test.
```

## Changelog
[Iteration 10] [substantive] Named `@sc-*` tag owners for the axe-only map rows and for non-axe rows with no path owner, so the Aggregation gate cannot fail on untagged rows. Made the bootstrap a11y-ci-gate-wire phase create `sc-coverage.json` and run the coverage and budget step with `if: always()`. Added the harness-held `TAPE connecting` state to the P1 axe verdicts and the §10 SLO list, with `a11y-path1` as owner. Aligned P1 and P5 with the SC 1.4.1 row's ATIS `<dd>` word assertions. Made the §1 `[resolved: …]` pointers land on real subsections with their D-A11Y-02/06/07 IDs. Replaced the generic § Strategy POUR ban with one grounded in SC IDs.
