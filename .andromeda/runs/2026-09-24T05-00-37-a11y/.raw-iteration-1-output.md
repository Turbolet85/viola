## Analysis

**Dimension 1: Downstream readiness**
- The anchor defect is confirmed. At line 573, a scrub failure emits `"csp-console"`-class with severity `critical`. That breaks the rule that non-axe checks emit `serious`, and it reuses the CSP id, which Section 10 keeps as a separate failure condition. The closed list has no scrub id. Patch 1.
- The anchor defect is confirmed. The State strips row puts the `<p>` "after the … heading position", but that heading sits inside `<caption>`, so the `<p>` would land inside `<table>` (invalid). `TAPE stopped` is placed "as the first tape line" inside `<ol>` without saying whether it is an `<li>`. Upstream says `TAPE stopped` is "a strip at the top of the tape" and the 401 strip is "in the rack position". Patch 3.
- The CI wiring inputs are missing. The draft names no source directory, no location for `eslint.config.js` (`npx --prefix e2e-web` runs from the repo root, so ESLint would not find a config under `e2e-web/` without `-c`), and no html-validate config path. The `elements` declaration does not say that `tbody` must permit the `display: contents` hosts. Upstream names crate `viola-ui` and the `crates/viola-e2e` layout. Patch 4.
- The coverage gate cannot be derived. The "Applies in v1" column mixes 7 status kinds, and nothing says which of them count. The Aggregation stage runs "after `gate`" with no `always()` condition, so it never runs on the failures it exists to explain. The adversarial question also holds: no artifact separates a scrub leak from a CSP error. Patch 5, which also labels `jq` as catalog-less transport (Dimension 3).

**Dimension 2: Scope faithfulness**
- The first tui boundary clause ("viola writes zero bytes to the terminal except the child's output") is only restated. The draft has assertions for keystrokes and for focus/mouse/resize, but none for the bytes. Patch 6.
- The P2 SC 3.2.4 addition is carried in the Section 3 map. The `[resolved: …]` pointers land on real subsections. The Section 7 v1.x brake text says "when built", and the Section 4 catalog row says "not asserted in v1". No patch.

**Dimension 3: Tool anchoring**
- The anchor is confirmed. Research does not back the claim that calling `options()` before `withTags()` keeps rules running. Research item (d) ends "Phase 3 decides", and the draft has no check that the rules actually ran. The same silent-skip risk applies to `target-size`, which runs only through the `wcag22aa` tag. Patch 2 adds a check on every `analyze()` that each rule id appears in `passes`, `violations`, `incomplete` or `inapplicable`, using a new `axe-rule-not-run` id (added to the closed list in Patch 1).
- Versions agree across Section 1, the body and the Decisions Log.
- Line 412 ("axe 4.1.2 rules") could be misread as an axe version. The same mix-up in the Section 3 map at line 538 is cosmetic. Deferred.
- SC 2.4.6 credit is also given to html-validate and aria snapshot. The claim that the best-practice rules close "1.3.1 / 2.4.6" is minor. Deferred.

**Dimension 4: Tier calibration**
- The anchor is confirmed. The SLO state list leaves out `stale` (P5), tape-line-expanded, after skip-link activation (Section 5), WHEEL `human` (P4), the scrolled-up state (the only state where the anchor exists) and the skip link while focused. This also closes the Dimension 6 gap: axe `color-contrast` never sees the focused skip link or the anchor. Patch 7.
- The "> 50 %" budget still has no named evaluating stage. Deferred because the patch budget ran out.

**Dimension 5: WCAG mapping**
- The anchor is confirmed. The SC 2.4.11 row names 1 header-growth trigger, while Section 5 names 3. Upstream line 211 matches Section 5 (`expired`, `TAPE stopped`, nonzero `skipped`); `budget-paused` is not a growth trigger. The row also leaves out combined growth and the zero-height `#tape-end` after skip-link activation. Patch 8.
- The A/AA level column is not patched; it is cosmetic.

**Dimension 6: Design tokens**
- No raw values were found. Viewport widths are credited to the tests. The skip-link and anchor contrast gap is handled at runtime by Patch 7. No token was invented.
- The `--strip-h` Typography/Target-size inconsistency and the way the test finds token-sized elements are deferred.

**Dimension 7: Anti-patterns**
- The anchor is confirmed: the § CI ban names pa11y and Lighthouse as acceptable runtime checks. This is a one-line fix but it can wait. Deferred to iteration 2, together with the § Universal "pa11y JSON / Lighthouse JSON" wording and the duplicate reduced-motion ban.

## Patches

### Patch 1: §3 Structured violation JSON schema — add `scrub-leak` / `axe-rule-not-run` to the closed list; scrub failure stops borrowing `csp-console` and `critical`
**Reason:** Dimension 1 (anchor). The scrub row broke the "non-axe → `serious`" rule and used `csp-console` for a second meaning, so the `jq` triage query could not tell a secret leak from a CSP error.
**Old:**
```
`state-word-missing`, `surface-absence`, `csp-console`.
  - `severity` uses the axe impact vocabulary (`minor|moderate|serious|critical`). Non-axe checks emit `serious`.
  - `selector` is the axe `target`, which 4.13.0 escapes for control characters, or a Playwright role locator description.
  - `remediation` is the axe `helpUrl`, or the Section-number anchor in this plan.
  - `ci_run_id` / `git_sha` are set from `GITHUB_RUN_ID` / `GITHUB_SHA` and absent on local runs.
- **Scrubbing (Error sanitization trigger):** the fixture scrubs results before any attach or write:
  - axe `url` is reduced to its path (`/`);
  - `nodes[].html` is dropped;
  - any string that matches the token, `Cookie`, `?t=`, an absolute path or `CLAUDE*` fails the test with `violation_type:"csp-console"`-class severity `critical`. The raw axe JSON attachment is the scrubbed object.
```
**New:**
```
`state-word-missing`, `surface-absence`, `csp-console`, `scrub-leak`, `axe-rule-not-run`. Each id has exactly one meaning: `csp-console` is only a `securitypolicyviolation` / Trusted Types console error; `scrub-leak` is only a scrubber match (below); `axe-rule-not-run` is only the rule-execution check (§ A11y testing tool pick → Configuration).
  - `severity` uses the axe impact vocabulary (`minor|moderate|serious|critical`). Non-axe checks, including `scrub-leak` and `axe-rule-not-run`, emit `serious`.
  - `selector` is the axe `target`, which 4.13.0 escapes for control characters, or a Playwright role locator description.
  - `remediation` is the axe `helpUrl`, or the Section-number anchor in this plan.
  - `ci_run_id` / `git_sha` are set from `GITHUB_RUN_ID` / `GITHUB_SHA` and absent on local runs.
- **Scrubbing (Error sanitization trigger):** the fixture scrubs results before any attach or write:
  - axe `url` is reduced to its path (`/`);
  - `nodes[].html` is dropped;
  - any string that matches the token, `Cookie`, `?t=`, an absolute path or `CLAUDE*` fails the test and writes one row with `violation_type:"scrub-leak"`, `severity:"serious"`, the `check_source` of the check whose output leaked, and a `selector` naming the attachment and JSON path of the match — never the matched value. The raw axe JSON attachment is the scrubbed object.
```

### Patch 2: §3 A11y testing tool pick → Configuration — replace the unanchored `options()`/`withTags()` ordering claim with a gating rule-execution check
**Reason:** Dimension 3 (anchor). Research does not state the ordering semantics, and nothing checks that the five best-practice rules or `target-size` actually ran. If they are skipped, the result is still `violations: []` and CI stays green.
**Old:**
```
  - **Illustrative anchor** (`options()` first, then `withTags()`, so the tag filter is not overwritten):
```
**New:**
```
  - **Rule-execution check (gating):** that `.options({rules})` before `.withTags()` keeps the five best-practice rules running under a tag-type `runOnly` is an assumption (a11y-research does not state it), and `target-size` runs only through the `wcag22aa` tag. After every `analyze()`, the fixture asserts that each of `region`, `landmark-one-main`, `landmark-banner-is-top-level`, `page-has-heading-one`, `heading-order` and `target-size` appears in one of `passes[]` / `violations[]` / `incomplete[]` / `inapplicable[]`. A missing id fails the test (`violation_type:"axe-rule-not-run"`). This also catches a merge-to-replace change in `options()` under the unpinned axe-core `4.13.x` range.
  - **Illustrative anchor** (assumed ordering: `options()` first, then `withTags()`; the rule-execution check above is the proof):
```

### Patch 3: §4 Per-component pattern catalog → State strips row — make each strip position valid HTML that can be built as written
**Reason:** Dimension 1 (anchor). "After the heading position" puts a `<p>` inside `<table>`, which html-validate rejects. "First tape line" does not say whether an `<ol>` child is an `<li>`. Upstream places the 401 strip "in the rack position" and `TAPE stopped` as "a strip at the top of the tape".
**Old:**
```
native `<p>` directly after the rack `<caption>`-bearing table's heading position (rack errors / 401) or as the first tape line (`TAPE stopped`);
```
**New:**
```
rack errors (503/404/405): a native `<p>` that is the next sibling after the affected rack's `<table>` inside `<main>`; 401 access strip: a native `<p>` that is the next sibling after the WRAPPED rack's `<table>` (rack position). Neither is ever inside `<table>` / `<caption>` / `<tbody>`, so reading order is rack heading (caption) → column headers → strip. `TAPE stopped`: a native `<p>` that is the first child of the `role="log"` `<div>`, before the trim notice and the `<ol>` (never an `<ol>` child, so the list holds only `<li>` event lines). html-validate content-model rules on the rendered DOM and the `ariaSnapshotJSON` region-order comparison (SC 1.3.2, 3.2.3) pin these positions;
```

### Patch 4: §3 Bootstrap phases → a11y-ci-gate-wire — name the lint source crate, the config locations and the html-validate `elements` content model
**Reason:** Dimension 1. `setup-project` cannot write the lint step without guessing: no source directory, no location for `eslint.config.js` (`npx --prefix` does not change the working directory), and no html-validate config. Without a `tbody` extension, the `display: contents` `viola-*` hosts inside `<tbody>` fail the content-model rules.
**Old:**
```
  - `ci.yml` ubuntu leg gets a lint step before `run --browser`: `npx --prefix e2e-web eslint -f json` over the Lit sources, plus `html-validate --formatter json` on the embedded `assets/index.html`.
```
**New:**
```
  - `ci.yml` ubuntu leg gets a lint step before `run --browser`: `npx --prefix e2e-web eslint -c e2e-web/eslint.config.js -f json` over the Lit sources of the `viola-ui` crate (`crates/viola-ui/`, following the workspace `crates/<name>` layout; the exact source glob is the config's `files` entry). `eslint.config.js` lives in `e2e-web/` so `eslint-plugin-lit-a11y` resolves from `e2e-web/node_modules`. The step also runs `npx --prefix e2e-web html-validate --config e2e-web/.htmlvalidate.json --formatter json` on the `assets/index.html` that `viola-ui` embeds. Both write to `e2e-web/test-results/lint/`.
  - The html-validate config `e2e-web/.htmlvalidate.json` (created in a11y-tooling-install) declares every `viola-*` element in `elements`: `viola-session-row` and `viola-transfer` with permitted content `tr` only, with `tbody` extended to permit those two hosts (their `display: contents` host sits between `<tbody>` and `<tr>`); `viola-atis`, `viola-event-feed` and `viola-readback` as flow content. The same config serves the lint stage and the rendered-DOM check (SC 4.1.1 row), so `no-unknown-elements` and the content-model rules do not misfire.
```

### Patch 5: §9 Per-pipeline-stage table → Aggregation row — set the run condition, define which SCs are "applicable" and separate scrub leaks in the report
**Reason:** Dimension 1 (derivable coverage gate plus the adversarial question). Aggregation "after `gate`" never runs once the job has failed. The mixed "Applies in v1" values leave `sc-coverage.json` undefined. No machine-readable artifact separates `scrub-leak` from `csp-console`. `jq` was also not labelled as catalog-less (Dimension 3).
**Old:**
```
| Aggregation (ubuntu, after `gate`) | `jq` over the Playwright JSON report: per `@sc-*` tag pass/fail, compared with `e2e-web/a11y/sc-coverage.json` (the applicable-SC list from Section 3) | `e2e-web/test-results/a11y/sc-coverage-report.json` | fails CI if any applicable SC has zero passing tagged tests; uploaded artifact |
```
**New:**
```
| Aggregation (ubuntu, after `gate`, with `if: always()` so it runs even when `gate` already failed the job; a missing Playwright JSON report fails this step; the artifact upload step is also `if: always()`) | `jq` (catalog-less JSON transport, not an a11y tool) over the Playwright JSON report and `e2e-web/test-results/a11y/*.ndjson`: per `@sc-*` tag pass/fail compared with `e2e-web/a11y/sc-coverage.json`, plus a per-`violation_type` row count. `sc-coverage.json` lists every row of the Section 3 per-SC map (ranges expanded). Rows marked `yes…` need ≥ 1 passing test tagged with that SC (1.4.10 only through the ≥ 760 reflow tests). Absence rows (`no media`, `no audio`, `no images`, `no inputs`, `no submissions`, `no custom pointer/motion/drag`, `no surface`) are met by the `surface-absence` spec carrying one `@sc-<id>` per absent SC. `single-page exception` / `single page` rows are met by the route check and region-order test (`@sc-2.4.5`, `@sc-3.2.3`). `indeterminate-language content` (3.1.2) is met by the tape `lang` DOM assertion | `e2e-web/test-results/a11y/sc-coverage-report.json` (per SC: `{sc, passing_tests, status}`; plus `violation_type_counts`, so a `scrub-leak` shows up separately from `csp-console`) | fails CI if any listed SC has zero passing tagged tests; uploaded artifact |
```

### Patch 6: §3 Keyboard test harness → Tooling — add the missing tui "zero viola bytes" boundary assertion
**Reason:** Dimension 2 (anchor). The first of the 3 tui boundary clauses in Section 1 is only restated. No harness asserts it, so a stray viola line on the child's terminal would not be caught.
**Old:**
```
and asserts they are delivered unblocked, and that `\x1b[I`/`\x1b[O`, mouse and resize sequences do not move the wheel.
```
**New:**
```
and asserts they are delivered unblocked, and that `\x1b[I`/`\x1b[O`, mouse and resize sequences do not move the wheel. A third boundary assertion covers the zero-viola-bytes clause: with the tests' `viola-fake-agent` as the child, the outer-PTY byte stream across start, a driver `send`, a refused automation send (`human-typing`) and exit contains no viola-originated bytes. That means none of viola's own message literals (diagnostics, refusal text, `hint:`) and no SGR or cursor control the child did not emit. On the Linux and macOS legs, the stream is compared byte for byte with an unwrapped fake-agent run under the same outer PTY. On Windows, where ConPTY re-renders output, the check is that viola's own literals are absent. The check never inspects `claude` content (R7). Each of the three boundary clauses is its own nextest case on all three OS legs.
```

### Patch 7: §10 Always-required SLO invariant — add every state the path scenarios and layouts require to the axe state list
**Reason:** Dimension 4 (anchor). P5 requires an axe verdict for `stale`. Section 5 defines tape-line-expanded and after-skip-link-activation. P4 has WHEEL `human`. The `N new lines below` anchor exists only while scrolled up, and the skip link is visible only when focused. All of these were outside the gating list, so SC 2.4.4 / 2.5.8 / 1.4.3 on the two focusable elements never reached axe.
**Old:**
```
  - axe `violations: []` after every state render (steady, bay-narrow, first-reading/empty, cocked, readback-refused, 503, `TAPE stopped`, 401 access strip);
```
**New:**
```
  - axe `violations: []` after every state render (steady, bay-narrow, first-reading/empty, tape-line-expanded, scrolled-up with the `N new lines below` anchor present, skip link focused, after skip-link activation, cocked, readback-refused, WHEEL `human`, stale, 503, `TAPE stopped`, 401 access strip). This list is the union of the per-state axe verdicts in Section 4 P1–P5 and the Section 5 layouts; a state added to either is added here;
```

### Patch 8: §3 WCAG criteria mapping → SC 2.4.11 row — match the Section 5 triggers, including combined growth and `#tape-end`
**Reason:** Dimension 5 (anchor). The map, which drives `@sc-2.4.11` coverage, names only the `TAPE stopped` growth. Section 5 and upstream layout name `expired`, `TAPE stopped` and nonzero `skipped`. The row also does not define the check for the zero-height `#tape-end` focus target.
**Old:**
```
after each Tab, and after the header grows (`TAPE stopped` box), the focused element's rect is not fully covered by the `<viola-atis>` rect
```
**New:**
```
after each Tab, after each header-growth trigger (`expired`, `TAPE stopped`, nonzero `skipped`), singly and combined (Section 5 Sticky header), and after skip-link activation, the focused element's rect is not fully covered by the `<viola-atis>` rect. The zero-height `#tape-end` counts as obscured if its rect lies within the header rect
```

## Changelog
[Iteration 1] [substantive] Added closed-list ids `scrub-leak` and `axe-rule-not-run` (serious; scrub rows no longer borrow `csp-console`/`critical`); made the axe best-practice and `target-size` rules prove they ran on every `analyze()`; gave state strips valid HTML positions; named the `viola-ui` lint sources, the ESLint/html-validate config paths and the `tbody` content model; made Aggregation run `always()` with a defined applicable-SC rule and per-`violation_type` counts; added the tui zero-viola-bytes boundary assertion; added the missing path/layout states to the SLO axe list; aligned the SC 2.4.11 map row with the Section 5 header-growth triggers and `#tape-end`.

Files reviewed: `D:/dev/projects/viola/.andromeda/runs/2026-09-24T05-00-37-a11y/meta-prompt.md`, `D:/dev/projects/viola/.andromeda/runs/2026-09-24T05-00-37-a11y/a11y-plan-draft.md`, `D:/dev/projects/viola/.andromeda/runs/2026-09-24T05-00-37-a11y/a11y-research.md`, `D:/dev/projects/viola/.andromeda/runs/2026-09-24T05-00-37-a11y/upstream-context.md`, `D:/dev/projects/viola/.andromeda/runs/2026-09-24T05-00-37-a11y/review-feedback-1.md`.

Deferred to iteration 2: the § CI and § Universal bans that still name pa11y/Lighthouse; the duplicate reduced-motion ban in § Visual and § Motion; the "axe 4.1.2 rules" wording; the stage that evaluates the "> 50 %" budget; the `--strip-h` Typography vs Target-size conflict; the A/AA level column in the SC map.
