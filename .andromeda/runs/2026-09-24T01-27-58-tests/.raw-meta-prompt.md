## Output Protocol

You are an iteration agent improving `D:/dev/projects/viola/.andromeda/runs/2026-09-24T01-27-58-tests/test-plan-draft.md`, the Comprehensive (2) test plan for **viola**. viola is a Rust 1.95 / edition 2024 workspace: the root `viola` bin plus `viola-core`, `viola-pty`, `viola-channel`, `viola-state`, `viola-agent-claude`, `viola-mcp` and `viola-ui`. It runs on std threads, with Tokio 1.53.1 only in `mcp`/`ui`. Its stack includes portable-pty `=0.8.1`, interprocess 2.4.4, axum 0.8.9 + SSE, rmcp 3.4.1 and a Lit 3.3.3 web page. Check your findings against:
- `upstream-context.md`, Architecture Excerpt / Stack
- `test-scope.md`, Sec 1–6
- `test-research.md`, the tool catalog

All three are in the same run dir.

Rules:

1. **Output:** patches (old → new) plus a changelog. Do NOT reproduce the full document.
2. **Patch format:**
   ```
   ### Patch N: <one-line description>
   **Old:**
   <exact text copied verbatim from the current draft, unique enough to locate>
   **New:**
   <replacement text>
   ```
   At most 8 patches per iteration. Spend them in this priority order:
   1. Test Scope Faithfulness
   2. Downstream Readiness
   3. Tool Anchoring
   4. Tier Calibration
   5. Performance Budget Coverage
   6. Cross-Surface Coordination Coverage
   7. E2E Selector Strategy Robustness
   8. Anti-Pattern Relevance

   Within one dimension, fix contradictions and false claims before gaps, and gaps before wording.
3. **Changelog:** exactly ONE line per iteration that classifies the iteration as a whole:
   `[Iteration N] [substantive|cosmetic] description`
   Never write one line per patch.
4. **PROHIBITED:**
   - reproducing the full document
   - restructuring sections without a concrete defect as the reason
   - labelling cosmetic changes as substantive
   - adding test code blocks longer than 5 lines, or any test-function bodies in any language (that is `/andromeda-implement` work)
   - adding security-plan content (threats, auth flows, crypto choices)
   - adding design tokens or component patterns (design plan)
   - adding obs-plan instrumentation schemas or observability / error-reporting platform picks (cross-references OK)
   - adding ARIA rules or a11y conformance criteria (a11y plan)
   - adding any human-in-the-loop tool or step (visual-review services, manual exploratory testing, recorder-authored tests, GUI-only result viewers)
   - editing historical entries in Section 12 Decisions Log (append only)
5. **If there are no issues:** output "No patches" and a single `[Iteration N] [cosmetic] no issues found` changelog line.

## Analysis Dimensions

### 1. Test Scope Faithfulness [priority: high]

- Section 1's security-vector triggers list three items that no later section plans a test for:
  - Vector 6: "a leading-slash argument is never taken as a prompt (§6 rewritten-path warning)"
  - Vector 6: "no env / flag / config disables a control"
  - multi-version-compat: "every ledger row has a `viola verify` probe with a post-condition"

  Check Section 4 (root bin / viola-agent-claude), Section 5 (CLI) and Section 6 (Paths 1–7, E1–E5, non-path suites). Is each one assigned to a concrete layer, suite file prefix (`cli_`, `hook_`, `contract_`, …) and verification signal? Or does it exist only in the §1 trigger list and the §2 trigger → layer map? Also check the GUI per-launch token entity: "`ui/<port>.url` (0600, removed on graceful shutdown)". Is the 0600 mode ever asserted? `cleanup` step 5 checks only that the file is absent.
- Compare the Section 1 Test Scope Summary field by field with `test-scope.md` (tier label, 18 entities, 8 surfaces, 5-command harness requirements, 7 critical paths, coverage triggers). Has anything been paraphrased, merged or dropped? For example, the tier justification says "Four drivers beyond the Standard baseline" but then lists five bullets (multi-platform CI, chaos, performance budgets, multi-version compat, mutation). Which count does test-scope.md Sec 6 actually give?
- Do the partially-testable entities follow the boundary/stub rule instead of claiming coverage? The entities are:
  - `viola-channel` cross-user euid / Windows pipe DACL
  - `viola-state` Windows foreign-owner SID / FAT volumes
  - the `CLAUDE*` strip vs real-session registration
  - `claude agents --json` live shape
  - real `rate_limits` values

  For each, does Sections 4–6 name the exact boundary function or stub and the local-only `agent-run run --local-live` path? Also check Windows channel-handle non-inheritance, which the E2 scenario and the §12 open questions say is asserted "only at the product unit boundary". Does §4 viola-channel actually list that unit case, or is it an orphan claim?

### 2. Tier Calibration [priority: high]

- Section 3 `run` step 3 says the Playwright suite is "skipped (with the skip recorded as a suite in the summary) where Chromium is not installed". Section 9 also runs `--browser` only on ubuntu. For a Comprehensive tier where web-spa carries critical paths 2–6 and E1/E3, can harness `run --all` or `viola-harness gate` exit 0 on ubuntu with Playwright skipped? If so, that is a silent coverage hole. Should a skip on ubuntu be a hard failure?
- Section 10 sets lines 85 / functions 95 / regions 80, enforced per OS, with "branch" mapped to `--fail-under-regions 80`. Two things need checking:
  - Given `#[cfg(windows)]` / `#[cfg(unix)]` code and the exclusions (`viola-fake-agent`, `crates/viola-e2e`, `tests/support`, `fuzz/`), is a 95 % function floor per OS realistic, or will it push agents to add exclusions?
  - Does the plan forbid widening `--ignore-filename-regex` as a back door to lowering thresholds? §11 Quality bans only "lower coverage threshold".
- Are the Comprehensive rows in the §2 pyramid backed by executable gates, or only described? The rows are Property (proptest 1.11.0 `cases: 512`, cargo-fuzz 0.13.2), Chaos (sysinfo 0.39.6 kill / `File::set_len` / SIGSTOP), Mutation (cargo-mutants 27.1.0) and Contract (insta 1.48.0 + jsonschema 0.57.0). For example:
  - is `unviable` mutants being exempt, plus "tested:0" for test-only diffs, stated as a gate rule in §10?
  - the SIGSTOP chaos case is Unix-only. What covers the stale-heartbeat / live-pid distinction on `windows-2025`?

### 3. Tool Anchoring (Catalog ↔ Plan) [priority: high]

- Cross-check every tool and version in Sections 3, 9 and 12 against `test-research.md`:
  - taiki-e/install-action v2.87.19 installs only `cargo-nextest,cargo-llvm-cov,cargo-mutants,cargo-deny`. How are these installed in CI, and at which catalog versions? Section 9 does not version them.
    - hyperfine 1.20.0
    - zizmor 1.30.1
    - cargo-modules 0.27.0
    - cargo-fuzz 0.13.2
    - jq 1.8.2 / jaq 3.1.1
  - Are `actions/upload-artifact v7.0.1` and the dtolnay/rust-toolchain version present in the catalog? If not, is there a Decisions Log entry?
  - Is cargo-deny 0.20.2 pinned anywhere?
- The time-mocking tools are mock_instant 0.6.1 (in-process `MockClock`) and tokio `test-util` (paused clock). Path 6 step 7 says to "Advance the injected clock (mock_instant) past `budget_override_until` and `send` to `builder` again", but `builder` is a separately spawned `viola run` process that the test process's `MockClock` cannot reach. The same applies to the §12 open question about driving the send-confirmation window "through the injected clock" in E2E. Does the plan need a documented cross-process time seam (for example a test-only env/flag on the fake-agent build, or pushing these assertions down to the §4 root-bin unit level)? Or is the scenario currently not agent-runnable as written?
- Are the picks in multi-candidate categories justified in §12?
  - portable-pty `=0.8.1` as the outer-PTY driver, over expectrl / rexpect (upstream 0.9.0 exists)
  - eventsource-client 0.18.0 vs the raw reqwest parser
  - nextest test groups vs the catalog's serial_test 4.0.1 fallback
  - criterion 0.8.2 rejected in favour of hyperfine

  Does each pick keep the catalog's `Agent-runnable` mechanism? Path 6 step 2 uses "proptest-generated stdin" inside an E2E scenario that then asserts fixed values (91, 89/86, exactly 90). Is proptest the right tool there, or should it be fixed synthetic stdin with proptest kept in the §6 Property suite?

### 4. Anti-Pattern Relevance [priority: medium]

- Section 11 still carries generic template bans that conflict with this plan:
  - §11 CI "NEVER skip caching of dependencies" contradicts §9 / §12 "no npm or Playwright browser cache".
  - §11 CI "NEVER run sequentially when you can parallelize" sits beside the deliberate `fixed-port` group (`max-threads = 1`) and the single-job mutation / fuzz stages.
  - §11 Test Strategy "NEVER violate test pyramid" is not tied to viola's layers.

  Should each be rewritten into a viola-specific ban (for example, a cache ban scoped to rust-cache in release workflows) or reconciled with the Decisions Log?
- Are bans duplicated inline in section bodies instead of cross-referenced to §11?
  - "no `bypassCSP`, no `toHaveScreenshot`" appears in the §6 drivers table and in §11 E2E.
  - `.env_clear()` must re-add `LLVM_PROFILE_FILE` appears in §6, §10 and §11 Integration.
  - `retries = 0` appears in §2, §10 and §11 CI/Quality.
  - `std::env::set_var` appears in §7 and §11.

  Keep the canonical ban in §11 and replace the duplicates with a pointer.
- Does every §11 domain have at least one ban grounded in a real viola pitfall? Candidates the plan's own findings support but §11 lacks:
  - parsing `claude agents --json` row names as targets
  - asserting on `-32603` messages other than `"internal error"`
  - Playwright tests passing when the browser suite was skipped
  - widening coverage exclusion regexes
  - using `mock_instant` to control time in another process

  Are the Universal bans (Percy/Chromatic/Applitools, manual smoke, codegen recorder, screenshot review, real network except 127.0.0.1 `viola ui`, real time) genuinely cross-stack, with viola-specific items (blanket approval, `cargo insta review`) placed in the right domain?

### 5. Downstream Readiness [priority: high]

- **setup-project** must build `scripts/agent-run.{sh,ps1}` and `viola-harness` from §3 alone:
  - §3 specifies boot / run / status / cleanup / logs. Section 9 also calls `viola-harness gate`, and §3 boot step 5 calls `viola-harness supervise --session <id>`. Neither has a command body, flags, input artifacts, JSON output shape or exit codes in §3.
  - The `status` document's `last_error` values are given only as "e.g. ready-503, list-exit-21, sessions-mismatch", and `boot`'s `reason` list and `run`'s `suites[].suite` enum have no closed set for `gate`.

  Are these enumerations closed and copy-pasteable?
- **obs** ownership is circular:
  - §3 Log format defines required process-log fields (`timestamp`, `level`, `target`, `message`, `event`, `process`, `instance`, `corr`) and says "obs-plan may add fields but must not rename or remove these".
  - The Bootstrap phases item `log-format-bind-with-obs` says to "consume the obs-plan §3 log format JSON schema verbatim".

  Which plan is the source of truth? Is `corr` defined precisely enough (channel request `id` vs `dialog_id` vs send `cursor`: which one for which `event`) for obs to derive an instrumentation contract without guessing? Is the kebab-case `event` value list closed or open?
- **route** derives phase order from the §3 Bootstrap phases list. Do these tools, which §9 requires, have a bootstrap phase or a named slot in `test-runner-install` / `coverage-tooling-install`?
  - cargo-mutants 27.1.0
  - cargo-fuzz 0.13.2 (nightly toolchain)
  - hyperfine 1.20.0
  - Playwright `install --with-deps chromium`
  - zizmor
  - cargo-modules
  - the `viola-fake-agent` feature build

  **a11y** needs the role-vs-attribute split from the §6 Selector strategy. Is that split stated there explicitly, or only implied through examples?

### 6. Performance Budget Coverage [priority: medium] [trigger: perf-budget trigger in test-scope Sec 5 OR test_tier=Comprehensive]

- The §10 Performance Budgets table fills p50 / p95 with "reported" and relabels p99 as "`max` over the listed runs". Is that acceptable for Comprehensive depth? Or should the table state explicit p50 / p95 trend thresholds, or explicitly rename the columns (for example Gate = `max`, with trend fields stored in the hyperfine `--export-json`) so downstream agents do not read "p99" literally? Also, with `--runs 30`, is `max` on shared GitHub runners (especially `windows-2025` and `macos-latest` arm64) a deterministic gate under the zero-flakiness rule? Or does the plan need a documented warm-up (`--warmup`) and a stated rule for handling runner noise that is not a retry?
- The spine-hook gate uses a provisional `max < 1.0 s` "until arch names the constant", which is the same value as SessionEnd's ~1 s. Is there a traceable §12 arch request that blocks chunk completion? Is there a rule that the gate value is read from the product constant, not a literal, once it exists? Are other perf-critical paths from test-scope Sec 4/5 missing from the table? For example:
  - `send` → `prompt-submitted` confirmation latency (bounded by the confirmation window)
  - `wait` wake latency after `turn-ended`
  - `boot`'s 20 s per-instance / 10 s UI readiness deadlines
- Every budget must be an exit-code gate. Is `jq -e '.results[0].max < 1.0'` shown for each hyperfine row, including `pre-tool-use` on an unverified CLI? Is the SSE row's "real-time smoke sees one within 16 s per OS" tied to a specific suite and exit code? And given §9 runs coverage instrumentation in the same matrix, does the plan state that the hyperfine gates run against a non-instrumented build?

### 7. Cross-Surface Coordination Coverage [priority: medium] [trigger: multiple surfaces in test-scope Sec 2 OR cross-surface coordination trigger in Sec 5]

- The cross-surface-coordination trigger (design Layout Categories, layout Signature Placements "flips same frame", Founder Direction 5 CL-1) is mapped in §2 only to "E2E". §5 Integration Test Strategy has no cross-surface coordination subsection. Should parity checks run at integration speed so failures localise before E2E? Candidates:
  - `list --json.items` == `/api/sessions.items` over a shared tempfile home via axum-test 21.1.0 + assert_cmd
  - CLI refusal JSON vs MCP `structured_content` from the same channel result
- For each multi-surface scenario (Paths 2–6, E3), does every listed surface have both a driver and a transition signal that proves the state crossed that boundary? Examples to check:
  - Path 3 lists "ipc-internal (channel + MCP `wait`/`last`)", but MCP `wait` appears only in step 7 on a dead instance. Is a live MCP `wait` wake asserted?
  - Path 7 lists api-service but not web-spa, even though `cli_verified:false` is rendered in the CLI column. Should the Playwright CLI cell be asserted?
- §11 has no cross-surface ban. Should it add a viola-specific rule, for example "NEVER assert a critical path on one surface only when test-scope Sec 4 lists several" and "NEVER compare CLI and web captions / payloads through separate fixtures instead of one shared home"? Also, `status`'s `api_sessions_equal_list` is computed by the harness. Is it asserted at the end of every multi-surface scenario, or only in `cross_list_parity`?

### 8. E2E Selector Strategy Robustness [priority: medium] [trigger: E2E Section 6 present AND UI surfaces (web-spa) in test-scope Sec 2]

- The §6 Selector strategy mixes three kinds of selector:
  - role queries (`table`, `row`, `columnheader`, `log`, `status`)
  - `data-*` state attributes (`data-dialog`, `data-liveness`, `data-wrapped`, `data-rb`)
  - custom-element tag names (`viola-session-row`, `viola-atis`, `viola-transfer`, `viola-event-feed`, `viola-readback`)

  Are all of these tags and attributes anchored in the design excerpt Brand Identity Anchors / layout excerpt Signature Placements in `upstream-context.md`? Or were some introduced by the plan (for example `viola-session-row`) without an upstream anchor? Is there an explicit fallback convention (for example a `data-testid` rule) for elements with no role and no signature anchor?
- Several assertions match visible copy: `locator('viola-atis')` containing `budget-paused` / `gate open`, `unable · not-delivered · no-prompt-submitted`, `DIALOG question`, `toHaveTitle('DIALOG builder · viola')`. Is that copy fixed by the design / layout excerpt, and so a stable anchor? Or should these assertions key on `data-*` state with text as a secondary check? Does the plan say where the canonical strings live, so that a copy change fails exactly one place?
- Are the synchronisation mechanisms consistent with the §11 E2E ban on `sleep(N)`?
  - Playwright auto-waiting locators
  - the single `page.waitForFunction` evaluation for the E3 "flips same frame" tape + transfer-marker readback
  - `boot`'s 100 ms file-state polling

  Is the gating axe `violations == []` check (@axe-core/playwright 4.13.0, run after the empty / 503 / cocked strips) scoped only as a guard on the role-query contract, with the rule set deferred to the a11y plan? Or does it currently make the test plan own a11y conformance?

Read the document, analyze along all dimensions, output patches and changelog.
