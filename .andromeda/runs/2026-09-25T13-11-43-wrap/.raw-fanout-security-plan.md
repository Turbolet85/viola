proposals:
  - {detector: D-security-auth, severity: warning, section: Security Decisions Log, basis: security-plan.md:609, change: "Remove the SQOS spike open question; add a dated 2026-09-25 entry — spike passed on interprocess =2.4.4 + windows-sys =0.61.2; CreateFileW(SQOS|IDENTIFICATION|OVERLAPPED) then Stream::try_from → server Identification (1); flags-absent/default connect → Impersonation (2); no OVERLAPPED → hang; interprocess's ReOpenFile(…, FILE_FLAG_OVERLAPPED) keeps the connect-time level; recipe pinned by tests/channel_sqos_open.rs; no default-connect fallback stands."}
  - {detector: D-security-auth, dependent-of: D-security-auth, section: Authentication & Authorization, basis: security-plan.md:205, change: "IPC client-side server verification row: replace 'This adoption path is the open spike … replacement SQOS open is recorded' with: spike passed (Decisions Log 2026-09-25), Identification measured, recipe pinned by tests/channel_sqos_open.rs; keep the never-fall-back clause."}
  - {detector: D-security-auth, dependent-of: D-security-auth, section: Bootstrap phases, basis: security-plan.md:373, change: "viola-channel client bullet: 'The SQOS spike (Decisions Log open question) must pass, or its replacement be recorded, before this chunk.' → 'The SQOS spike passed (Decisions Log 2026-09-25); the client uses the recorded recipe.'"}
  - {detector: D-security-auth, section: Security Decisions Log, basis: security-plan.md:611, change: "Remove the SHA-256 crate open question; record in the 2026-09-25 entry: sha2 =0.11.0, default-features = false (MIT OR Apache-2.0, rust-version 1.85), pure Rust, no cc on the three [graph] triples, cargo deny bans pass; <hash> = first 8 bytes / 16 hex, pinned by tests/contract_content_hash.rs; FNV-1a / DefaultHasher ban stands."}
  - {detector: D-security-auth, dependent-of: D-security-auth, section: Data Protection, basis: security-plan.md:265, change: "Code-bearing artefacts: 'The SHA-256 implementation is an open question (Decisions Log). It must be pure Rust …' → 'The SHA-256 implementation is sha2 =0.11.0 (default-features = false, pure Rust, passes the C-build ban; Decisions Log 2026-09-25).'"}
  - {detector: D-security-auth, dependent-of: D-security-auth, section: Bootstrap phases, basis: security-plan.md:375, change: "viola-state re-hash bullet: 'The hash crate must first be picked and recorded under the Decisions Log open question.' → 'using sha2 =0.11.0 (Decisions Log 2026-09-25).'"}
  - {detector: D-security-deps, severity: escalate, section: Dependency Security, basis: security-plan.md:308, change: "deny.toml additions: add a [licenses] bullet — interprocess's 0BSD transitive crates doctest-file and recvmsg are admitted only by per-crate [[licenses.exceptions]] (allow = [\"0BSD\"], commented); 0BSD is never added to the global allow, which stays [MIT, Apache-2.0, Zlib, Unicode-3.0]; any new exception needs its own Decisions Log entry."}
notes: "D-security-input: no drift (no product external-input surface). D-security-auth library check: pins match §Authentication line 189; no windows-targets hit in security-plan."
