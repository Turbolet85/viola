proposals:
  - detector: D-tests-framework
    severity: warning
    section: "## 9. CI Integration → Pipeline structure (Lint row)"
    change: "Lint row: replace the hard-coded `cargo check -p viola-core -p viola-pty -p viola-state -p viola-channel -p viola-agent-claude` with `test -s scripts/sync-crates.txt` then `cargo check` with one `-p` per crate listed in `scripts/sync-crates.txt` (the single sync-crate list; an empty list fails; no `--features`; target set is the five sync crates, today only `viola-core`). Move the ubuntu-only deny/zizmor clause out to a new Supply-chain row (ubuntu `supply-chain` job, `contents: read`): `cargo deny --format json check` (deny.toml: advisories, licences, sources, bans), then the tokio ban run once per listed sync crate as the sole root `cargo deny --config deny-sync.toml --manifest-path crates/<crate>/Cargo.toml check bans`, then `bash scripts/deny-probes.sh` (13 permanent negative ban probes plus a clean control), then `zizmor --format=json .github/workflows/`. The JSON goes to `target/supply-chain/`, uploaded `if: always()` as artifact `supply-chain` for 7 days. Add the weekly `nightly.yml` job `advisories` (`cargo deny check advisories`, cron plus `workflow_dispatch`, no cache)."
    sidecar: "test-plan §9: the Lint row reads the sync crates from scripts/sync-crates.txt. deny/zizmor move to a separate Supply-chain row (sole-root deny-sync.toml tokio ban, deny-probes.sh, JSON artifact `supply-chain`), plus the weekly nightly.yml advisories job."
    rationale: "Report Changes → Harness / gate surface: `ci.yml` job `lint` runs `cargo check` with one `-p` per crate in `scripts/sync-crates.txt`. A separate ubuntu `supply-chain` job runs cargo deny, the sole-root tokio ban per listed crate, deny-probes.sh and zizmor, and uploads `target/supply-chain/`. `nightly.yml` job `advisories` is new. Schema/config lists deny-sync.toml and sync-crates.txt. Expected amendments names 'test-plan §9 Lint row' (test-plan.md:1399). Decisions: the ban probes are PERMANENT in CI."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1399"
  - detector: D-tests-framework
    severity: warning
    section: "## 2. Test Strategy → Trigger → layer map (Supply chain (V9) row)"
    change: "Supply chain (V9) row, Plan location: '§9 Lint stage' becomes '§9 Lint stage (sync-crate `cargo check`) + Supply-chain stage (cargo deny, sole-root tokio ban, ban probes, zizmor) + nightly.yml weekly advisories'."
    sidecar: "test-plan §2 trigger map: the V9 supply-chain gates are located in the §9 Lint + Supply-chain stages and nightly.yml, no longer the Lint stage alone."
    rationale: "Report Harness / gate surface: deny/zizmor/probes run in the new `supply-chain` job, not in `lint`. The §2 map still places all of V9 in the §9 Lint stage."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:486"
    dependent-of: D-tests-framework
  - detector: D-tests-framework
    severity: warning
    section: "## 9. CI Integration → Build failure conditions"
    change: "Split the 'Lint errors' bullet. Lint keeps fmt, clippy, the sync crates in scripts/sync-crates.txt failing to build without tokio (or an empty list), cargo-modules, rmcp `client` and the a11y lint. A new 'Supply-chain errors' bullet covers a cargo deny ban/advisory/licence/source finding, a listed sync crate failing the sole-root deny-sync.toml tokio ban, deny-probes.sh not ending `13/13 banned, control clean`, and a zizmor finding. Also add: the nightly.yml weekly `cargo deny check advisories` run failing."
    sidecar: "test-plan §9 build failure conditions: deny/zizmor findings move under a Supply-chain bullet, which adds the sole-root tokio-ban and ban-probe failures, plus the nightly advisories run."
    rationale: "Report Harness / gate surface: the fail-closed deny, sole-root tokio ban, deny-probes.sh and zizmor steps live in the `supply-chain` job, and the probes are a new permanent gate (Decisions). The bullet still labels deny/zizmor as 'Lint errors' and has no probe or sole-root failure."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1434"
    dependent-of: D-tests-framework
  - detector: D-tests-framework
    severity: warning
    section: "## 9. CI Integration → Pipeline structure (paragraph after the table)"
    change: "'The workflow has `permissions: {}` at the top and `contents: read` per job' becomes 'Every workflow (`ci.yml`, `nightly.yml`) has `permissions: {}` at the top and `contents: read` per job'. In `nightly.yml`, cargo-deny 0.20.2 comes from the same taiki-e pin. Also record the cargo-deny 0.20 CLI form: `--config <path>` is global and goes before `check`, and `check -c` is rejected."
    sidecar: "test-plan §9: the least-privilege sentence now covers both workflows. It also records the cargo-deny 0.20 CLI form: global `--config` before `check`."
    rationale: "Report Counts moved: CI workflows went from 1 to 2 (ci.yml + nightly.yml). nightly.yml has `permissions: {}` and job `contents: read`. Dev-tool versions measured that cargo-deny 0.20.2 rejects `check -c`, and Reverted facts shows the first-written `-c` form was replaced. The paragraph speaks of a single workflow."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1411"
    dependent-of: D-tests-framework
  - detector: D-tests-framework
    severity: warning
    section: "## 12. Test Decisions Log → Test crate deviation"
    change: "Replace 'and is added to the cargo-deny tokio wrappers list' with: the tokio ban runs through `deny-sync.toml` once per crate in `scripts/sync-crates.txt`, each as the sole root (`cargo deny --config deny-sync.toml --manifest-path crates/<crate>/Cargo.toml check bans`). `viola-e2e` is never a root there, so its tokio dev-graph is outside the ban without any `wrappers` allowlist. Neither `wrappers` nor `--exclude` is used. `--exclude` false-fails on feature-unified optional tokio."
    sidecar: "test-plan §12: the claim that viola-e2e joins a cargo-deny tokio `wrappers` list is retired. The tokio ban is a sole-root deny-sync.toml check per listed sync crate."
    rationale: "Report 'Spec claims disproved by measurement' 2 names test-plan.md:1617: under sole roots viola-e2e is not a root, no `wrappers` list exists, and a wrappers allowlist is the direct-parent form arch rejects. deny.toml has 'No tokio entry, no `wrappers`'. Expected amendments lists 'L1617 wrappers sentence'. `grep wrappers` finds no other tokio-wrappers occurrence in test-plan; line 173 'wrappers' means `viola run` processes."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1617"
