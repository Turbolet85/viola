proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Repository
    change: Add the repo resources this chunk lands. `deny.toml` is the workspace cargo-deny policy. `deny-sync.toml` holds the tokio ban only; it is run once per sync crate as sole root. `scripts/sync-crates.txt` is the single sync-crate list, `viola-core` today; CI and local gates fail if it is empty. `scripts/deny-probes.sh` runs 13 negative ban probes plus a clean control. `.github/workflows/nightly.yml` is the weekly advisory run. Two gitignored output dirs are also new. `target/deny-probes/run-<utc>-<pid>/` holds throwaway probe projects. `target/supply-chain/` holds `deny.json` and `zizmor.json`, uploaded as CI artifact `supply-chain` for 7 days.
    sidecar: Occupied Resources/Repository — registered deny.toml, deny-sync.toml, scripts/sync-crates.txt, scripts/deny-probes.sh, nightly.yml, target/deny-probes/, target/supply-chain/ (supply-chain-and-workflow-gates)
    rationale: Report Changes → Files lists these as new files. Harness/gate surface lists the "New gitignored output dirs `target/deny-probes/`, `target/supply-chain/`" and artifact `supply-chain`. None of them appear in the §Occupied Resources filesystem/repository list, which today names only the target/agent-run, e2e-home and harness dirs.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:375-382
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → Project directory structure
    change: Add `deny-sync.toml` beside `deny.toml`, and add `scripts/sync-crates.txt` and `scripts/deny-probes.sh` beside `scripts/agent-run.{sh,ps1}`. Under `.github/workflows/`, list `nightly.yml` (weekly advisories plus workflow_dispatch) next to `ci.yml`.
    sidecar: Directory tree — added deny-sync.toml, scripts/sync-crates.txt, scripts/deny-probes.sh, workflows/nightly.yml
    rationale: This tree is a second listing of the repo layout. Report Changes → Files adds these four files, and the report's Expected amendments name the tree as a site.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:418,444,449-450
    dependent-of: D-arch-resources
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → Build system
    change: Rewrite the dependency-policy bullet. `cargo deny check` over `deny.toml` covers 4 families. (1) advisories: unmaintained/unsound "all", yanked deny, one reasoned ignore. (2) licences: MIT, Apache-2.0, Zlib, Unicode-3.0. (3) sources: unknown registry and unknown git denied. (4) bans: the C crates `cc`, `libsqlite3-sys`, `openssl-sys`; the telemetry crates `opentelemetry-otlp`, `opentelemetry-stdout`, `sentry`, `tracing-appender`; and feature bans on veil, rmcp, axum and tracing-subscriber. The policy is evaluated for the three target triples. The tokio ban is no longer in `deny.toml`. It lives in `deny-sync.toml` (`exclude-dev = true`, same triples) and runs once per crate listed in `scripts/sync-crates.txt` as sole root: `cargo deny --config deny-sync.toml --manifest-path crates/<crate>/Cargo.toml check bans`. It is never checked with `--exclude`, because `--exclude` false-fails under workspace feature unification. It is also never checked with a `wrappers` allowlist. Limit: `viola-channel` false-fails as its own root once `viola-mcp` enables its `tokio` feature, so it is covered through the sync roots that depend on it.
    sidecar: Build system — tokio ban mechanism changed from graph-with-exclusions to sole-root per sync crate via deny-sync.toml; deny.toml families now advisories/licences/sources/bans (C, telemetry, feature)
    rationale: Report "Spec claims disproved by measurement" #1 measured the documented mechanism ("checked over the graph with `viola`, `viola-mcp` and `viola-ui` excluded") as false. The chunk lands the sole-root form instead (Changes → Schema/config for `deny-sync.toml` and `deny.toml`). The code contradicts arch's stated pattern.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:392
  - detector: D-arch-decisions
    severity: warning
    section: Established Decisions → [Concurrency / Backend Framework]
    change: Replace "CI enforces this with the `cargo deny` `tokio` ban, evaluated for the Windows, macOS and Linux target triples" with this. CI enforces it with a sole-root `cargo deny` tokio ban: `deny-sync.toml`, run once per crate in `scripts/sync-crates.txt`, evaluated for the Windows, macOS and Linux target triples. CI job 3 adds a `cargo check` of the listed sync crates without the `tokio` feature on all three OSes.
    sidecar: [Concurrency] decision — enforcement text now names the sole-root deny-sync.toml ban and the sync-crates.txt list
    rationale: The decision restates the tokio-ban mechanism that report "Spec claims disproved" #1 retires. Changes → Harness surface shows job `lint` running `cargo check` with one `-p` per listed crate, and the sole-root ban running in `supply-chain`.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:44
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → Project directory structure
    change: Change the `deny.toml` tree comment from "cargo-deny: licences, C-crate bans, tokio bans" to "cargo-deny: advisories, licences, sources, bans (C, telemetry, features)". Give `deny-sync.toml` the comment "sole-root tokio ban, per scripts/sync-crates.txt crate".
    sidecar: Directory tree — deny.toml comment now lists 4 families; tokio ban attributed to deny-sync.toml
    rationale: Report Counts/qualifiers moved says tree comment :418 is now wrong. The policy carries 4 families, and the tokio ban lives in `deny-sync.toml` (deny.toml has "No tokio entry").
    basis: D:/dev/projects/viola/.andromeda/architecture.md:418
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies → Code quality row
    change: 'Change the Code quality row to "rustfmt, clippy (`-D warnings`), `cargo check`; cargo-deny 0.20.2 (advisories, licences, sources, bans: C, telemetry, feature; tokio ban via `deny-sync.toml` per sync crate); zizmor 1.30.1 (GitHub workflow linter, default persona, no config); cargo-modules 0.27.0 (module graph review)". Add "workflow lint" to the role column.'
    sidecar: Stack/Code quality — added zizmor 1.30.1 (workflow linter); cargo-deny families corrected to 4 families + sole-root tokio ban
    rationale: Report Dev-tool versions introduces zizmor 1.30.1, which has 0 hits in architecture.md. Zizmor is a new tool that §Stack does not list. Counts/qualifiers moved flags the :36 row "licences, C-dependency bans" as retired by the 4-family policy.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:36
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: Replace "one workflow `ci.yml`" with two workflows. `ci.yml` is triggered on push and pull request. `nightly.yml` runs on `schedule` (cron `17 4 * * 1`) plus `workflow_dispatch`, with `permissions: {}` and one job, `advisories` (ubuntu, `cargo deny check advisories`, no cache). Schedule and dispatch run only from the default branch, `build/viola-0.1.0`.
    sidecar: CI/CD — workflows 1 → 2 (ci.yml + nightly.yml weekly advisory run)
    rationale: Report Counts/qualifiers moved says "CI workflows 1 → 2 (`ci.yml` + `nightly.yml`)". Harness surface describes nightly.yml. P4 operator decision placed the weekly advisory run in `nightly.yml` rather than as a `schedule:` trigger on ci.yml. That contradicts the "one workflow" pattern.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:456
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: 'Change "Jobs wired today" to name all four `ci.yml` jobs: `test`, `mutants`, `lint` and `supply-chain`. `lint` is a 3-OS matrix: `test -s scripts/sync-crates.txt`, then `cargo check` with one `-p` per listed crate and no `--features`; this is target job 3. `supply-chain` runs on ubuntu. It installs cargo-deny 0.20.2 via taiki-e and zizmor 1.30.1 via `cargo install --locked`. Its fail-closed steps are `cargo deny --format json check`, the sole-root tokio ban per listed crate, `scripts/deny-probes.sh` (13/13 banned + control) and `zizmor .github/workflows/`. It uploads `target/supply-chain/` as artifact `supply-chain` with `if: always()`; this is target job 4. Target job 3 reads its crate list from `scripts/sync-crates.txt`. The note "the ban itself is enforced by job 4" now points at the sole-root `deny-sync.toml` step.'
    sidecar: CI/CD — ci.yml jobs 2 → 4 (lint, supply-chain wired); target jobs 3/4 now read scripts/sync-crates.txt and run deny + probes + zizmor
    rationale: Report Counts/qualifiers moved says "`ci.yml` jobs 2 → 4". Harness/gate surface gives the `lint` and `supply-chain` job contents. The arch text lists only `test` and `mutants` as wired, hardcodes the five-crate `-p` list and says job 4 is a bare `cargo deny check`.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:459-464
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: Setup-steps bullet — taiki-e/install-action installs the version-pinned cargo tools, including cargo-deny 0.20.2. The one exception is zizmor 1.30.1, installed by `cargo install --locked zizmor@1.30.1` in `supply-chain`.
    sidecar: CI/CD setup — zizmor install path recorded (cargo install --locked, not taiki-e)
    rationale: Report Dev-tool versions says the CI image gets "zizmor 1.30.1 via `cargo install --locked` (in `ci.yml`)". The setup-steps bullet claims taiki-e installs the version-pinned cargo tools, and zizmor contradicts that.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:458
    dependent-of: D-arch-decisions
