# layouts extract
## No domain coverage
Out of scope. This chunk changes only CI (ci.yml), the viola-e2e harness (mutants base, the union gate verdict, secret-scan scope) and test-plan, and its Boundaries rule out any change to product-crate behaviour. It does not create or change any layout-templates.md surface. The plan has two surfaces, web-spa (the Bay and its wireframes) and cli (the `viola` verbs), and neither is touched. The amendments file D:/dev/projects/viola/.andromeda/layout-templates-amendments.md does not exist, so the amendment history is empty.
