# design extract

## No domain coverage
The chunk touches only CI (ci.yml), the `viola-e2e` harness (chunk-base resolution, the per-leg mutation union, secret-scan scope) and test-plan, with no product-crate behaviour change. The design plan (D:/dev/projects/viola/.andromeda/design-system.md) covers only the product surfaces: §Surface: web-spa and §Surface: cli, where the cli surface is the `viola` binary's stdout/stderr. No token, typography, motion, iconography or component-pattern rule applies, and the amendment sidecar (design-system-amendments.md) does not exist, so the history is empty.
