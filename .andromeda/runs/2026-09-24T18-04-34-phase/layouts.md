# layouts extract

## No domain coverage
This chunk makes no product behaviour change. It pins MAX_FRAME in a test, splits harness/run.rs, decomposes functions in viola-harness and the fake agent, and removes duplicated test code, so it creates or changes no region, component, focus stop, breakpoint or output line on either surface in layout-templates.md (web-spa bay, `viola` cli verbs). The amendment history (layout-templates-amendments.md) does not exist, which is normal on a fresh project and means it is empty.
