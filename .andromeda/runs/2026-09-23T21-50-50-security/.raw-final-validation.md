## Issues Found

**Pass 1 summary (checks that passed):**
- **Attack vectors:** all nine Phase 1 vectors (threat-assessment.md §2) have a mitigation or an accepted risk. The vectors are IPC, PTY paste, loopback HTTP, hook stdin, MCP, CLI arguments, `~/.viola/`, child spawn/PATH, and supply chain.
- **Data classes:** all five Phase 1 data classes are protected (file modes, cookie gate, the consolidated NEVER-log floor, strict-modes).
- **Compliance:** the triggers are "None", so a missing Compliance Controls section is correct.
- **Tools and versions:** every named tool and version traces to security-research.md. The SHA-256 crate is correctly flagged as an open question. The gitleaks/govulncheck blockquote is template boilerplate, which meta-prompt.md line 88 says to leave alone.
- **Anti-Patterns:** all 8 subsections are there, and they are specific to this stack.
- **Tier markers:** they are honoured. Logging & Monitoring and Supply chain integrity are correctly absent at Minimal. Key management and Data lifecycle are omitted with an explicit note.

- **[STALE_ARTIFACT]** The "Needs without a researched tool (flagged, not invented)" and "v1.x release prerequisites" blocks belong to the initial `2026-09-23` entry. In `.raw-security-plan.md` and `iteration-1.md` they sit in that initial entry, after Open questions (in `iteration-1.md`, after Known gaps). When the Phase 3.5 entry was inserted, they ended up after `- **By:** /andromeda-security Phase 3.5, user review`. They are now credited to the user review, which did not decide them (compare review-feedback-1.md). Several body sections point to "see the Decisions Log" for these items: Rate limiting, Update policy, Secret scanning, the Markdown sanitizer, and Supply chain integrity (line 344 and `supply-chain-signing-init`). Those pointers land in the wrong entry.
  Section: Security Decisions Log (initial entry vs Phase 3.5 entry); cross-refs from API Security, Dependency Security, Secret Management, Bootstrap phases
  Severity: medium

- **[STALE_ARTIFACT]** Key-decision "Unix:" bullet in the Decisions Log contains a Windows-only sentence left over from the iteration 10 companion patch: "On Windows the check also covers the version folders in use and the same trusted files, because a file can carry an explicit, non-inherited ACE…". The "Windows:" bullet itself never says that the Windows check covers the version folders and trusted files. Auth & Authz does say this, on line 207.
  Section: Security Decisions Log, initial entry, Key decision (Windows / Unix bullets)
  Severity: low

- **[CONTRADICTION]** Data Protection § Logs says "`events.ndjson` and `instances/<name>/diagnostics/` are 0600 inside 0700 directories". Error Handling says "`instances/<name>/diagnostics/` (0600; …)". Both give the `diagnostics/` directory mode 0600. That conflicts with the Auth & Authz rule that directories are 0700, and a 0600 directory could not be written to. The logging-redaction-wire floor states it correctly: "`diagnostics/` files are 0600".
  Section: Data Protection (At rest, Logs); Error Handling (Internal logging)
  Severity: low

- **[CONTRADICTION]** The API Security intro scopes the section to "the loopback GUI (`viola-ui`…)". Yet the table ends with an "MCP transport" row (rmcp features, `deny.toml` bans) that has nothing to do with the GUI.
  Section: API Security
  Severity: low

## Patches

### Patch 1: Remove the misattributed initial-entry blocks from the Phase 3.5 entry
**Reason:** STALE_ARTIFACT. The "Needs without a researched tool" and "v1.x release prerequisites" blocks were displaced into the Phase 3.5 user-review entry.
**Old:**
```
- **By:** `/andromeda-security` Phase 3.5, user review
- **Needs without a researched tool (flagged, not invented):**
  - GUI/IPC rate limiting: not needed at this tier.
  - Secret scanning in CI (no scanner researched): revisit with the v1.x signing credentials.
  - An automated dependency-update bot (none researched): manual review, driven by the weekly `cargo deny check advisories`.
  - An HTML sanitizer for assistant Markdown (none researched): the frontend must render text only.
  - Encryption at rest (none researched, and plaintext is an arch decision).
- **v1.x release prerequisites** (Supply chain integrity, deferred with release):
  - dist `>=0.33.0` with GitHub Artifact Attestations and Azure Artifact Signing (x86_64).
  - cargo-auditable `>=0.7.6` + `cargo audit bin` (cargo-audit `>=0.22.2`) on release binaries.
  - self_update 1.3.0 built with `signatures` (zipsign).
  - zizmor cache-poisoning findings addressed.
  - No `rust-cache` in the release workflow.
[Iteration 1]
```
**New:**
```
- **By:** `/andromeda-security` Phase 3.5, user review
[Iteration 1]
```

### Patch 2: Put the two blocks back in the initial entry
**Reason:** STALE_ARTIFACT, the companion to Patch 1. This puts the blocks back where the original plan had them, so the body's "see the Decisions Log" pointers land in the initial entry.
**Old:**
```
  - macOS client verification has no pid check: `peer_creds()` gives no pid there, so it rests on euid + the verified 0700 socket directory. Accepted for v1 because macOS is CI-only (brief O5 unmeasured). Revisit before public distribution.

`2026-09-23` — Phase 3.5 user review (round 1)
```
**New:**
```
  - macOS client verification has no pid check: `peer_creds()` gives no pid there, so it rests on euid + the verified 0700 socket directory. Accepted for v1 because macOS is CI-only (brief O5 unmeasured). Revisit before public distribution.
- **Needs without a researched tool (flagged, not invented):**
  - GUI/IPC rate limiting: not needed at this tier.
  - Secret scanning in CI (no scanner researched): revisit with the v1.x signing credentials.
  - An automated dependency-update bot (none researched): manual review, driven by the weekly `cargo deny check advisories`.
  - An HTML sanitizer for assistant Markdown (none researched): the frontend must render text only.
  - Encryption at rest (none researched, and plaintext is an arch decision).
- **v1.x release prerequisites** (Supply chain integrity, deferred with release):
  - dist `>=0.33.0` with GitHub Artifact Attestations and Azure Artifact Signing (x86_64).
  - cargo-auditable `>=0.7.6` + `cargo audit bin` (cargo-audit `>=0.22.2`) on release binaries.
  - self_update 1.3.0 built with `signatures` (zipsign).
  - zizmor cache-poisoning findings addressed.
  - No `rust-cache` in the release workflow.

`2026-09-23` — Phase 3.5 user review (round 1)
```

### Patch 3a: Remove the Windows sentence from the Unix key-decision bullet
**Reason:** STALE_ARTIFACT. Windows content is sitting in the "Unix:" bullet.
**Old:**
```
matching the Windows read rule, so pre-existing homes are covered on both OSes. On Windows the check also covers the version folders in use and the same trusted files, because a file can carry an explicit, non-inherited ACE that the directory checks do not see. On both OSes the check also covers the plugin folder's
```
**New:**
```
matching the Windows read rule, so pre-existing homes are covered on both OSes. On both OSes the check also covers the plugin folder's
```

### Patch 3b: Put that sentence in the Windows key-decision bullet
**Reason:** STALE_ARTIFACT, the companion to Patch 3a. The Windows bullet should state its own coverage, to match Auth & Authz line 207.
**Old:**
```
and it tests the generic bits (`GENERIC_READ`, `GENERIC_WRITE`, `GENERIC_ALL`) that inherit-only ACEs keep unmapped. On both OSes the check runs in every process
```
**New:**
```
and it tests the generic bits (`GENERIC_READ`, `GENERIC_WRITE`, `GENERIC_ALL`) that inherit-only ACEs keep unmapped. The check also covers the `bin/`/`plugin/` version folders in use and the trusted files (`snapshot.json`, `ledger/stamps.json`, the pinned exe), because a file can carry an explicit, non-inherited ACE that the directory checks do not see. On both OSes the check runs in every process
```

### Patch 4a: Give 0600 to the diagnostics files, not the directory (Data Protection)
**Reason:** CONTRADICTION. As written, the `diagnostics/` directory gets 0600, which conflicts with "Directories are 0700".
**Old:**
```
  - `events.ndjson` and `instances/<name>/diagnostics/` are 0600 inside 0700 directories.
```
**New:**
```
  - `events.ndjson` and the files in `instances/<name>/diagnostics/` are 0600 inside 0700 directories.
```

### Patch 4b: Give 0600 to the diagnostics files, not the directory (Error Handling)
**Reason:** CONTRADICTION, the same defect as Patch 4a.
**Old:**
```
- Full error detail goes only to `instances/<name>/diagnostics/` (0600; format owned by obs)
```
**New:**
```
- Full error detail goes only to `instances/<name>/diagnostics/` (files 0600; format owned by obs)
```

### Patch 5: Make the API Security scope cover the MCP transport row
**Reason:** CONTRADICTION. The intro says the section covers only the GUI, but the table has an MCP transport row.
**Old:**
```
_Applies to the loopback GUI (`viola-ui`, axum 0.8.9 + tower-http 0.7.1). The IPC channel's controls are in Authentication & Authorization and Input Validation._
```
**New:**
```
_Applies to the loopback GUI (`viola-ui`, axum 0.8.9 + tower-http 0.7.1), plus the MCP transport feature set (last row). The IPC channel's controls are in Authentication & Authorization and Input Validation._
```
