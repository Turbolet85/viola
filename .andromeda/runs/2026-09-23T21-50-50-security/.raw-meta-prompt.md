## Output Protocol

Rules for iteration agents working on `D:/dev/projects/viola/.andromeda/runs/2026-09-23T21-50-50-security/security-plan-draft.md`:

1. **Output format.** Give patches (old → new) plus a changelog. Do NOT reproduce the full document.
2. **Patch format.** Each patch looks like this:
   ```
   ### Patch N: <short description>
   **Old:**
   <exact text copied verbatim from the current draft, long enough to be unique>
   **New:**
   <replacement text>
   ```
   The **Old** text must match the draft byte for byte, including backticks, `§` signs, en dashes and table pipes. If you cannot quote it exactly, do not write the patch.
3. **Changelog.** Write one line per change: `[Iteration N] [substantive|cosmetic] description`. A change is substantive only if it changes a control, a requirement, a version, a threat mapping, a ban, or what a downstream reader can derive. Wording, ordering and formatting changes are cosmetic.
4. **Prohibited:**
   - Reproducing the full document.
   - Restructuring sections without a stated defect. Keep the template order: Threat Model Summary → Auth & Authz → Input Validation → Data Protection → API Security → Dependency Security → Bootstrap phases → Secret Management → Error Handling → Anti-Patterns → Decisions Log.
   - Labelling cosmetic changes as substantive.
   - Editing the Threat Model Summary except to restore verbatim fidelity with `threat-assessment.md`.
   - Adding tools or versions that are not in `security-research.md`.
   - Adding content owned by other specialists: test cases (tests' domain), design tokens or component patterns (design's domain), observability instrumentation schemas (obs' domain), accessibility attribute rules (a11y's domain), or picks of observability and error-reporting platforms.
5. **Decisions Log.** Any patch that changes a security decision (tier, boundary mechanism, arch amendment, accepted risk) must come with a companion patch that updates the `## Security Decisions Log`.
6. **No issues.** If you find nothing, output "No patches" and add a single cosmetic changelog line saying so.

## Analysis Dimensions

### 1. Threat Model Faithfulness [high]

- The Threat Model Summary is labelled "VERBATIM copy from threat-assessment.md".
  - Diff it against `threat-assessment.md` §1 Data Classification, §2 Attack Surface, §3 Auth Model, §4 Infrastructure, §5 Compliance Triggers and §6 Security Tier.
  - Look for any vector, data-classification entry or tier-justification clause that was paraphrased, merged or dropped. Examples: the `heartbeat` reader, the `hook statusline` `VIOLA_DIR` path, or the "affordance, not enforcement" MCP tool-list note.
- Check that each §2 vector maps to at least one concrete control in the plan body:
  - **Child process spawning / PATH resolution:** `list`, `mcp` and `ui` resolve `claude` from their own PATH. Is any control stated beyond the `.cmd`/`.bat` refusal and the absolute-path ban for `hooks.json`/`.mcp.json`? If the gap is accepted, is it recorded as an accepted risk?
  - **MCP driver prompt-injection into `answer` `allow`:** is it covered by anything beyond the accepted-risk line?
  - **Unbounded `events.ndjson` growth:** it is persistent and never rotated. Does the plan address it as a local disk-exhaustion or availability concern, or only defer it to arch?
- Check each §1 data class against Data Protection, Secret Management and the Anti-Patterns:
  - `permission.input`, which holds Bash commands and Write-tool file bodies that may contain secrets.
  - `budget.json` usage data.
  - `/api/sessions` unwrapped session identifiers.
  - Are all of them protected (0600/0700, cookie-gated, excluded from external errors)? Is any class left with no explicit control?
- Compliance is "None". Confirm the plan adds no Compliance Controls block. Also check that nothing in the v1.x distribution notes (`self_update`, winget/Homebrew/Scoop) quietly implies a compliance trigger that §5 does not list.

### 2. Tier Calibration [high]

- The tier is "Minimal (0), with targeted elevations for the local privilege boundary". Check that every above-Minimal control traces to one of the six §6 elevations:
  - IPC access control and impersonation
  - bracketed-paste breakout
  - GUI output encoding
  - GUI cross-user and cross-origin readability
  - v1.x brake auth
  - `~/.viola/` integrity

  Then check the full Auth & Authz table, the Windows SQOS + `server_process_id()` + sysinfo start-time verification, the SHA-256 re-hash of `bin/<version>-<hash>/`, and the 7-day CVE SLA. Is each one justified by a named elevation, or is any of it Standard-tier rigor with no stated driver?
- Minimal-tier SKIP markers:
  - Is `## Logging & Monitoring` absent, with its security floors pushed into the `logging-redaction-wire` bootstrap item and Anti-Patterns § Logging?
  - Is `### Supply chain integrity` absent, with the v1.x prerequisites only in the Decisions Log?
  - Does any section still carry Standard/Hardened material that should be cut? Candidates: the Key management and Data lifecycle notes in Data Protection, and the v1.x Azure Artifact Signing / zipsign entries under "What counts as secret".
- Check the opposite direction too. Is any elevation under-served relative to how it is described in §6? Two examples:
  - "integrity of `~/.viola/`": is the Windows `%USERPROFILE%` inheritance (user + SYSTEM + Administrators) justified?
  - "GUI output encoding": it is delegated to design with no researched sanitizer.

### 3. Tool Anchoring (Catalog ↔ Plan) [high]

- Check every named library and version against `security-research.md`. Flag any that are absent or that disagree with the catalog:
  - interprocess 2.4.4, windows-sys 0.61.2, sysinfo 0.39.6
  - getrandom 0.4.3, constant_time_eq 0.6.0, atomic-write-file 0.3.1
  - axum 0.8.9, tower-http 0.7.1, rmcp 3.4.1, schemars 1.2.2
  - nutype 0.8.0, serde_json 1.0.151, serde_path_to_error 0.1.20
  - chrono 0.4.45, notify 8.2.0, vt100 0.16.2, portable-pty 0.8.1
  - bytes `>=1.11.1`, tracing-subscriber `>=0.3.20`
  - the pinned action SHAs for `dtolnay/rust-toolchain` and `Swatinem/rust-cache`
- Check version syntax by tool class:
  - cargo-deny `>=0.20.2`, zizmor `>=1.30.1`, cargo-audit `>=0.22.2`, dist `>=0.33.0` and cargo-auditable `>=0.7.6` are external CLI tools and must be floors.
  - self_update 1.3.0 and every crate dependency must be exact pins.
  - Flag any place where the Infrastructure block, the Decisions Log and Dependency Security disagree on syntax. For example, "dist 0.33.0" in one place and `>=0.33.0` in another.
- Check that picks are justified:
  - Does the plan say why atomic-write-file was chosen over the catalogued tempfile 3.27.0?
  - Why constant_time_eq over any other catalogued comparison crate?
  - Why hand-written `Sec-Fetch-Site`/`Origin` code over `tower-csrf`/`axum_csrf`? (A rejection is noted in API Security; is it traced to research?)
  - The SHA-256 re-hash is a hard requirement, but its crate is an open question ("pick one at implementation time"). Is that acceptable as a "no catalogued option" note? Or does it leave a MUST control unimplementable for `setup-project`, so that it needs a stronger constraint (pure-Rust, passes the cargo-deny C-build ban, recorded before the `auth-scaffolding-baseline` phase)?

### 4. Anti-Pattern Relevance [medium]

- Check that every ban under Authentication, Input, Data Protection, API, Secrets, Logging and Code Patterns is grounded in viola's Rust / interprocess / axum / portable-pty stack. Flag any that are generic web leftovers. For example, is `X-Forwarded-For` a real pitfall for a loopback server with no proxy, or filler?
- Check that no frontend bans are missing from the centralized list. The `innerHTML` / `v-html` / `dangerouslySetInnerHTML` / Markdown-to-HTML / inline `<script>` / `eval` bans appear only inline in the API Security "Frontend output-encoding constraints" row. Should they move to Anti-Patterns § API (or a Code Patterns entry) with a cross-reference left behind?
- Look for bans duplicated inline in section bodies instead of cross-referenced:
  - "Reject, don't strip" and "never over raw bytes" (Input Validation)
  - "Do not add `CorsLayer`" (API Security CORS row)
  - "Never use `ServeDir`" (Static assets row)
  - "Do not use axum-extra's `Host` extractor" (Host header row)

  Decide which inline repeats should become `(See § …)` references.
- Universal has 7 bans. Most name viola artefacts: `hooks.json` PATH resolution, `ledger/stamps.json`, `snapshot.json` writers, `VIOLA_DIR`. Are they really universal (they apply across every chunk and crate), or stack-specific bans that belong under Data Protection or Code Patterns? Does a genuinely cross-cutting principle, such as "fail open to the human", still meet the ≥5 universal floor after any moves?

### 5. Downstream Readiness [high]

- **design:**
  - Can design derive every UI security constraint for `viola-ui` from this plan alone? That includes text-only rendering of prompts, `last_assistant_message`, plan text and tool `input`, no Markdown-to-HTML, and the CSP with `require-trusted-types-for 'script'`.
  - Does it also cover the `?t=` token exchange flow: what the page shows on a 401 `urn:viola:problem:unauthorized` and how the user is told to recover? Is anything left implicit?
- **tests (scope only):**
  - Does the plan name enough fuzz and property targets for tests to scope them? Candidates: `validate_paste_text`, the `Last-Event-ID` parser, channel ndjson framing with `MAX_FRAME`, the hook stdin parser, the vt100 `catch_unwind` path.
  - Does it name the cross-OS verification surfaces: the Windows DACL, the Unix 0700 dir + `peer_creds`, the macOS euid-only gap?
  - Or must tests reverse-engineer them from Anti-Patterns?
- **obs:**
  - The Logging section is skipped. Is there one consolidated NEVER-log list obs can consume, covering the GUI token, the `Cookie` header, `CLAUDE_CODE_MESSAGING_TOKEN`/`_SOCKET`, R8-stripped `CLAUDE*` values, and tool `input` outside `diagnostics/`?
  - Or is it scattered across `logging-redaction-wire`, Error Handling, and Anti-Patterns § Secrets and § Logging? If scattered, is a single cross-referenced list needed?
- **route / setup-project:**
  - Does each Bootstrap phase map to concrete artefacts `setup-project` can materialise? That means the `deny.toml` blocks, the `ci.yml` schedule, zizmor and permissions edits, and `.claude/rules/*` security rule files for `@` imports.
  - Are the 8 arch amendments in the Decisions Log phrased so route can sequence them? In particular, the Unix endpoint path move and the v1 GUI cookie must happen before the `viola-ui` and `viola-channel` chunks.
- **a11y:** Is it explicit that there are no auth or recovery forms, so a11y has no secure-form surface to derive?

### 6. API Hardening Completeness [medium] [trigger: loopback HTTP GUI viola-ui (axum 0.8.9 + tower-http 0.7.1) is a §2 attack-surface vector]

- **Cookie scoping (cross-port and cross-user):**
  - Cookies are not port-scoped, and `SameSite=Strict` treats all ports on `127.0.0.1` as same-site. Does the plan address another local user's HTTP server on `127.0.0.1:<other port>` receiving `viola_<port>` in a same-site request?
  - Does it address that server's page issuing same-site requests to `/api/*`? The `Sec-Fetch-Site: same-origin` check guards only the v1.x POSTs.
  - Does the "GUI cross-user readability" elevation hold for v1 GET/SSE reads with only the cookie + Host check?
- **Host allowlist edge cases:**
  - The launch URL uses `127.0.0.1`. A cookie set there is not sent to `localhost:<port>`. Is allowing both `127.0.0.1:<port>` and `localhost:<port>` in the Host allowlist consistent with the cookie flow?
  - What happens with a missing `Host` header (HTTP/1.0), letter case (`LOCALHOST`), or a trailing dot? Is exact matching specified precisely enough to implement?
- **Controls table coverage:**
  - Rate limiting is "None in v1". Is there a bound on concurrent SSE `/api/events` connections, or on per-connection tail file handles? Without one, a local process holding a valid cookie, or hammering 401s, could exhaust resources.
  - Is the `/?t=` exchange's failure behaviour specified: the response to a wrong token, and whether the `Cache-Control: no-store` / `Referrer-Policy` headers apply to the failed response too?
  - Is the `CompressionLayer` `text/event-stream` exclusion stated as a concrete predicate for tower-http 0.7.1, or left as "re-test"?

### 7. Auth Recovery Completeness [medium, adapted] [trigger: GUI per-launch token exchanged for a browser-session cookie viola_<port>, HttpOnly, SameSite=Strict, no Max-Age]

_There are no accounts, so the recovery surfaces are adapted: password reset / account recovery / MFA map to cookie or launch-URL loss and token compromise._

- The plan omits `### Authentication Recovery Flows` because the token is "API-key style". Yet the Auth & Authz table, the "Session / token rotation" row and Secret Management's "Per-incident" line already define three recovery paths: restart `viola ui`, re-read `<viola home>/ui/<port>.url`, and the stderr launch line.
  - Should these be consolidated into a short recovery sub-section, so the recovery behaviour is stated once and cross-referenced?
  - It would cover lost cookie after the browser closes, a lost terminal line, suspected token leak, and a stale `.url` after a crash (it is removed only on graceful shutdown).
- Recovery rate limiting and enumeration:
  - Is it stated that no recovery shortcut may exist? For example, no `/api/token` endpoint, no reprint over any GUI route, and no token in the 401 Problem Details `detail`.
  - Is the corresponding ban present in Anti-Patterns § Authentication, next to the existing "Account-flow bans … N/A" line?
- Two `viola ui` launches, and stale files:
  - Two launches on different ports each get their own `viola_<port>` cookie and `.url` file. Is it specified what happens on a port collision, or when a crashed launch leaves a stale `.url` file that a later launch on the same port must overwrite atomically (atomic-write-file 0.3.1, 0600)?
  - Can a stale token ever be accepted?

Read the document, analyze along all dimensions, output patches and changelog.
