`agent-run run --mutants` mutation-tests the chunk diff: the working tree plus untracked files from the merge-base of the base, where the base is `AGENT_RUN_CHUNK_BASE`, then `git merge-base HEAD origin/main`, then `github.event.before` or the PR base sha in CI. An unreachable base (the all-zero sha included) exits 1 with `reason:"base-missing"` and leaves no `target/agent-run/chunk.diff`. The verdict reads `mutants.out/outcomes.json`: any missed or timed-out mutant, or a cargo-mutants exit of 1/4/5/6/70, exits 1. Unit tests over fixture outcomes prove each of these branches. The ubuntu `mutants` CI job runs the same command on every push and pull_request, and it concluded success on the push that carries this chunk. This chunk's own run on base `13b7ee32e6227d1cb6cecc96fbe412fe34a4e0e6` exits 0 with the `mutants` suite `survived: 0`, over a diff that contains the panic hook and the role logger, so observability code is mutated like product code. Each guard test in this plan (the base-missing guard and the mutants exit-code guard) kills its own remove-the-guard mutant within that run.
