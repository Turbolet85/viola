proposals:
  - detector: D-obs-stack
    severity: warning
    section: "## 3. Observability Harness Contract > ### Bootstrap phases > logger-stack-install (disallowed-macros exemption bullets)"
    change: "Replace the exemption bullets. clippy 1.98.1 cannot exempt obs_event!'s inner ::tracing::event! with an allow attribute in any expansion position; only a crate-level #![allow] in the caller crate silences it. The workspace clippy.toml therefore bans only the level macros tracing::{info,warn,error,debug,trace} by path, and tracing::event is deliberately not listed. Raw event! is caught by a fail-closed grep (\\bevent!\\s*[({[] in *.rs outside crates/viola-core/src/obs.rs) in scripts/lint-probes.sh. The inner #[allow(clippy::disallowed_macros)] at crates/viola-core/src/obs.rs:155 stays but has no effect. Proof both ways, by lint-probes.sh: raw tracing::info! fails, an obs_event! call lints clean, and the grep finds a planted raw event! and passes obs_event!."
    sidecar: "obs-plan §3 logger-stack-install: the disallowed-macros exemption through an inner allow was measured false on clippy 1.98.1. The level macros are now banned by path in clippy.toml, raw event! is caught by a fail-closed grep in scripts/lint-probes.sh, and the inner allow is kept but inert (chunk 2026-09-24-observability-gates)."
    rationale: "The report's 'Spec claims disproved by measurement' item 1 tested 8 placements of the allow in a throwaway workspace; all still reported 2 errors per call, and viola showed 16 errors at obs_event! sites. Under 'Schema / config', clippy.toml lists only tracing::{info,warn,error,debug,trace}. The operator decision at /implement P1 moved the raw-event! ban to the lint-probes.sh grep (Deviation 1). 'Insufficient fixes' records obs.rs:155 as inert. So the §3 bullets describe a mechanism that does not work."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:783-786; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:53,91-96,124"
  - detector: D-obs-stack
    severity: warning
    section: "## 3. Observability Harness Contract > ### Bootstrap phases > obs-ci-gate-wire (bullet 1)"
    change: "Bullet 1 should read: clippy.toml disallowed-macros bans raw tracing::{info,warn,error,debug,trace} outside viola_core::obs (tracing::event is not listed, because listing it fires at every obs_event! site). Raw event! is banned by the fail-closed grep in scripts/lint-probes.sh, which runs in the ci.yml lint job on the Linux leg only (source text is OS-independent)."
    sidecar: "obs-plan §3 obs-ci-gate-wire bullet 1: the clippy.toml ban is narrowed to the level macros, and the raw-event! ban moves to the lint-probes.sh grep on the Linux lint leg."
    rationale: "The report cites this bullet ('ban tracing::{event,info,warn,error,debug,trace}') in disproved claim 1. Under 'Negative API facts', tracing::event was added to clippy.toml and then removed because it fired at all 16 obs_event! call sites. Deviation 2 puts the grep on the Linux lint leg only."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:799; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:53,91,94,125"
    dependent-of: D-obs-stack
  - detector: D-obs-stack
    severity: warning
    section: "## 9. CI Integration > Pipeline integration table (Lint / typecheck row)"
    change: "The Lint / typecheck row should read: clippy -D warnings incl. disallowed-macros (raw tracing::{info,warn,error,debug,trace}! outside viola_core::obs), plus the fail-closed raw-event! grep in scripts/lint-probes.sh (Linux leg, proven both ways; mechanism in §3 logger-stack-install). This replaces 'raw tracing::*! … exemption mechanism in §3 logger-stack-install'."
    sidecar: "obs-plan §9 Lint row: disallowed-macros covers only the level macros, and the raw-event! grep in lint-probes.sh is added as a lint gate."
    rationale: "The row repeats the retired claim twice: that clippy bans every tracing::*! macro, and that an allow-based exemption exists in §3. The report's 'Harness / gate surface' lint job lists the lint probes on Linux, and disproved claim 1 retires the exemption."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1243; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:73-76,84"
    dependent-of: D-obs-stack
  - detector: D-obs-stack
    severity: warning
    section: "## 11. Obs Anti-Patterns > ### Logs"
    change: "In the rule 'NEVER call tracing::{event,info,warn,error,debug,trace}! directly outside viola_core::obs', change the enforcement parenthetical from '(clippy disallowed-macros)' to '(clippy disallowed-macros for the level macros; the fail-closed raw-event! grep in scripts/lint-probes.sh for event!)'."
    sidecar: "obs-plan §11 Logs: the enforcement of the raw-tracing ban is split between clippy (level macros) and the lint-probes.sh grep (event!)."
    rationale: "The rule is unchanged, but it names clippy disallowed-macros as the enforcement for all six macros. The report shows clippy cannot ban event! without breaking every obs_event! site (report:91), and the ban is carried by the grep (report:92)."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1376; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:91-92"
    dependent-of: D-obs-stack
  - detector: D-obs-stack
    severity: warning
    section: "## 8. PII Scrubbing & Compliance > ### PII Scrubbing > Integration points (item 1, Source allow-list)"
    change: "Item 1 should say it is enforced by clippy disallowed-macros (the level macros), the fail-closed raw-event! grep in scripts/lint-probes.sh, and the grep gate G1 on bare #[instrument]."
    sidecar: "obs-plan §8 integration point 1: the lint-probes.sh raw-event! grep is added to the enforcement of the obs_event!-only source allow-list."
    rationale: "Item 1 says clippy disallowed-macros alone guarantees that only obs_event! emits. The report measured that clippy no longer covers raw event! and that the lint-probes.sh grep does (report:76, 92, 96)."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1193; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:76,92"
    dependent-of: D-obs-stack
  - detector: D-obs-stack
    severity: warning
    section: "## 10. SLO Invariants & Telemetry Budgets > Build / deploy failure conditions"
    change: "Add a failure of the raw-event! grep in scripts/lint-probes.sh (a raw event! outside crates/viola-core/src/obs.rs, or the probe's own positive/negative control failing) beside the clippy disallowed-macros / print_stdout / print_stderr / dbg_macro failure."
    sidecar: "obs-plan §10 failure conditions: a lint-probes.sh raw-event! grep failure is listed as a build failure."
    rationale: "The failure list treats the clippy disallowed-macros failure as the only signal for the raw-tracing ban. For event! that signal moved to the fail-closed grep, which the operator required to be proven both ways (report:96, 143)."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1336; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:76,96"
    dependent-of: D-obs-stack
  - detector: D-obs-stack
    severity: warning
    section: "## 12. Obs Decisions Log"
    change: "Append a new entry (leave D-25 as written) that supersedes D-25's clause 'The disallowed-macros exemption is an allow attribute inside the macro expansion'. The entry says the clause was measured false on clippy 1.98.1, that the operator decided at /implement P1 (overseer, founder-delegated) to ban the level macros by path in clippy.toml and catch raw event! with a fail-closed grep in lint-probes.sh, and gives the evidence file .andromeda/runs/2026-09-24T12-21-11-implement/clippy-disallowed-macros-measurement.md. Impact: §3 logger-stack-install and obs-ci-gate-wire, §8, §9, §10, §11."
    sidecar: "obs-plan §12: a new Decisions Log entry records that D-25's inner-allow disallowed-macros exemption is superseded by the path ban plus the raw-event! grep."
    rationale: "D-25 is a past decision whose clause restates the retired claim. Rewriting a Decisions Log entry would falsify history, so the supersession is recorded as a new entry. It also documents the operator ruling that the report cites under Decisions & corrections."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1639; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-observability-gates/report.md:95-96,143"
    dependent-of: D-obs-stack
# D-obs-instrumentation: no drift. The chunk adds no product hot-path operation. The report says src/ and crates/viola-core gained no code apart from the fake agent's crate-level #![allow] (report:48). The new surfaces (viola-harness schema-check / secret-scan, lint-probes.sh, install-ripgrep.sh, ci.yml steps) are test/CI tooling and marked instrumentation n/a (report:117-121). obs-plan §1 classes build/CI as not-instrumentable.
# D-obs-pii: no drift. No product logging was added. schema-check never prints line content, and secret-scan never prints or writes matched bytes; its hit report is {file,line,offset,class} (report:34,117-118). This matches §8 item 6 ('file, line, byte offset and pattern class, never the matched bytes') and §9 G4 ('never the line's content').
