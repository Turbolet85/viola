proposals:
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Test Harness Contract → Internal harness subcommands"
    change: "Add two internal `viola-harness` subcommands beside `supervise`. Like it, they are forwarded unchanged by both shims and are not part of the 5-command agent surface. (1) `schema-check` is the G4 check body. It prints `{\"v\":1,\"cmd\":\"schema-check\",\"ok\",\"files\",\"lines\",\"torn\",\"skipped_non_file\",\"failures\":[{file,line,keyword}]}` and its reasons are `empty-scope` and `schema-unreadable`. (2) `secret-scan` is the §6 canary/secret scan body. It prints `{\"v\":1,\"cmd\":\"secret-scan\",\"ok\",\"files\",\"skipped_non_file\",\"mode_check\":\"unix\"|\"skipped-windows\",\"hits\":[{file,line,offset,class}]}`, adds `report:\"unwritten\"` when the hit file cannot be written, and its reason is `empty-scope`. Its classes are `claude-stripped`, `token-query`, `cookie`, `gui-token`, `content-canary`, `mode` and `unreadable`. It writes `target/secret-scan/hits.json` only when there are hits and deletes a stale copy on every run. Each prints one JSON document and exits 0, 1, or 2 on usage error. Add their reason and class values to Closed enums."
    sidecar: "§3 Internal harness subcommands: added `schema-check` (G4 body) and `secret-scan` (canary/secret scan body) beside `supervise`, with output shapes, reasons and closed classes (chunk 2026-09-24-observability-gates)."
    rationale: "The report's Symbols/APIs section adds two `viola-harness` subcommands, `schema-check` and `secret-scan`, and `COMMANDS` goes from 6 to 8 (5 agent + 3 internal). Counts moved notes that test-plan §3 still lists the internal set as supervise, ui-restart, gate. This is Expected amendment 2. obs-plan §9 G4 and secret-scan steps now invoke these commands, but test-plan §3 does not declare them, so only one side of the harness contract changed."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:610-643; report.md:32-37,62,104-106"
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Test Harness Contract → 5-command implementation → `run` step 2 (root rstest chain `booted_wrapper`)"
    change: "The root `booted_wrapper` waits for the interim readiness lines (`process-start` for `self` and `claude-child`) with an exit-aware wait bounded at 10 s (`READY_WITHIN`). If `child.try_wait()` reports that the wrapper exited first, it panics with `wrapper {name} exited before ready: {status}` instead of waiting out the deadline. Harness `boot` keeps its 20 s per-instance bound."
    sidecar: "§3 `run` step 2: root `booted_wrapper` interim readiness bound 20 s → 10 s, exit-aware (panics on early wrapper exit). Harness boot bound unchanged (chunk 2026-09-24-observability-gates)."
    rationale: "Symbols/APIs: `Wrapper::wait_ready` now takes `&mut self`, panics on exit, and `READY_WITHIN` goes from 20 s to 10 s. Counts moved: the root-chain readiness bound goes from 20 s to 10 s, while the harness `boot` bound stays 20 s. Expected amendment 1 names `:542` as the one `20 s` site that changes; `:530`, `:738` and `:1483` keep 20 s."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:542; report.md:46-47,63,100-103"
  - detector: D-tests-obs-harness
    severity: warning
    section: "§3 Test Harness Contract → Internal harness subcommands → `gate` (CI upload sentence)"
    change: "Replace 'CI uploads `target/agent-run/artifacts/` as `agent-run-${{ matrix.os }}`' with: in the per-OS `test` job, `target/agent-run/` leaves CI only in obs-plan's scan-gated `harness-${{ matrix.os }}` artifact (`if: failure() && steps.secret-scan.outcome == 'success'`). There is no separate `agent-run-<os>` upload."
    sidecar: "§3 `gate`: dropped the `agent-run-<os>` upload claim; the test job's harness output goes only in the scan-gated `harness-<os>` artifact (chunk 2026-09-24-observability-gates)."
    rationale: "Counts moved: the `ci.yml` `test` job gains 4 uploads (`diag`, `junit`, `harness`, `secret-scan`) and loses 1 (`agent-run-<os>`). The upload surface under Harness/gate follows obs-plan §9: `harness-<os>` is `if: failure() && … == 'success'`. obs-plan (Pipeline integration decision: 'The CI harness output goes to its own `harness-<os>` artifact') and the new CI agree. test-plan §3 still claims an unconditional `agent-run-<os>` upload, which obs-plan's scan-before-upload rule forbids."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:638; report.md:67,78-83"
  - detector: D-tests-obs-harness
    severity: warning
    section: "§9 CI Integration → Test report format"
    change: "Replace the '`target/agent-run/artifacts/` as `agent-run-${{ matrix.os }}`' bullet with: on test failure, `target/agent-run/` (including `artifacts/` and the `logs.ndjson` / `status.json` capture) is uploaded as `harness-${{ matrix.os }}`, gated on `steps.secret-scan.outcome == 'success'`. Add the always-on scan-gated `diag-<os>` and `junit-<os>` uploads, and `secret-scan-<os>` (the hit report, on a failed scan). All use upload-artifact v7.0.1 with `retention-days: 7`."
    sidecar: "§9 Test report format: `agent-run-<os>` upload replaced by the scan-gated `harness-<os>` / `diag-<os>` / `junit-<os>` / `secret-scan-<os>` set (chunk 2026-09-24-observability-gates)."
    rationale: "Same retired claim as the §3 `gate` site. The report's Counts moved says the test job loses the `agent-run-<os>` upload, and Harness/gate surface lists the four scan-gated uploads."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1419-1426; report.md:67,78-83"
    dependent-of: D-tests-obs-harness
  - detector: D-tests-framework
    severity: warning
    section: "§3 Test Harness Contract → Bootstrap phases → test-runner-install (`.config/nextest.toml` `[profile.mutants]`)"
    change: "`[profile.mutants]` sets `slow-timeout = { period = \"5s\", terminate-after = 2 }`, a 10 s kill below cargo-mutants 27.1.0's 20 s auto-timeout floor (`max(20 s, ~5 × baseline)`), and keeps `fail-fast = { max-fail = 1, terminate = \"immediate\" }`. Add `[[profile.mutants.overrides]] filter = 'package(viola-e2e)'` with `slow-timeout = { period = \"15s\", terminate-after = 2 }`, a 30 s kill, so the harness's own tests (for example the by-design 20 s boot-deadline test) are not cut off."
    sidecar: "§3 test-runner-install: `[profile.mutants]` slow-timeout 15 s×2 → 5 s×2 (10 s kill), plus a `package(viola-e2e)` override keeping 15 s×2 (chunk 2026-09-24-observability-gates)."
    rationale: "Schema/config: `.config/nextest.toml` `[profile.mutants]` goes from 15s×2 to 5s×2 and gains the new `package(viola-e2e)` override at 15s×2. Counts moved: the mutants kill goes from 30 s to 10 s for root and stays 30 s for `viola-e2e`. Deviation 3 and disproved claim 2 explain the override. Expected amendment 1 names `:729` as the `profile.mutants` site that changes; `:1727` keeps."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:729; report.md:56-58,64,97,126"
  - detector: D-tests-framework
    severity: warning
    section: "§3 Test Harness Contract → Bootstrap phases → ci-tool-install"
    change: "Drop jaq's CI role. CI log assertions (G2) use the runner-provided `jq`, presence-checked in the `test` job and never installed or pinned. Add: ripgrep 15.2.0 (PCRE2), the G1/G3 gate tool, is installed in the `lint` job by `scripts/install-ripgrep.sh`, which is pinned, sha256-verified against the release `.sha256` assets, idempotent, and prints `tool-missing: <tool>` when a tool is missing. It installs into `target/tools/ripgrep/bin` and is placed on PATH. taiki-e/install-action has no ripgrep manifest."
    sidecar: "§3 ci-tool-install: G2 uses runner `jq` (presence-checked, not installed); ripgrep 15.2.0 via `scripts/install-ripgrep.sh` (chunk 2026-09-24-observability-gates)."
    rationale: "Dev-tool versions: ripgrep 15.2.0 is installed by `scripts/install-ripgrep.sh` in the CI `lint` job, and jq is runner-provided and presence-checked, not installed. Cross-project claims: taiki-e has no `manifests/ripgrep.json` (404). Expected amendment 3 names `:758` and `:1412` as the jaq sites that change; `:606` and `:682` are assertion-form mentions and keep."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:758; report.md:68-71,77,90,107-109"
  - detector: D-tests-framework
    severity: warning
    section: "§9 CI Integration → tool-install paragraph (after the Pipeline structure table)"
    change: "Remove jaq 3.1.1 from the `cargo install --locked` list. State that G2 uses the runner-provided `jq` (presence-checked, not installed), and that ripgrep 15.2.0 comes from `scripts/install-ripgrep.sh` (pinned, sha256-verified) in the `lint` job. hyperfine, cargo-modules and zizmor stay on `cargo install --locked`."
    sidecar: "§9 tool install: jaq dropped for runner `jq` (G2); ripgrep 15.2.0 via `scripts/install-ripgrep.sh` (chunk 2026-09-24-observability-gates)."
    rationale: "Same retired claim as §3 ci-tool-install ('jaq 3.1.1 … installed with `cargo install --locked` in the jobs that invoke them'). The report's Dev-tool versions section and Expected amendment 3 name `:1412` as a changing site."
    basis: "D:/dev/projects/viola/.andromeda/test-plan.md:1412; report.md:68-71,107-109"
    dependent-of: D-tests-framework
