proposals:
  - detector: D-security-auth
    severity: warning
    section: "Secret Management (**Secret scanning in CI:** line)"
    change: "Secret scanning in CI: no repo secret scanner in v1 (none researched; revisit with the v1.x signing credentials). The CI `test` job does run the self-implemented `viola-harness secret-scan` canary/leak scan (`id: secret-scan`, `if: always()`) over test homes' diagnostics. It covers the classes claude-stripped canaries, `?t=`, `cookie:` / `viola_<digits>=`, the GUI token read from each `ui/*.url`, the content canary, and Unix non-0600 mode. It never prints or writes the matched bytes. The `diag-`, `junit-` and `harness-<os>` artifact uploads run only when the scan succeeds. The `mutants` job's `mutants.out/` upload is still unscanned (an open CARRY)."
    sidecar: "Secret Management: 'Secret scanning in CI: Not in v1' corrected. CI now runs the harness secret-scan canary gate before artifact uploads (chunk 2026-09-24-observability-gates)."
    rationale: "Report Changes > Symbols / APIs adds the `secret-scan` subcommand and its scan classes, including the gui-token read from `ui/*.url` and the cookie pattern. Harness / gate surface wires it into ci.yml `test` (`id: secret-scan`) and gates the uploads on `steps.secret-scan.outcome == 'success'`. Coverage says matched bytes are never printed or written. That flow is consistent with the NEVER-log floor, but the doc still says CI has no secret scanning, which no longer matches the secret-handling surface CI actually runs. Decisions & corrections records the unscanned `mutants.out/` upload as a CARRY."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:421"
  - detector: D-security-auth
    severity: warning
    section: "Bootstrap phases (derive for route / setup-project) - secret-scanning-ci-gate"
    change: "secret-scanning-ci-gate: no repo secret scanner in v1 (none researched). The CI `test` job runs the internal `viola-harness secret-scan` canary scan over test-home diagnostics, and every scan-gated artifact upload (`diag-`, `junit-`, `harness-<os>`) depends on its success. `mutants.out/` is still unscanned (CARRY). `.gitignore` still excludes local `--home` test directories so recorded state never lands in git."
    sidecar: "Bootstrap secret-scanning-ci-gate: 'Not wired in v1' replaced by the harness secret-scan upload gate (chunk 2026-09-24-observability-gates)."
    rationale: "The same retired claim restated in the bootstrap list: 'Not wired in v1. There are no secrets in the repo or CI, and no scanner was researched'. The report's ci.yml `test` job now has a `secret-scan` step that gates uploads (Harness / gate surface)."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:381"
    dependent-of: D-security-auth
# I did not propose edits to three places:
# - security-plan.md:601, the dated Decisions Log entry "Secret scanning in CI (no scanner researched)". It is a historical record, not live body text.
# - security-plan.md:161, the CI job list. It sits inside the verbatim Threat Model copy from threat-assessment.md.
# D-security-input: no drift.
# - No IPC, HTTP or product deserialisation surface was added. The report says "No env var, port, socket or product symbol was added", and src/ and viola-core gained no code apart from one `#![allow]`.
# - The two new subcommands, `schema-check` and `secret-scan`, are internal to the test harness. They read files that the tests themselves produce.
# D-security-deps: no drift.
# - The only new crate is jsonschema =0.57.0. It was already locked in the graph as a root dev-dependency, and the change adds no new package or version.
# - `cargo deny check` is green, and jsonschema is not on any ban list (C-build, sync-crate tokio, telemetry, rmcp or axum features).
# - ripgrep 15.2.0 and jq are dev tools, not banned. ripgrep is pinned and checksum-verified.
# - No new GitHub Action was added. upload-artifact@043fb46d v7.0.1 is already in the pinned set.
