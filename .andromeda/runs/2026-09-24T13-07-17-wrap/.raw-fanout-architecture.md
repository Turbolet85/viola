proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Repository
    change: Add three gitignored target/ paths, in the same form as the `target/deny-probes/` line. (1) `target/secret-scan/`, whose only file is `hits.json`. `viola-harness secret-scan` writes it only when there are hits, removes a stale copy on every run, and CI uploads it as artifact `secret-scan-<os>` (7 days) only when the scan fails. (2) `target/tools/ripgrep/`, where `scripts/install-ripgrep.sh` installs the pinned ripgrep 15.2.0 in `bin/`. (3) `target/lint-probes/run-<utc>-<pid>/` plus `target/lint-probes/target/`, the throwaway crates and shared target dir that `scripts/lint-probes.sh` writes on each run.
    sidecar: Occupied Resources → Repository registers target/secret-scan/, target/tools/ripgrep/ and target/lint-probes/ from the observability-gates chunk.
    rationale: The report's "Harness / gate surface" bullet lists these as new registered-path candidates, all gitignored. Its "Symbols / APIs" bullet says secret-scan writes target/secret-scan/hits.json only on hits. Expected amendment 4 names this registry. None of the paths appears in arch today.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:381-385
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → Project directory structure
    change: Add `clippy.toml` at the repo root, commented "disallowed-macros: tracing::{info,warn,error,debug,trace}". Under `scripts/` add `lint-probes.sh` ("proves each clippy ban fires, plus controls and the fail-closed raw-`event!` grep") and `install-ripgrep.sh` ("pinned, sha256-verified ripgrep 15.2.0 into target/tools/ripgrep"), beside `deny-probes.sh`.
    sidecar: The directory tree registers clippy.toml, scripts/lint-probes.sh and scripts/install-ripgrep.sh.
    rationale: The report's Files → New lists clippy.toml, scripts/lint-probes.sh and scripts/install-ripgrep.sh. "Harness / gate surface" lists them as registered-path candidates, and expected amendment 4 cites the scripts/ tree. The tree has only agent-run, sync-crates.txt and deny-probes.sh, and no clippy.toml.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:418-423,448-451
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: 'Rewrite the "Jobs wired today" `test` and `lint` entries. `test` runs the per-OS harness suites and lifecycle with AGENT_RUN_KEEP_HOMES=1, then follows the obs-plan §9 order: G2 (jq presence check first), G4 `schema-check` (id schema-conformance), a harness capture on failure, and `secret-scan` (id secret-scan, if: always()). Its uploads are gated on that scan: `diag-<os>` and `junit-<os>` run if always() && scan success, `harness-<os>` if failure() && scan success, and `secret-scan-<os>` if always() && scan failure, all with upload-artifact v7.0.1 and 7 days. The `agent-run-<os>` upload is gone. `lint` runs per OS: fmt, clippy -D warnings --features fake-agent, the ripgrep install, rg --pcre2-version, G1, G3, and lint-probes on Linux only.'
    sidecar: The ci.yml job description retires the agent-run-<os> artifact, registers the scan-gated diag/junit/harness/secret-scan-<os> uploads, and lists the lint job's new gates.
    rationale: 'The report''s "Counts / qualifiers moved" says the test job gains 10 steps (jq, G2, G4, harness capture, secret scan, 4 uploads) and loses the `agent-run-<os>` upload, and the lint job gains 8. "Harness / gate surface" gives the exact upload conditions. Arch still says `target/agent-run/artifacts/` is uploaded as `agent-run-<os>` and describes lint as "target job 3" only.'
    basis: D:/dev/projects/viola/.andromeda/architecture.md:468
    dependent-of: D-arch-resources
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies (Code quality row)
    change: 'Extend the Code quality row. clippy -D warnings --features fake-agent now runs with workspace lint bans: [workspace.lints.clippy] sets print_stdout, print_stderr and dbg_macro to deny, and clippy.toml disallowed-macros bans tracing::{info,warn,error,debug,trace}. A fail-closed raw-`event!` grep sits in scripts/lint-probes.sh. Add ripgrep 15.2.0 (PCRE2 10.45) as the G1/G3 gate tool, installed by scripts/install-ripgrep.sh; jq, runner-provided and presence-checked, for G2; and jsonschema 0.57.0 for the harness schema-check (G4).'
    sidecar: The Stack Code quality row records the clippy print/dbg/raw-tracing bans, ripgrep 15.2.0, jq and the jsonschema-backed schema-check.
    rationale: The report's "Dev-tool versions" adds ripgrep 15.2.0 and runner jq as gate tools. "Schema / config" adds [workspace.lints.clippy] and clippy.toml disallowed-macros. Deviation 1 and "Spec claims disproved" 1 define the raw-event! grep. Dependencies adds jsonschema to viola-e2e. None of these is in §Stack.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:36
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: 'Setup steps now say that ripgrep 15.2.0 is not installed through taiki-e/install-action, which has no ripgrep manifest. `scripts/install-ripgrep.sh` downloads the official release asset, checks it against the release''s .sha256 pin, and installs it idempotently into target/tools/ripgrep/bin, exporting it on PATH in the `lint` job. jq is runner-provided and presence-checked in the `test` job, never installed.'
    sidecar: CI setup steps record the pinned, checksum-verified ripgrep install script and the runner-provided jq.
    rationale: 'The report''s "Dev-tool versions" and "Harness / gate surface" describe install-ripgrep.sh as pinned, checksum-verified and idempotent. "Cross-project claims" records that taiki-e/install-action 7623a79… has no manifests/ripgrep.json (404). Decisions (P4/P5) set jq as presence-checked with no install. Arch''s setup steps list only taiki-e install-action and cargo install (zizmor) as tool-install paths.'
    basis: D:/dev/projects/viola/.andromeda/architecture.md:467
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → Build system
    change: 'Change the Lint bullet to: `cargo fmt --all --check` and `cargo clippy --workspace --all-targets --features fake-agent -- -D warnings`, under [workspace.lints.clippy] (print_stdout, print_stderr and dbg_macro deny; viola-e2e opts out of the workspace table and denies only dbg_macro; the fake agent carries a crate-level allow for print_*) and clippy.toml disallowed-macros on the tracing level macros. The only sanctioned emitter is `obs_event!`. Raw `event!` is caught by the fail-closed grep in scripts/lint-probes.sh, because clippy 1.98.1 cannot exempt a macro''s inner expansion. G1/G3 are ripgrep gates, and lint-probes.sh proves every ban fires and every control passes.'
    sidecar: The Build system Lint bullet records the workspace clippy bans, clippy.toml, the obs_event!-only rule, the raw-event! grep and the lint-probes proof.
    rationale: The report's "Schema / config" adds [workspace.lints.clippy], clippy.toml, viola-e2e's own [lints.clippy] and the fake-agent #![allow]. "Harness / gate surface" says the lint job runs clippy with --features fake-agent and lint-probes proves 4 bans and 2 controls. Deviation 1 and the operator decision at /implement P1 lock the level-macro ban plus a raw-event! grep. The Lint line still names only fmt and a featureless clippy.
    basis: D:/dev/projects/viola/.andromeda/architecture.md:393
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: 'Change target job 2 to `cargo clippy --workspace --all-targets --features fake-agent -- -D warnings`, with the workspace lint bans and clippy.toml in force. Note that target jobs 1 and 2 are now wired in the `lint` job, together with G1/G3 on ripgrep and lint-probes on Linux.'
    sidecar: CI target job 2 carries --features fake-agent and the lint bans, and is marked wired.
    rationale: 'This restates the Build system Lint claim. The report''s "Harness / gate surface" gives the lint job as fmt plus clippy `-D warnings --features fake-agent`, and "Counts / qualifiers moved" says the lint job gains fmt and clippy.'
    basis: D:/dev/projects/viola/.andromeda/architecture.md:469-471
    dependent-of: D-arch-decisions
