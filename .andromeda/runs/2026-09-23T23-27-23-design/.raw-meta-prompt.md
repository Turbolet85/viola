## Output Protocol

You are an iteration agent improving `D:/dev/projects/viola/.andromeda/runs/2026-09-23T23-27-23-design/design-system-plan-draft.md` (Design System — viola). You may cross-reference, read-only, `exploration.md`, `library-shortlist.md`, `quiz-design.md`, `tooling-decisions.md`, `review-feedback-1.md` and `review-feedback-2.md` in the same run directory.

Rules:
1. **Output patches (old → new) plus a changelog.** Do NOT reproduce the full document.
2. **Patch format:**
   ### Patch N: <one-line description>
   **Old:**
   ```
   <exact text copied from the current draft, long enough to be unique>
   ```
   **New:**
   ```
   <replacement text>
   ```
   Use **at most 8 patches per iteration.** Spend them in this priority order:
   1. Internal contradictions between sections, where a value, word or treatment disagrees across Brand Identity / Color Palette / Typography / Motion / Iconography / Surface: web-spa / Surface: cli / Anti-Patterns / Design Decisions Log.
   2. Signature (the readback box) or expression-budget drift (base `0.2`, web-spa `0.3`, cli TTY `0.2`, machine output `0.0`).
   3. Deviations from `exploration.md` or `library-shortlist.md` that are not recorded in the Design Decisions Log.
   4. Downstream-readiness gaps: tokens or states that a downstream skill would have to invent.
   5. Anti-pattern completeness and cross-reference hygiene.
3. **Changelog:** write exactly ONE line for the whole iteration, never one line per patch:
   `[Iteration N] [substantive|cosmetic] <description>`
4. **Prohibited:**
   - Reproducing the full document.
   - Restructuring sections without a stated defect.
   - Labelling cosmetic rewording as substantive.
5. **If you find no issues,** output `No patches` and a `cosmetic` changelog line.

Lane rules. Any patch that breaks one of these is invalid:
- **Stay in the design lane:** design tokens, patterns, visual and motion states, and anti-patterns. Do not change tooling settled by Phase 1 and architecture. That covers Lit 3.3.3 light-DOM `viola-*` elements, hand-written `@layer` CSS in `/assets/app.css`, clap 4.6.7, windows-sys, the hand-written SGR module and the CSP. Do not propose a component library, an animation library, web fonts, a build step, or backend or data-model changes.
- **Do not introduce a ninth hex value.** The palette is closed: #1E2124, #2A2E33, #4A5057, #E6D8AE, #9C9278, #3B3A36, #D97706, #5B8DB8.
- **Phase 4.5 settled several decisions.** The founder approved the colours, expression levels and signature "as drafted", along with the per-OS font stacks from review rounds 1 and 2. Change these only to resolve an internal contradiction, and record the reason in the Design Decisions Log.
- **Do not write content that other specialists own:**
  - no test cases (you may define visual or motion scope that tests derive from);
  - no observability instrumentation content (you may define visual error states);
  - no new accessibility-attribute rules or standards-conformance claims (you may state contrast values, the motion budget and focus-ring colour);
  - no security-plan content.

## Analysis Dimensions

### 1. Brand Identity Coherence [priority: high]
- **Does "the readback box" show up as concrete tokens in at least 3 sections, with the same state vocabulary everywhere?** Check Brand Identity, Color Palette (the `Readback::*` rows), Motion ("The readback close"), Iconography (the `linear-gradient` strike), Surface: web-spa (`--rb-size: 16px`, `--rb-rule`, `--rb-fill`, `--rb-ink`, `--rb-strike`, the `data-rb` table) and Surface: cli (`[RB]` / `[  ]` / `[/ ]`). Specific checks:
  - Brand Identity says "three drawn states" plus a fourth printed word, `unconfirmable`. `data-rb` has four values (`open` / `read` / `refused` / `unconfirmable`), while `exploration.md` names `data-rb="open|read|refused"`. Is the count stated the same way everywhere?
  - `unconfirmable` reuses the `open` drawing, so the two are visually identical. Is that trade-off stated once, in one place, and does the Squint Test still work?
- **Does the personality ("Tower strip-bay handoff, read back … nothing glows unless the lamp is on it") carry through the rationales?** Look at the Color Palette rationale, the Typography rationale (DIN / Bahnschrift as strip-form lettering), the Depth Strategy ("shadows would read as glow"), the Border Radius personality and the Motion easing ("a hand pushes the strip out"). Does the Iconography section tie its text-glyph-only rule to the strip-bay or phraseology metaphor, or does it justify glyphs only by CSP constraints?
- **Does the expression table in Brand Identity agree with the Motion section's own text?** The table says web-spa `0.3` gets exactly two motions (cock 12px / 160ms ease-out, fade ≤120ms). Line 38 says "A 0.2 project gets `transition: none` or `150ms ease-out` at most". Is it clear that the 160ms cock is licensed only by the web surface's 0.3 and never leaks into base-0.2 contexts?

### 2. Domain Anchoring (Exploration → Plan) [priority: high]
- **Are the Semantic Colors rows internally consistent with the Domain status table and with the Color World?** Specifically, Semantic "Warning" lists `unconfirmable` with border #9C9278, but `Readback::unconfirmable` gives border #E6D8AE. The same comparison applies to `unverified-cli` (the `CliVerified::false` row) and `Window::expired`. Decide which is right, based on the Border Progression rule that "Emphasis #E6D8AE = borders whose presence is the state". Then patch the other.
- **Is every Border Progression and Surface Scale entry tied to a named Color World material?** Check:
  - The 2px departure-blue left tick on `<viola-transfer>` appears in Component Patterns but not in Border Progression.
  - Inset and Base share #1E2124, and Raised-2 and Raised-3 are #2A2E33 plus rules.
  - The Border Progression Deviation note changes the in-strip field grid from strip buff (Color World) to lamp-off buff. Is it also recorded in the Design Decisions Log? (It is listed under "Customisations"; confirm the wording matches.)
- **Do the domain anchors in Brand Identity cover all of `exploration.md` Domain Concepts?** The anchors should be flight-progress strip, strip bay / racks, cocked strip, readback, handoff / tower voice recorder, plus ATIS and fuel state as used by `<viola-atis>`. Is any concept used in the Surface sections (ATIS age, "coasting track", "minimum fuel") missing from the Brand Identity anchor list, or anchored only inline?

### 3. Library-Shortlist Faithfulness (Shortlist ↔ Plan) [priority: high]
- **Are the palette deviations from the shortlist logged?** The Color Palette rationale cites Developer Tool #81 (run-green accent dropped, slate warmed), Coding-Challenge #151 (amber) and Transportation #96 (route blue lightened from #2563EB to #5B8DB8). Are these recorded in the Design Decisions Log, or only in the rationale paragraph?
- **Is the typography translation logged?** Typography says the foundation is "library pairing #1 (Fira Code + Fira Sans)" translated to Bahnschrift + Cascadia Mono. The shortlist also records a fallback (pairing #3, mono for the name cell) "if Bahnschrift is too condensed or too signage for the name cell". Given that the Callsign role uses Bahnschrift at normal width (not semi-condensed), is that choice, and the rejection of the pairing-#3 fallback, recorded?
- **Does the plan name the shortlist direction and apply its UX guidelines?**
  - The Design direction paragraph should name "Precision & Density" (primary) and "Utility & Function" (secondary), plus the Minimalism & Swiss Style (#1) and E-Ink / Paper (#56) presets and the rejected presets (OLED #7, Real-Time Monitoring #31, HUD / Sci-Fi #51). At the moment the paragraph reads as prose with no shortlist reference.
  - Are at least 7 of the 10 shortlist UX guidelines visibly applied or cross-referenced: Color Only #37, Color Contrast #36, Reduced Motion #9, Excessive Motion #7, Content Jumping #19, Error Feedback #33, Errors announced #44, Loading Indicators #78 (overridden), Horizontal Scroll #69, Focus States #28? Guideline #78 is overridden. Is the override cited by guideline number in the web-spa Per-Surface Ban on loading?

### 4. Anti-Pattern Relevance [priority: high]
- **Does the Universal Bans font entry agree with the Typography per-OS stacks?** Both stacks end in the generic `sans-serif` / `monospace`. On a machine missing "DIN Alternate", "Avenir Next Condensed" and "DejaVu Sans Condensed", the generic `sans-serif` could resolve to Arial or Helvetica, both banned. Does Anti-Patterns state, as the Typography section does, that a resolved banned face is a failure and the generic families are last-resort only? Or do the two sections contradict each other?
- **Are all 8 rejected defaults from `exploration.md` "Defaults to Reject" present with `— rejected because:`-style reasoning?** The 8 are: traffic-light dots, the optimistic "Sending…" / "Sent ✓" pattern, the node-graph canvas, auto-sorting, the chat-transcript feed, budget rings or bars, robot/person icons with a toggle, and skeleton shimmer / neon "hacker" theme. Does the web-spa Per-Surface list also carry the shortlist's industry anti-patterns: no command palette (#81 "Key Effects"), and the Real-Time Monitor effect set (pulse, blinking dots, toasts, auto-refresh indicators)?
- **Do other sections re-state bans inline, where they could drift from Anti-Patterns?** Check Motion "Hard limits" against the web-spa ban "NEVER animate `busy`, `stale`, `turn-ended`…", Iconography "no robot or person icons, no status dots, no checkmarks" against the Rejected Defaults, and the Border Radius note about `radius-full` against the "NEVER use a radius other than 0" ban. Where the lists differ (for example, Motion bans "hover colour transitions" and "3D transforms" but the web-spa ban list does not), make one section canonical and have the others cross-reference it.

### 5. Downstream Readiness [priority: high]
- **Can `tests` derive render-assertion scope from the plan alone, without inventing thresholds?**
  - Font rendering: the resolved-family check on the Linux fallback (DejaVu Sans Condensed + DejaVu Sans Mono).
  - Layout: no horizontal scroll at ≥1024px with `--strip-cols` measured in DejaVu Sans Mono `ch`; callsign not clipped (Bahnschrift 15px in a `minmax(16ch, 32ch)` column sized in `--font-field` ch).
  - The readback box: `RB` at 11px / 600 fitting the 14px interior of a 16px box with 1px rules.
  - Motion budget: exactly two transitions, 160ms and ≤120ms, both 0ms under `prefers-reduced-motion`.
  - The 760–1023px two-line strip: `--strip-h: 32px` is a fixed `block-size`. Does the plan give the wrapped strip's height, or would tests have to guess?
- **Can `obs` and `a11y` derive their inputs without new design tokens?**
  - For `obs`, visual error states: `TapeConnection::connecting|open|closed`, `Problem::*` strips, "sessions: no reading yet", and `skipped` nonzero boxing.
  - For `a11y`: contrast pairs from the contrast-rules table, focus ring #E6D8AE 2px / offset 2px / radius 0, the reduced-motion budget, and the forced-colors mapping (band → `Highlight`, strike → dashed).
  - The plan hard-codes accessibility attributes and roles in Component Patterns and Navigation Pattern. Do not add more. Where they appear, are they framed as visual or announcement intent that a11y derives and owns, not as a design-owned rule?
- **Can `route` and `setup-project` materialise bootstrap items straight from the plan?**
  - For `route`: the `@layer tokens` block as the design-tokens bundle, "no font loading" as the typography step, iconography N/A, and bootstrap order `<viola-session-row>` + `<viola-readback>` first. Does the web-spa Surface state this order explicitly, matching `exploration.md` "Bootstrap phases"?
  - For `setup-project`: rules such as the closed eight-hex token list, the extended banned-font list (Inter, Roboto, Arial, Helvetica, system-ui, Open Sans, Lato, Space Grotesk, `-apple-system`) and an expression-budget rule. Can these be lifted verbatim from Anti-Patterns and the Self-Validation Protocol? Are the Self-Validation tests (Swap, Squint, Signature, Token, Sameness) still generic template text, or are they tied to viola? For example, the Signature Test should name the readback box, and the Token Test should name the eight `--c-*` values.

### 6. Multi-Surface Consistency (web-spa ↔ cli) [trigger: detected surfaces > 1] [priority: medium]
- **Do the CLI samples use the same phraseology and separators as the web?**
  - The web uses `driver · budget-paused`, `skipped 0 · 0 · 0` and the rack label `UNWRAPPED · READ-ONLY`.
  - The `viola list` sample prints `driver - budget-paused`, `skipped 0 0 0` and `-- UNWRAPPED (read-only) --`.
  - Iconography says the CLI separator is "two spaces", and the CLI is ASCII-only.
  - Is the ASCII substitute for `·` defined once, and do all samples follow it?
- **Is the handoff anchor visible on the CLI at all?** The web shows `<viola-transfer>` markers (`→ builder  since …` with a readback box) and the inbound twin `← overseer`. The CLI `viola list` table has no link or transfer indication, and handoff blue is "not used". Is that omission deliberate and recorded, and does Iconography list `←` (U+2190) and its ASCII form alongside `→` / `->`?
- **Do the six fixed columns (NAME · LIVE · STATUS · WHEEL · DIALOG · CLI) and their printed words (`n/a` vs `unknown`, `DIALOG <kind>`, `<version> verified`) match exactly between the web strip and the `viola list` table?** Does the ATIS line (`5H 62 %  7D 41 %  read 4m ago  gate open`) keep the same order and words on both surfaces, including `expired` and `resets …`?

### 7. CLI Surface Completeness [trigger: detected surfaces include cli] [priority: medium]
- **Are the colour fallbacks complete for the terminals the plan targets?**
  - Amber maps to truecolor `38;2;217;119;6`, 256-colour `172` and 16-colour `33`.
  - `stale` uses SGR `2` (dim) at every colour depth. Is it noted that legacy conhost and some terminals ignore SGR 2, so the `stale` word in LIVE is the only cue there? The quiz allows colour only as a second cue, so this is acceptable if stated.
  - Is "reserved 67" for handoff blue justified, when the Per-Surface cli ban says nothing is coloured except `DIALOG` and `stale`?
- **Does the refusal hint list cover every typed reason and detail in the exit-code table?** The hint list covers `human-typing`, `budget-paused`, `unverified-cli`, `not-delivered · turn-running` and `not-delivered · control-character`. The `viola send` sample shows a hint for `no-prompt-submitted`. There are no hints for `input-not-ready`, `unknown-dialog`, `unknown` (exit 14), `instance-unreachable` (exit 21) or `wrapper-fault` (exit 20). Either state which reasons get no hint, or add the missing hint phrasing without quoting upstream text.
- **Could the `viola verify` step counter be confused with the readback mirror?** Its `[03/14]` prefix is also a bracketed left-column token, like `[RB]` / `[  ]` / `[/ ]`. Is the counter set apart well enough by width or position that the readback mirror's bracket vocabulary stays unique? Also check that the Typography cli note ("Bold is used only for the NAME column", "Terminal monospace (user's configured font)") and the Spacing note ("fixed-width terminal grid, two spaces between columns") match the samples.

### 8. Expression–Anti-Pattern Alignment (low expression: 0.2 base / 0.3 web) [trigger: expression level <= 0.3] [priority: medium]
- **Is the tape-line / transfer-marker fade licensed by the expression table?** The Motion duration table's 0.3–0.4 row lists Entrance `none` and Hover `150ms ease-out`. The plan instead uses 0ms hover and an entrance fade (opacity 0 → 1, 120ms linear, `@starting-style`, `data-live` only). Is this deliberate deviation from the table row stated in Motion and logged in the Design Decisions Log? Or does the 0.3 row need a viola-specific annotation so downstream phases don't read "Entrance: none" as a ban on the fade?
- **Is the Hard limits block at least as long and explicit as the active-motion list?** It should name spinners, pulses, blinking, skeleton shimmer, looped animation, stagger, hover colour transitions, parallax, spring physics, 3D transforms, scroll-driven, canvas/WebGL and animation libraries, plus the banned triggers (`busy`, `stale`, `turn-ended`, unconfirmed success). Does every Surface section respect it? Check for accidental motion: the `read 4m ago` age "updated each minute without motion", the tape auto-follow scroll, the `document.title` change, and the `N new lines below` line. Is it stated whether auto-follow uses instant or smooth scrolling?
- **Does the CSS in the web-spa Tokens block match the stated budget?** Check three things:
  - `transition` appears only in `@layer motion`, on `viola-session-row[data-dialog="pending"]` (so the return into line is instant) and on `.tape-line[data-live]`, `viola-transfer[data-live]`.
  - Reduced motion zeroes `--cock-dur` and `--fade-dur` but keeps `translateX(var(--cock-offset))` and the amber band.
  - The cocked + `stale` combination (the cock wins for offset, band and `DIALOG`; other cells stay lamp-off) is expressed by the `states` layer selectors, not left to cascade order by accident.

Read the document, analyze along all dimensions, output patches and changelog.
