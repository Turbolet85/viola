2026-09-24 (2026-09-24-diagnostics-plane, phase P5): not claimed. This chunk creates the per-role home-level sinks and the owner-only per-instance detail files, so content-bearing detail now exists only per instance. The acceptance still needs the home strict-modes check before any diagnostics open and the owner/mode/symlink checks on existing diagnostics files (Home and code-bearing file integrity), plus the architecture amendment naming both roots and every sink (wrap-authored).
