2026-09-24 (2026-09-24-diagnostics-plane, phase P5): not claimed. This chunk builds the closed event vocabulary, obs_event!, the run role's schema-true lines (null as key absence) and the logs detail source with torn-line marking. The acceptance still needs the hook/mcp/ui/cli roles (no producer yet), the hook stderr/exit guarantee, the events source and the --kind filter. It stays pooled for the chunk that lands the last of those.
