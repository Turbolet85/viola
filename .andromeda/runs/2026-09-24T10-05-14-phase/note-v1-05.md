2026-09-24 (2026-09-24-diagnostics-plane, phase P5): not claimed. This chunk commits both line schemas and routes the panic payload to the detail file, leaving one payload-free error line (proven in-process). The acceptance still needs the hook exit-0 path, CI schema conformance of every line (G4), the canary secret scan, and the print and raw-log lint bans. Those belong to Observability gates and the hook chunk.
