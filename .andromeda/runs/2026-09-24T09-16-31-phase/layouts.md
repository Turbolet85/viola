# layouts extract

## No domain coverage
This chunk touches only `deny.toml`, the CI workflow YAML, a scheduled advisory run and the harness gate scripts. It creates or changes no web-spa region or component and no `viola` CLI output structure, so none of the layout-templates sections (web-spa wireframes and components, cli output structures, focus order, breakpoints) applies. The amendment history file `.andromeda/layout-templates-amendments.md` does not exist yet, so there is no history to pull.
