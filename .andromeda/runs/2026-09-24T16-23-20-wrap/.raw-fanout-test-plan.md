proposals (13) — D-tests-framework, all severity warning; saved verbatim-in-substance from the doc-agent return:

1. §9 Pipeline structure (Lint row) [primary] — basis test-plan.md:1428. Replace the rmcp `cargo tree` assertion and the per-crate `--acyclic` / `orphans --package <crate> --deny` with `bash scripts/orphans-check.sh --probe && bash scripts/orphans-check.sh`: `cargo modules orphans -p <pkg> {--lib|--bin <name>} [--features <required-features>] --deny` per lib/bin target from `cargo metadata --no-deps`, 5 targets at HEAD including `viola-fake-agent` with `--features fake-agent`, probe last line `orphans-check probes: 1/1 fired, control clean`. cargo-modules 0.27.0 is installed via `cargo install --locked` in the 3-OS lint job. `--acyclic` becomes an on-demand review. The rmcp `cargo tree` assertion is carried to the "MCP server for drivers" (viola-mcp) chunk.
2. §9 Build failure conditions (Lint errors) [dependent-of D-tests-framework] — basis :1470. Replace "a cargo-modules cycle/orphan, rmcp `client` in the release graph" with an orphan in any lib/bin target (orphans-check not ending `N/N targets clean`, or its probe not ending `1/1 fired, control clean`). The rmcp release-graph condition returns with viola-mcp.
3. §10 Build failure conditions [dependent] — basis :1529. Narrow "cargo-modules failure" to "cargo-modules orphans failure (per lib/bin target)".
4. §3 Bootstrap quality-gate-config-emit [dependent] — basis :797. The gate list becomes: cargo deny (root and fuzz/Cargo.lock), zizmor, cargo-modules orphans (scripts/orphans-check.sh), plus the release job's scripts/release-check.sh.
5. §1 Coverage scope (boundary policy entity) [dependent] — basis :98. "cargo-modules boundary review" becomes: the orphans gate per lib/bin target in CI lint, plus the on-demand `--acyclic` review.
6. §9 Pipeline structure (Release build row) — basis :1438. The ci.yml `release` job: a 3-OS matrix, fail-fast false, contents read, running release-check.sh --probe then release-check.sh (`cargo build --release --locked --bin viola --message-format=json`, judged on its own compiler-artifact executables, verdict `release-check: viola only`). A plain exit-code job, with no upload and no gate step.
7. §3 Bootstrap quality-gate-config-emit [dependent] — basis :799. "`viola-harness gate --require …` as the last step of every job" becomes "every test-suite job"; lint, supply-chain and release are plain exit-code jobs.
8. §9 Pipeline structure (Supply-chain row) — basis :1429. Add the fuzz lockfile audit step (advisories, sources) from the repo root; deny-fuzz.json rides the supply-chain artifact; the nightly advisories job also covers fuzz/Cargo.toml.
9. §9 Build failure conditions (Supply-chain errors) [dependent] — basis :1471. Add a fuzz/Cargo.lock advisory/source finding; the weekly clause covers the root and fuzz manifests.
10. §2 Trigger → layer map (V9 row) [dependent] — basis :486. The plan location lists the Lint stage (sync-crate cargo check, orphans), the Supply-chain stage (including the fuzz lock audit), nightly weekly advisories (both lockfiles) and the Release build job.
11. §3 Bootstrap ci-tool-install (jq bullet) — basis :786. Runner-provided jq is used by G2 (test), orphans-check.sh (lint) and release-check.sh (release), and is never installed; the scripts print tool-missing: jq. The runner-image versions are recorded.
12. §9 tool-install paragraph [dependent] — basis :1441. The same jq-consumer extension.
13. §12 Test Decisions Log — basis :1809. A new entry for this chunk (orphans gate, --acyclic on demand with its measured reason, rmcp CARRY, release job, fuzz lock audit; impact §1, §2 V9, §3, §9, §10).
