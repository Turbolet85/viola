proposals:
  - detector: D-obs-pii
    severity: escalate
    section: "8. PII Scrubbing & Compliance → PII Scrubbing → Integration points, item 6 (Verification), sub-list \"Unscanned uploads, admissible by content\""
    change: "Add a third bullet to the unscanned-uploads inventory, after the nightly fuzz/artifacts bullet: the ci.yml `supply-chain` artifact (`target/supply-chain/deny.json`, `deny-fuzz.json`, `zizmor.json`, where deny-fuzz.json is cargo-deny JSON diagnostics over `fuzz/Cargo.lock`, new this chunk) holds only advisory, source, ban and workflow-audit diagnostics. It contains no absolute path, argv, log content or user input: measured at 0 absolute-path hits in all three files at chunk 2026-09-24-workspace-tree-and-code-graph-planes. The only raw hit was the `s:/` inside `https://`, a false positive. A member that could carry a host path or log content must be scanned before upload, or dropped from the upload."
    sidecar: "obs-plan §8 item 6: supply-chain artifact (deny.json, new deny-fuzz.json, zizmor.json) added to the unscanned-uploads-admissible-by-content inventory, backed by the 0-absolute-path measurement (chunk 2026-09-24-workspace-tree-and-code-graph-planes)."
    rationale: "Report Changes → Schema / config: `target/supply-chain/deny-fuzz.json` is a new CI report file that rides the existing `supply-chain` artifact upload, and no secret-scan step covers it. §8 item 6 lists which unscanned uploads are admissible by content, but it names only the mutants verdicts and nightly `fuzz/artifacts/`. The report finds 0 hits for `supply-chain` in that block. The report's Coverage row ('deny-fuzz.json … PII: 0 absolute paths measured ✓ (admissible by content)') and the Expected amendments row for obs-plan §8 item 6 give the measured basis: deny.json, deny-fuzz.json and zizmor.json hold 0 absolute paths. No raw PII is logged. The drift is that §8's upload-admissibility record omits an unscanned upload member, so the PII baseline no longer describes what CI uploads."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1208-1210; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-workspace-tree-and-code-graph-planes/report.md:30,92-94,101"

# D-obs-instrumentation: no drift. The report changes no Rust symbol (mutants "no-rust-delta"). The only new operations are the CI gate scripts scripts/release-check.sh and scripts/orphans-check.sh, plus the ci.yml release job. The report's Coverage section marks all of them "instrumentation: n/a". They are not product hot paths, and obs-plan:149-152 classes build and CI infrastructure as not-instrumentable.
# D-obs-stack: no drift. The Dependencies bullet says "none" and both lockfiles are unchanged. cargo-modules 0.27.0 is a dev and CI lint tool, not a logging or telemetry library, so the §3 logging stack (tracing + tracing-subscriber 0.3.23) is untouched.
# Duplicate sweep for the D-obs-pii claim, with wording greps "Unscanned", "admissible by content", "mutants-verdict-<os>.json" and "cargo deny check advisories", then a read of §9 and §11. No other site in the doc lists the unscanned uploads. §9 Platform (:1232) only points to §8 item 6 for fuzz/artifacts. The §9 artifact table (:1236-1242) covers telemetry artifacts, not the supply-chain one. §11 CI and PII bans (:1399-1413) make no claim about the unscanned-upload set. No dependent-of proposals.
