proposals (13) — severity warning; saved in substance from the doc-agent return:

1. D-arch-resources · Occupied Resources → Repository (basis architecture.md:376-391). Add gitignored `target/orphans-probes/run-<utc>-<pid>/` (orphans-check --probe throwaway crates), `target/release-check/` (the local gate entry's CARGO_TARGET_DIR; the script never sets it), `target/perf/` (test-plan §10), `target/nextest/ci/junit.xml` (test-plan §9), `e2e-web/test-results/` with `a11y/` and `lint/` (test-plan §9, a11y-plan §9).
2. D-arch-resources · Repository, the target/supply-chain/ bullet (basis :388). It lists deny.json, deny-fuzz.json (fuzz lock advisories+sources, from the Fuzz lockfile audit step) and zizmor.json.
3. D-arch-resources · Project directory structure (basis :424-478). Add scripts/release-check.sh and orphans-check.sh; tests/cmd/*.toml (carrying the word trycmd) and snapshots/; e2e-web/ (package.json, playwright.config.ts, tsconfig.json noEmit strict, .htmlvalidate.json, eslint.config.js, tests/*.spec.ts, fixtures/a11y.ts, schemas/a11y-row.v1.json, a11y/sc-coverage.json); a11y/sr-pass/ (TEMPLATE.json, <date>-<at>.json); crates/viola-ui/assets/index.html and app.css. The agent notes diag-line.v1.json already appears 3 times elsewhere.
4. D-arch-decisions · Build system, boundary review (basis :405). `cargo modules orphans --deny` gated per lib/bin target in lint via scripts/orphans-check.sh (+ --probe); `--acyclic` stays on demand (0.27.0 reports every type<->inherent-method pair as a cycle whatever the filters).
5. [dependent] · Stack Code quality row (basis :36). cargo-modules 0.27.0 installed by `cargo install --locked`, orphans gate in lint, `--acyclic` on demand; jq also consumed by the two scripts.
6. [dependent] · CI/CD Setup steps (basis :483). The cargo-modules install route (no taiki-e manifest); jq also required by release-check.sh and orphans-check.sh.
7. D-arch-decisions · Build system, a code-graph planes bullet (basis :397-407). Rust plane; TS plane e2e-web/ only via a tracked e2e-web/tsconfig.json (noEmit, strict); viola-ui never under a tsconfig (CI guard pathspecs); rmcp cargo-tree assertion carried to viola-mcp.
8. D-arch-decisions · Build system, dependency policy (basis :404). "owed to" → the audit is wired (ci.yml supply-chain Fuzz lockfile audit from the repo root → deny-fuzz.json; nightly advisories over fuzz/Cargo.toml; the root deny.toml resolved by walking up).
9. [dependent] · CI/CD nightly sentence (basis :481). advisories covers the root and fuzz lockfiles.
10. [dependent] · tree comment nightly.yml (basis :475). root + fuzz lockfile advisories.
11. D-arch-decisions · CI/CD jobs wired (basis :484). 7 → 8; lint gains cargo-modules install + orphans; supply-chain gains the fuzz audit; release job details; check-runs 12 → 15.
12. [dependent] · CI/CD target job 6 (basis :491). `cargo build --release --locked --bin viola` via scripts/release-check.sh in the per-OS release job; judged on its own compiler-artifact records, never target/release/.
13. [dependent] · tree comment ci.yml (basis :474). Adds module orphans, the 3-OS release job and the fuzz lockfile audit.
