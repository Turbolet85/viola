proposals:
  - detector: D-arch-decisions
    severity: warning
    section: Established Decisions
    change: "[Error Handling] should say anyhow 1.0.104 is used only in the `viola` bin's `main`, its dispatch (`cmd::dispatch` → `cmd::Failure`) and the catch-site reporter `obs::report_internal_error`. The reporter writes the dispatch error's `chain()` to the owner-only instance `diagnostics/detail-<role>.ndjson`, never to a role line or to stdout/stderr. The rationale should also cover chains kept for the owner's post-mortem, not only chains shown to a human on the terminal."
    sidecar: "arch §Established Decisions [Error Handling]: anyhow's allowed scope widened from main + dispatch to include the obs catch-site reporter (report_internal_error → instance detail file), per chunk 2026-09-24-log-redaction-and-never-log-floor."
    rationale: "The report's Changes, Crates / modules bullet, says `src/obs.rs` gains `anyhow` use. The Symbols / APIs bullet adds `obs::report_internal_error(&anyhow::Error, Option<&DetailSink>)` and `cmd::Failure { error: anyhow::Error, … }`, and says the catch site writes the chain to `instances/<name>/diagnostics/detail-run.ndjson` with no terminal output. The locked decision limits anyhow to 'the `viola` bin's `main` and dispatch', so anyhow in the obs module goes beyond it. It is still inside the root bin crate, and no new library was added. The Dependencies bullet says none were added, and veil was not added."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:95; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-log-redaction-and-never-log-floor/report.md:24,12,19"
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies
    change: "In the Error types row, the Role cell should say 'Typed errors that callers can branch on; context chains built only at dispatch and rendered only by the catch-site obs reporter into the instance detail file', instead of 'context chains only at dispatch'."
    sidecar: "arch §Stack Error types row: context chains are now rendered by obs::report_internal_error into the instance detail file, not held only at dispatch."
    rationale: "The Stack row repeats the same confinement claim ('anyhow … (bin edge only)'; 'context chains only at dispatch'). The report says the chain now leaves dispatch in `cmd::Failure` and is rendered by `obs::chain_detail_line` / `report_internal_error` as `chain: [string]` in the detail file (report Changes, Symbols / APIs). 'Bin edge only' still holds, because src/obs.rs is in the root bin crate. The 'only at dispatch' wording would outlive a change applied only to Established Decisions."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:27; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-log-redaction-and-never-log-floor/report.md:13-16,24"
    dependent-of: D-arch-decisions
