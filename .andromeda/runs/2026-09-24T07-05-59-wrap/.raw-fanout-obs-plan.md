proposals:
  - detector: D-obs-stack
    severity: warning
    section: "## 3. Observability Harness Contract > ### Bootstrap phases > obs-ci-gate-wire"
    change: "Only viola-harness (crates/viola-e2e) omits [lints] workspace = true with its own [lints.clippy]; the fake agent is a [[bin]] of the root package, inherits the root [lints], and is exempted by a crate-level #![allow(clippy::print_stdout, clippy::print_stderr)]; the member-list assertion names only viola-e2e."
    sidecar: "obs-plan §3 obs-ci-gate-wire: fake agent exempted by a bin-level #![allow]; member-list assertion names only viola-e2e."
    rationale: "Report Changes > Files (src/bin/viola-fake-agent.rs, root package bin) + Deviations 7."
    basis: "obs-plan.md:790"
  - detector: D-obs-stack
    severity: warning
    section: "## 11. Obs Anti-Patterns > ### Logs"
    change: "print!/println! carve-out names viola-harness (own [lints.clippy]) and the fake agent (bin-level #![allow])."
    sidecar: "obs-plan §11 Logs: carve-out lists the fake agent's bin-level #![allow]."
    rationale: "same retired claim as the primary."
    basis: "obs-plan.md:1373"
    dependent-of: D-obs-stack
# Commentary (not proposals): libraries match §3 exactly; raw tracing macros vs obs_event! (Deviation 8), no duration_ms on process-exit self, no run.start span, no VIOLA_DIR step, diagnostics/ before strict-modes, no detail file — code-side gaps, doc stays target. PII: no drift. obs 1.89 hits (:1565, :1599) are §12 Decisions Log history.
