proposals:
  - detector: D-security-input
    severity: escalate
    section: "## Security Decisions Log"
    change: "Add a dated 2026-09-24 entry recording a known deferral. The walking-skeleton `viola run` accepts `--home <dir>` (and the `<user home>/.viola` default) without `std::fs::canonicalize` and without the strict-modes check. On Windows it also creates a `--home` outside `%USERPROFILE%` without the protected user + SYSTEM DACL. The home-integrity chunk owns both fixes. The skeleton reads no `snapshot.json`, `statusline_command` or ledger, so no trusted read happens before them yet. The §Input Validation CLI row and the §Anti-Patterns Data Protection ban stay unchanged as the target, and the gap must close before the first chunk that reads a snapshot, statusline_command or the ledger."
    sidecar: "security-plan Decisions Log: record the interim --home deferral (no canonicalisation or strict-modes; Windows protected DACL deferred), owned by the home-integrity chunk."
    rationale: "The report's Coverage of new surfaces marks `viola --home` path → validation ✗ ('not canonicalised or strict-modes-checked; owned by the home-integrity chunk'), and Deviation 9 defers the Windows protected DACL for `--home`. §Input Validation (CLI arguments row) requires `--home` to be canonicalised and then pass the strict-modes check. The Anti-Patterns Data Protection ban says never to accept a Windows `--home` outside %USERPROFILE% without the explicit DACL. This new external-input boundary ships without the required validation, and the doc has no record that it is deferred. The other new input surfaces are fine: the `viola run` name goes through `ViolaName::try_new` via the clap value parser, and the harness validates the session-id charset and uses `ViolaName` instances."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:229; D:/dev/projects/viola/.andromeda/security-plan.md:498; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-three-os-ci-headless-harness-skeleton/report.md:130; report.md:148"
  - detector: D-security-auth
    severity: warning
    section: "## Security Decisions Log"
    change: "Add a dated 2026-09-24 entry recording a known deferral. The walking-skeleton `viola run` spawns its child with the full inherited environment: `Command::new(program).args(args).spawn()`, with no `env_remove` or `env_clear`. The R8 `CLAUDE*` strip that §Secret Management requires is therefore not applied yet (`CLAUDE_CODE_MESSAGING_TOKEN`, `CLAUDE_CODE_MESSAGING_SOCKET` and the other `CLAUDE*` identity variables). Name the chunk that lands the R8 strip list as the owner. Until then the skeleton must not wrap a live `claude` session inside a Claude Code parent. §Secret Management Storage stays unchanged as the target."
    sidecar: "security-plan Decisions Log: record that the skeleton `viola run` does not strip R8 CLAUDE* credentials from the child's environment yet (deferred)."
    rationale: "Report Symbols/APIs, `viola run`: 'it spawns the child directly (no shell, stdio inherited, no env added)'. No strip list is listed, and the only CLAUDE* check is on log lines ('no `CLAUDE*` or argv in lines'). §Secret Management says inherited credentials 'are removed from the child by the R8 strip list'. This chunk's secret handling for the real `viola run` verb does not match that. Severity is warning per the detector."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:411; D:/dev/projects/viola/viola-0.1.0/chunks/2026-09-24-three-os-ci-headless-harness-skeleton/report.md:24; D:/dev/projects/viola/src/run/mod.rs:153"
  - detector: D-security-deps
    severity: escalate
    section: "## Dependency Security (CI integration)"
    change: "Replace the pinned-actions list with exactly what ci.yml uses: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1`, `Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2`, `taiki-e/install-action@7623a79cdfecb99d681017af368ca353d9f49bb5 # v2.87.19` and `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1`. The toolchain comes from `rust-toolchain.toml` (1.98.1) through a `rustup toolchain install` step, with no toolchain action. `dtolnay/rust-toolchain` is no longer used."
    sidecar: "security-plan Dependency Security: pinned-action list now checkout / rust-cache / taiki-e install-action / upload-artifact (SHA-pinned); dtolnay/rust-toolchain replaced by a rustup step reading rust-toolchain.toml."
    rationale: "The report's Harness/gate surface CI bullet lists the four actions SHA-pinned plus 'rustup toolchain install, no toolchain action'. Expected amendment 2 is carried and counts 2 `dtolnay/rust-toolchain` sites in security-plan. Three of the new third-party actions (checkout, taiki-e/install-action, upload-artifact) are not vetted anywhere in §Dependency Security, and the one listed toolchain action is gone. The new crates (anyhow, chrono, clap, nutype, serde, serde_json+preserve_order, sysinfo, tempfile, thiserror, tracing, tracing-subscriber 0.3.23) break no ban: no C-building crate, no tokio, crates.io only, tracing-subscriber at or above 0.3.20, and no serde_json `unbounded_depth`. They need no change."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:329-331; D:/dev/projects/viola/.github/workflows/ci.yml:22,26,27,28,75"
  - detector: D-security-deps
    severity: escalate
    section: "## Dependency Security (Pinning)"
    change: "Replace 'The host toolchain moves to Rust `>=1.96` to match CI's `@stable` 1.98.x.' with: 'The toolchain is pinned exactly by `rust-toolchain.toml` (`channel = \"1.98.1\"`, rustfmt + clippy), and host and CI both install it through rustup. The workspace `rust-version` floor is 1.96.'"
    sidecar: "security-plan Dependency Security Pinning: the toolchain is the exact rust-toolchain.toml 1.98.1 pin, not CI @stable; rust-version 1.96."
    rationale: "This restates the retired claim that CI tracks `@stable` via a toolchain action. The report's Schema/config says `rust-toolchain.toml` sets `channel = \"1.98.1\"` and workspace `rust-version = \"1.96\"`, and the CI bullet says the toolchain comes from a `rustup toolchain install` step."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:320"
    dependent-of: D-security-deps
  - detector: D-security-deps
    severity: escalate
    section: "## Threat Model Summary (Attack surface: Supply chain)"
    change: "Replace 'Third-party GitHub Actions: `dtolnay/rust-toolchain@stable` and `Swatinem/rust-cache@v2.9.2`.' with 'Third-party GitHub Actions (SHA-pinned): `actions/checkout` v7.0.1, `Swatinem/rust-cache` v2.9.2, `taiki-e/install-action` v2.87.19, `actions/upload-artifact` v7.0.1. The toolchain comes from `rust-toolchain.toml` through rustup, with no toolchain action.'"
    sidecar: "security-plan Threat Model supply-chain entry: the action list now matches ci.yml; dtolnay/rust-toolchain@stable removed."
    rationale: "This is the second `dtolnay/rust-toolchain` site counted by the report's expected amendment 2 (security-plan=2), and it restates the retired action list. This section is marked as a verbatim copy of threat-assessment.md, so the orchestrator should apply the same edit there or accept the divergence."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:131"
    dependent-of: D-security-deps
