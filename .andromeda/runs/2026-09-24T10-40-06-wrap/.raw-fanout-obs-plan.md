proposals:
  - detector: D-obs-pii
    severity: escalate
    section: "§8 PII Scrubbing & Compliance → Default-deny posture"
    change: "State that G4 validates against `schemas/diag-line.v1.json`, which enforces default-deny with a top-level `unevaluatedProperties: false` (not per-event `additionalProperties: false`) …"
    sidecar: "§8 Default-deny posture: diag-line.v1.json keyword corrected from per-event `additionalProperties: false` to top-level `unevaluatedProperties: false`."
    rationale: "Report Spec claims disproved #1; tests/contract_diag_schema.rs green."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\obs-plan.md:1201"
  - detector: D-obs-pii
    severity: escalate
    section: "§8 PII Scrubbing & Compliance → Default-deny posture → Detail-file scope"
    change: "`detail-<process>.ndjson` lines are validated by the sibling `schemas/diag-detail.v1.json` with a top-level `unevaluatedProperties: false` …"
    sidecar: "§8 Detail-file scope: diag-detail.v1.json default-deny keyword corrected to `unevaluatedProperties: false`."
    rationale: "Same retired claim, obs-plan.md:1202."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\obs-plan.md:1202"
    dependent-of: D-obs-pii
  - detector: D-obs-stack
    severity: warning
    section: "§3 Observability Harness Contract → Logging stack"
    change: "The macro always attaches `event`, `process` and `instance` …; `corr` is not attached by the macro — it is an ordinary typed `key = value` field the caller passes on events that carry one (no special macro arm: ambiguous with the generic `key = value` arm in `macro_rules`) …"
    sidecar: "§3 Logging stack: `obs_event!` attaches event/process/instance only; `corr` is a caller-supplied typed field."
    rationale: "Report Symbols bullet + Reverted/negative API facts."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\obs-plan.md:605"
  - detector: D-obs-stack
    severity: warning
    section: "§11 Obs Anti-Patterns (NEVER do these)"
    change: "Keep the raw-tracing ban; its justification becomes: only `obs_event!` guarantees `event`, `process` and `instance`, with `corr` set explicitly by the caller."
    sidecar: "§11 Anti-Patterns: the raw-tracing ban no longer claims `obs_event!` guarantees `corr`."
    rationale: "Restatement of the retired §3 claim."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\obs-plan.md:1366"
    dependent-of: D-obs-stack
