proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Filesystem (`config.json` row)
    change: "`config.json`: user settings (budget thresholds, GUI port, `diagnostics_level` `\"info\"` default | `\"debug\"`); the read is `viola_core::MAX_FRAME`-capped (16 MiB), requires `v == 1`, reports unknown keys as `parse-rejected{parser:\"config-json\",detail:\"unknown-keys\",count}` at WARN and a malformed/unreadable file as `detail:\"malformed\"`/`\"unreadable\"`; absent file → `info`; never read from an env var."
    sidecar: "arch §Occupied Resources/Filesystem: registered the `config.json` key `diagnostics_level` (info|debug) and its capped, tolerant read (chunk 2026-09-24-diagnostics-plane)."
    rationale: "Report Changes → Schema/config: 'New `config.json` key `diagnostics_level` …'. The registry row still lists only budget thresholds and GUI port. Expected amendments: carried."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:365"
  - detector: D-arch-resources
    severity: warning
    section: Cross-cutting Patterns → Config management (Settings bullet)
    change: "Settings: budget thresholds as `Percent` (defaults `five_hour` 90, `seven_day` 85), the GUI port (default 47319) and `diagnostics_level` (`info` default | `debug`; the only source of the process-log level — `RUST_LOG` is inert)."
    sidecar: "arch §Cross-cutting Patterns/Config management: Settings list now names `diagnostics_level`."
    rationale: "Duplicate occurrence of the config.json settings enumeration."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:483"
    dependent-of: D-arch-resources
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Filesystem (home-level `diagnostics/` row)
    change: "`diagnostics/`: home-level per-role process logs, codes only, one line per `write`, schema `schemas/diag-line.v1.json`: `run-<name>.ndjson`, `hook-<name>.ndjson`, `mcp.ndjson`, `ui-<port>.ndjson`, `cli-<name>.ndjson` (naming implemented for all five roles; only `run` has a producer today); dir 0700, files 0600 on Unix; line format owned by obs. Content-bearing detail stays in `instances/<ViolaName>/diagnostics/`."
    sidecar: "arch §Occupied Resources/Filesystem: home `diagnostics/` row lists all five role file names."
    rationale: "Report Counts bullet: role file names now implemented for all 5 roles; arch :369 no longer matches."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:369"
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Filesystem (`instances/<ViolaName>/…` row, `diagnostics/` entry)
    change: "`diagnostics/` holds the owner-only content-bearing detail files `detail-<role>.ndjson` (schema `schemas/diag-detail.v1.json`; dir 0700, files 0600 on Unix; one `write_all` per line); codes-only process lines go to the home-level role files."
    sidecar: "arch §Occupied Resources/Filesystem: registered the instance detail files; removed the 'reserved for hook, wrapper and in-session mcp diagnostics' wording."
    rationale: "Report Symbols/Schema/Harness bullets: write_detail/detail_path, panic → detail-run.ndjson, logs streams detail-*.ndjson."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:370"
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Repository (`schemas/` rows)
    change: "Add `schemas/diag-line.v1.json` and `schemas/diag-detail.v1.json` (pinned by `tests/contract_diag_schema.rs`)."
    sidecar: "arch §Occupied Resources/Repository: registered the two diag schemas."
    rationale: "Report Files + Schema/config bullets; Expected amendments: carried."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:378"
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → Project directory structure (`schemas/` tree comment)
    change: "`schemas/` — JSON schemas: `fake-script.v1.json` (test-side), `diag-line.v1.json` + `diag-detail.v1.json` (diagnostics line contracts)."
    sidecar: "arch §Infrastructure Patterns/tree: `schemas/` comment lists the diag schemas."
    rationale: "Duplicate occurrence of the schemas registry claim."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:435"
    dependent-of: D-arch-resources
  - detector: D-arch-decisions
    severity: warning
    section: Established Decisions → [Hook Contract]
    change: "Its codes-only process log goes to the home-level `diagnostics/hook-<name>.ndjson` and content-bearing detail to the instance's `diagnostics/` detail file (format owned by obs); a failure to write either is ignored."
    sidecar: "arch §Established Decisions [Hook Contract]: hook diagnostics split into home-level role file + instance detail file per obs D-08/D-09."
    rationale: "Report Spec claims disproved #2; Expected amendments: carried."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:67"
  - detector: D-arch-decisions
    severity: warning
    section: Cross-cutting Patterns → Diagnostic output channels
    change: "Each role's codes-only process log goes to its home-level role file; content-bearing detail to the instance's detail file; `mcp` writes only MCP frames to stdout and its diagnostics no longer go to stderr or the instance dir."
    sidecar: "arch §Cross-cutting Patterns/Diagnostic output channels: mcp/hook/ui/cli log to home-level role files; detail to the instance detail file."
    rationale: "Report Spec claims disproved #2 cites architecture.md:492."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:492"
    dependent-of: D-arch-decisions
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies (ORM / migrations row)
    change: "N/A. Every channel `params`, event, snapshot and config record carries `v` … Exception: diagnostics lines carry no `v`; their version lives in the schema filename."
    sidecar: "arch §Stack ORM/migrations row: narrowed 'Every record carries `v`' to name the diagnostics-line exception."
    rationale: "Report Decisions: diag lines carry no `v`; the operator narrowed the same rule at P5 review."
    basis: "D:\\dev\\projects\\viola\\.andromeda\\architecture.md:21"
