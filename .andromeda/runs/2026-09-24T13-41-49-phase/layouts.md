# layouts extract

## No domain coverage
This chunk only covers CI and test-harness gates: lint, MSRV, coverage floors, the fuzz/proptest replay, zero retries, `viola-harness gate`, the zizmor concurrency decision, the `mutants.out/` upload and the `file_mode` mutant tests. It creates or changes nothing on either surface in layout-templates.md. That covers the web-spa bay (§Surface: web-spa) and the `viola` verb output structures (§Surface: cli). `viola-harness` / `agent-run` are developer tooling, not the product CLI surface. The amendment sidecar `layout-templates-amendments.md` does not exist, so the history is empty.
