`Cargo.toml` `[workspace.package]` declares `rust-version = "1.96"`, and `rust-toolchain.toml` pins the single current stable (`channel = "1.98.1"`). The `ci.yml` `msrv` job installs 1.96 with `rustup toolchain install 1.96` (no toolchain action) and runs `cargo check --workspace` and `agent-run run --unit` under `RUSTUP_TOOLCHAIN=1.96`. Its log prints `rustc 1.96`, it ends in `viola-harness gate --require nextest-unit`, and it is green on the chunk's pushed sha. External CLI tools are stated as minimum floors, not exact pins (security-plan.md:321).
