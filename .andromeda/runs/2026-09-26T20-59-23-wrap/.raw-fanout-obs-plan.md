proposals:
  - detector: D-obs-pii
    severity: escalate
    section: "## 8. PII Scrubbing & Compliance → ### PII Scrubbing → Integration points, item 6 (Verification), harness-<os> sub-bullet"
    change: "The scan applies the Critical-class patterns to `target/agent-run/*` except exactly `target/agent-run/chunk.diff` (the mutation leg's diff: repository source text, which hits `?t=` by construction). The `harness-<os>` upload excludes that same file (`!target/agent-run/chunk.diff`), so it is never scanned and never uploaded. A `chunk.diff` anywhere else under the capture is still scanned. The canary exemption for the other `target/agent-run/*` files is unchanged."
    sidecar: "obs-plan §8 item 6: secret-scan scope over target/agent-run/* now skips exactly the mutation leg's chunk.diff, and the harness-<os> upload excludes the same file (chunk 2026-09-26-ci-chunk-base-and-union-verdict)."
    rationale: "Report Changes → Symbols/APIs: `secret_scan::scan` skips exactly `<agent_run>/chunk.diff`, and a `chunk.diff` elsewhere is still scanned (report.md:17). Harness/gate surface: the `harness-<os>` upload `path` is `target/agent-run/` plus `!target/agent-run/chunk.diff` (report.md:26). Spec claims disproved: the scan scope included repository source text by construction (report.md:36). Coverage: the skipped file is never uploaded (report.md:52). No raw PII leaves CI, because the unscanned file is not uploaded. However, §8's stated verification scope ('the scan applies … to `target/agent-run/*`') no longer describes the mechanism that enforces the never-log floor. The report lists this as an expected amendment (report.md:47)."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1206"
  - detector: D-obs-pii
    severity: escalate
    section: "## 9. CI Integration → Telemetry artifact handling (table), `agent-run logs` / harness-<os> row"
    change: "Storage cell: its own artifact `harness-${{ matrix.os }}` (upload-artifact v7.0.1 SHA-pinned, `path: target/agent-run/` plus `!target/agent-run/chunk.diff`, `retention-days: 7`). The mutation leg's `chunk.diff` is excluded because the scan skips it (§8 item 6)."
    sidecar: "obs-plan §9 artifact table: harness-<os> upload path now excludes target/agent-run/chunk.diff, matching the secret-scan skip."
    rationale: "Restates the retired claim, since the harness upload path is the whole of `target/agent-run/`. Report.md:26 says the CI `test` job's `harness-<os>` upload `path` is `target/agent-run/` plus `!target/agent-run/chunk.diff`."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1240"
    dependent-of: D-obs-pii
  - detector: D-obs-pii
    severity: escalate
    section: "## 9. CI Integration → Step order and conditions, step 3 (Secret scan)"
    change: "Secret scan, `id: secret-scan`, `if: always()`, over `target/e2e-home/**/diagnostics/*.ndjson` and `target/agent-run/*` except the mutation leg's `target/agent-run/chunk.diff`, which is skipped by exact path and excluded from the step-4 `harness-<os>` upload. It also covers `target/nextest/ci/junit.xml` (rest unchanged)."
    sidecar: "obs-plan §9 step 3: secret-scan scope over target/agent-run/* minus the mutation leg's chunk.diff (excluded from the upload too)."
    rationale: "Restates the retired full-scope claim. Report.md:17 (`secret_scan::scan` skips exactly `<agent_run>/chunk.diff`), report.md:26 (upload exclusion). Report.md:47 names 'Step order step 3' as an expected obs-plan amendment."
    basis: "D:/dev/projects/viola/.andromeda/obs-plan.md:1285"
    dependent-of: D-obs-pii
# D-obs-instrumentation: no drift. The new symbols (resolve_base / chunk_flip / pre_ci_parent / pending_marker / commit_sha, gate(root), union, cfg_legs::compiled_legs, secret_scan::scan skip) are all in the test-only viola-e2e harness. The report says "no product crate touched" (report.md:18) and marks instrumentation n/a for every new surface (report.md:50-52). §1 puts Build/CI infra outside instrumentation scope.
# D-obs-stack: no drift. The Dependencies bullet adds only syn =2.0.119 and proc-macro2 =1.0.107, consumed by viola-e2e (report.md:19). Neither is a logging or telemetry library, and the tracing / tracing-subscriber stack in §3 is untouched.
# Out of scope for these three detectors (not proposed): the report also expects an obs-plan §9 Pipeline-integration "Mutation" row amendment (obs-plan.md:1253, "a mutant is red only when no leg caught it"). Under the new union a mutant is judged only by the legs that compile its line (cfg_legs::compiled_legs), falling back to every named leg when none does, and is red iff no judged leg caught it and some judged leg reads missed or timeout (report.md:15, :47). That is a CI-gate claim, not instrumentation, stack or PII, so the orchestrator should route it through whichever detector owns gate semantics.
# §12 Decisions Log D-27 (obs-plan.md:1669, "scan … covers the agent-run capture in target/agent-run/") is history/baseline and is intentionally not amended.
