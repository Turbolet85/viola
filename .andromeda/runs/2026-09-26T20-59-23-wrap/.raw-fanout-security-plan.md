proposals:
  - detector: D-security-auth
    severity: warning
    section: "Bootstrap phases (derive for route / setup-project) → secret-scanning-ci-gate"
    change: "The canary scan runs over test-home diagnostics, the harness capture except the mutation leg's `<agent_run>/chunk.diff`, and the nextest JUnit report. `chunk.diff` is skipped by exact path because it holds repository source text, which trips `?t=` by construction; a `chunk.diff` anywhere else under the capture is still scanned. The `harness-<os>` upload drops the same file (`!target/agent-run/chunk.diff`), so the skipped file is never uploaded. The rest stays as is: every scan-gated upload (`diag-`, `junit-`, `harness-<os>`) waits for the scan, and exactly two uploads leave CI unscanned."
    sidecar: "security-plan Bootstrap secret-scanning-ci-gate: scan scope and harness-<os> upload now exclude the mutation leg's chunk.diff (chunk 2026-09-26-ci-chunk-base-and-union-verdict)."
    rationale: "Report Changes, Symbols/APIs: `secret_scan::scan` now skips exactly `<agent_run>/chunk.diff`. Harness/gate surface: the `harness-<os>` upload path is `target/agent-run/` plus `!target/agent-run/chunk.diff`. Spec claims disproved: 'the scan covers every file under target/agent-run/' is retired, and the scope is narrowed on both the scan side and the upload side. The doc still says the scan runs over 'the harness capture' as a whole and that `harness-<os>` is gated with no exclusion. The count of unscanned uploads stays at two, because chunk.diff is never uploaded."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:386"
  - detector: D-security-auth
    severity: warning
    section: "Secret Management → Secret scanning in CI"
    change: "After the class list, add a scope sentence: the scan reads every file under `target/agent-run/` except the mutation leg's `<agent_run>/chunk.diff`, which is skipped by exact path and excluded from the `harness-<os>` upload, so it is never uploaded. Keep: it never prints or writes the matched bytes, and the test-home uploads run only when it succeeds."
    sidecar: "security-plan §Secret Management 'Secret scanning in CI': scope sentence added (target/agent-run/ minus the mutation leg's chunk.diff, which is never uploaded)."
    rationale: "The report's Expected amendments name this exact site: 'security-plan §Secret Management \"Secret scanning in CI\": scope sentence where it names target/agent-run/'. Here the scan is described with no scope, so the retired whole-capture scope is only implied; this restates the narrowed scope from Symbols/APIs `secret_scan::scan` and Harness/gate surface `harness-<os>`."
    basis: "D:/dev/projects/viola/.andromeda/security-plan.md:430-438"
    dependent-of: D-security-auth
# Detector results:
# - D-security-deps: no drift. The Dependencies bullet adds syn "=2.0.119" and proc-macro2 "=1.0.107" as exact
#   [workspace.dependencies] pins (the §Dependency Security Pinning rule). Both are MIT OR Apache-2.0, inside the
#   allow list. Neither builds C code, is a telemetry crate or is a banned feature. `cargo deny check` is four-green.
#   Only the test-only crate viola-e2e uses them.
# - D-security-input: no drift. This chunk adds no product external-input boundary (no product crate changed, per the
#   Crates/modules bullet). The only new parser is the harness-side `cfg_legs::compiled_legs`. Coverage says it accepts
#   Normal path components only and keeps every leg on any input it cannot read. The derived base resolves to a
#   commit sha or None.
# - A stale example the three invariants do not cover. Not proposed; flagged for the orchestrator to route:
#   security-plan.md:340 (§Dependency Security → CI integration) says "A value from the event payload reaches a
#   step only through `env:` (e.g. `AGENT_RUN_CHUNK_BASE`)". The report's Harness/gate surface removes the
#   `AGENT_RUN_CHUNK_BASE: ${{ github.event... }}` env line. `ci.yml` `github.event` references go from 1 to 0, and
#   AGENT_RUN_CHUNK_BASE is now "an explicit override of the derived base", no longer set from the event payload.
#   The rule still holds, but the example no longer describes CI. Suggested wording: "ci.yml reads no
#   `github.event` value; any future event-payload value reaches a step only through `env:`, never through
#   `${{ }}` inside `run:`."
