proposals:
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Environment variables
    change: "`AGENT_RUN_CHUNK_BASE`: an explicit override of the mutation gate's derived chunk base, read only by `viola-harness` (`run_cmd`). Without it the harness derives the base: the last master-route `· complete ·` flip, bounded at the oldest `operator pre-CI commit`'s parent (`HEAD^` on the wrap push), else `git merge-base HEAD origin/main`. CI no longer sets it."
    sidecar: "Occupied Resources → Environment variables: AGENT_RUN_CHUNK_BASE changes from the base CI passes in to an explicit override of the harness-derived chunk base (chunk 2026-09-26-ci-chunk-base-and-union-verdict)"
    rationale: "Report Harness/gate surface: 'AGENT_RUN_CHUNK_BASE meaning: from \"the base CI sets\" to \"an explicit override of the derived base\"'. Symbols/APIs `resolve_base` gives the new order. The registry entry still calls it 'the mutation gate's diff base', which is the old meaning."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:364"
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: "In the `mutants` sentence, replace '(base = the PR base sha or `github.event.before`, passed through `env:`)' with: each leg runs over the whole chunk, using a base the harness derives itself (the last master-route `· complete ·` flip bounded at the oldest pre-CI commit's parent, `HEAD^` on the wrap push). Only `LEG` passes through `env:`, and the step is named 'Mutation leg (whole chunk)'. The run document carries `\"base\"`."
    sidecar: "CI/CD approach: the mutants legs no longer take the base from github.event through env:; the base is derived per chunk by the harness"
    rationale: "Report Harness/gate surface: the `AGENT_RUN_CHUNK_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}` env line was REMOVED and only `LEG` stays. Counts: `ci.yml` `github.event` refs went 1 → 0. Expected amendments list this site (arch 522). Line 522 still restates the retired CI-supplied base."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:522"
    dependent-of: D-arch-resources
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: "In the concurrency-rationale sentence, drop the claim that a cancelled run 'would drop a push's `--in-diff` mutation diff'. Each mutation run now covers the whole chunk from the derived base, so a cancelled run loses no diff. The remaining reason is that a concurrency group would cancel a pending run's `always()` gate and upload chain."
    sidecar: "CI/CD approach: the no-concurrency rationale no longer cites losing a push's per-push mutation diff"
    rationale: "The rationale depends on the per-push base (`github.event.before`) that the report retires (Harness/gate surface; resolve_base derived base, 'whole-chunk mutation base that survives the operator pass'). Once the base is whole-chunk, a later run covers what a cancelled run would have covered. The report keeps 'no `concurrency:`' as MET, so only the stated reason changes. Lower confidence: the orchestrator should confirm the reading before applying."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:519"
    dependent-of: D-arch-resources
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: "In the `mutants-verdict` sentence, replace 'a mutant is red only when no leg caught it and some leg missed it or timed out' with: `gate` also reads the checked-out source (same sha). Each mutant is judged only by the named legs whose `#[cfg]`s compile its line (`harness::cfg_legs::compiled_legs`, via syn). If no leg compiles it, every named leg judges it. It is red when no judged leg caught it and some judged leg missed it or timed out."
    sidecar: "CI/CD approach: the mutants-verdict union now judges each mutant only by the legs that compile its line"
    rationale: "Report Symbols/APIs `union`: 'a mutant is judged only by the named legs that COMPILE its line … when none does, by every named leg'. `gate(artifacts, root, …)` gains `root`. Harness: the `mutants-verdict` gate 'now reads the checked-out source (same sha) for `#[cfg]`s'. Expected amendments list the union sentence for this section. This is the registered `mutants-verdict` job contract, not a resource registry entry, so its fit with this detector's scope is loose."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:522"
  - detector: D-arch-resources
    severity: warning
    section: Occupied Resources → Repository
    change: "In the `target/agent-run/` entry, add that `target/agent-run/chunk.diff` (the mutation leg's diff) is never read by `secret-scan` (exact-path skip; a `chunk.diff` anywhere else is still scanned) and is never uploaded (`harness-<os>` excludes it)."
    sidecar: "Occupied Resources → Repository: target/agent-run/chunk.diff is excluded from secret-scan and from the harness-<os> upload"
    rationale: "Report Symbols/APIs `secret_scan::scan` skips exactly `<agent_run>/chunk.diff`. Harness: the `harness-<os>` upload `path` = `target/agent-run/` plus `!target/agent-run/chunk.diff`. Expected amendments: 'architecture §Occupied Resources → Repository: target/agent-run/chunk.diff never scanned or uploaded'."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:386"
  - detector: D-arch-resources
    severity: warning
    section: Infrastructure Patterns → CI/CD approach
    change: "In the `test` job's upload list, state that `harness-<os>` uploads `target/agent-run/` except `target/agent-run/chunk.diff`."
    sidecar: "CI/CD approach: the harness-<os> upload excludes target/agent-run/chunk.diff"
    rationale: "Report Harness/gate surface: the CI `test` job's `harness-<os>` upload `path` = `target/agent-run/` plus `!target/agent-run/chunk.diff`. Expected amendments list the `harness-<os>` upload at arch 522. Line 522 lists the upload without its scope."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:522"
    dependent-of: D-arch-resources
  - detector: D-arch-decisions
    severity: warning
    section: Stack and Technologies (Code quality row)
    change: "Register syn `=2.0.119` (default features off; `full`, `parsing`, `printing`, `visit`) and proc-macro2 `=1.0.107` (`span-locations`) as test-only `[workspace.dependencies]` pins used only by `viola-e2e`. They are the mutation gate's `#[cfg]` reader (`harness::cfg_legs`). Licences MIT OR Apache-2.0."
    sidecar: "Stack: syn =2.0.119 and proc-macro2 =1.0.107 added (test-only, viola-e2e, the mutation gate's cfg reader)"
    rationale: "Report Dependencies: '[workspace.dependencies] + syn = \"=2.0.119\" … and proc-macro2 = \"=1.0.107\" … consumed by viola-e2e only'. Neither library appears in §Stack or §Established Decisions. The Code quality row already lists test-harness libraries such as jsonschema 0.57.0 for `schema-check`. No locked decision is contradicted: no C build, allowed licences, deny four-green, no product crate touched."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:37"
  - detector: D-arch-decisions
    severity: warning
    section: Infrastructure Patterns → Crate dependency direction
    change: "Add an entry: `viola-e2e` (test-only; no product crate depends on it) → syn, proc-macro2 (`harness::cfg_legs`, the gate's `#[cfg]` evaluation per leg)."
    sidecar: "Crate dependency direction: viola-e2e → syn, proc-macro2 recorded"
    rationale: "Report Dependencies plus Crates/modules (new module `viola-e2e::harness::cfg_legs`). Expected amendments: 'architecture §Infrastructure Patterns → crate dependencies: viola-e2e + syn / proc-macro2'. The section has no `viola-e2e` entry today."
    basis: "D:/dev/projects/viola/.andromeda/architecture.md:428-437"
    dependent-of: D-arch-decisions
