CONDENSED record of the return, not byte-verbatim (the full YAML is in this wrap's conversation; each proposal's section, basis and change are kept, prose tightened).

(The test-plan doc-agent returned 10 proposals, all detector D-tests-obs-harness, severity warning; proposal 1 primary, 2–10 `dependent-of: D-tests-obs-harness`. Preamble comments: D-tests-coverage no drift — cfg_legs (compiled_legs_* ×5, union_by_compiled_legs_* ×5), derived resolve_base (chunk_base_* ×6 + resolve_base_needs_a_real_commit), secret-scan skip (×2) all unit-tier and mutation-tier covered; D-tests-framework no drift — nextest via `run --unit`, cargo-mutants 27.1.0 `--test-tool=nextest`; D-tests-obs-harness: obs-plan §3 (548–812) states none of the changed claims, so the §3 pair agrees once test-plan §3 is amended; historical §12 entries 1767/1803 left untouched.)

1. §3 → `run` step 4 Mutation → Base (basis test-plan.md:553): derived whole-chunk base — `AGENT_RUN_CHUNK_BASE` (trimmed, non-empty) an explicit override kept for tests; else read `.andromeda/master-route.md` from HEAD's tree, every `· pending ·` marker, the OLDEST commit in HEAD's history carrying `chore({marker}): operator pre-CI commit`, bound = its parent else HEAD; base = `git log -1 -G ' · complete · '` from the bound; `HEAD^` when that commit is HEAD (wrap push); fallback `git merge-base HEAD origin/main`; full sha via `git rev-parse --verify --quiet {rev}^{commit}`, else `base-missing`. CI passes no event value (`mutants` sets only `LEG`, keeps `fetch-depth: 0`). Drop the `/implement`-sets-it sentences, the PR-base / `github.event.before` env line, the force-push note and the `git cat-file -e` wording; keep "never an empty chunk.diff"; add the measured hazard (an unbounded `-G` pickaxe moves on a mid-pass edit of a complete record).
2. §3 → `run` → Output format (`mutants` object) (basis :566): every `--mutants` form carries `"base":"<full sha>"`; leg file unchanged.
3. §3 → `gate` breach list, mutants union bullet (basis :655): compiling-leg union — `gate` reads the checked-out source, `cfg_legs::compiled_legs` (syn) per leg family (`windows-` → windows; `ubuntu-` → unix+linux; `macos-` → unix+macos); a leg dropped only on a cfg proven false; unknowns keep the leg; empty set → every leg; red iff no judged leg `caught` and some judged leg `missed|timeout`; unviable everywhere reported not red; missing leg file `artifact-missing`; `gate` gains the source root, CLI flags unchanged.
4. §3 → `secret-scan` → Scope (basis :628): every file under `target/agent-run/` except exactly `target/agent-run/chunk.diff` (repo source text); a `chunk.diff` elsewhere still scanned; the same file excluded from the `harness-<os>` upload.
5. §3 → `gate` → CI placement paragraph (basis :659): `target/agent-run/` (incl. `artifacts/`, minus `chunk.diff`) leaves CI only in the scan-gated `harness-<os>` artifact, path `target/agent-run/` + `!target/agent-run/chunk.diff`; `mutants-verdict`'s gate reads the checked-out source.
6. §6 → Error sanitization and secret scan → Canary bullet (basis :1265): CI scan over `target/e2e-home/**`, the harness capture in `target/agent-run/` except `chunk.diff` (excluded from scan and upload), and the JUnit report.
7. §9 → stage table → Mutation row (basis :1439): only `LEG` through `env:`, step "Mutation leg (whole chunk)", no `github.event` value, base derived by the harness; `mutants-verdict` compiling-leg union; keep `unviable > caught` red per leg.
8. §9 → Test report format → `harness-${{ matrix.os }}` bullet (basis :1455): `target/agent-run/` minus `chunk.diff`.
9. §10 → Mutation gate (basis :1501): union judged by compiling legs (syn over `#[cfg]`, proven-false drops), empty-set fallback, miss on the compiling leg stays red; replaces "fails a mutant only when no leg caught it and some leg missed it"; keep the cargo-mutants cfg note; witness: run 36165685381 replay `ok:true` where the old union printed exactly the two breaches.
10. §12 → append a 2026-09-26 entry (basis :1767): (1) derived whole-chunk base bounded at the oldest pre-CI commit's parent, wrap push HEAD^, override kept, CI passes no `github.event`, run doc carries `base`; (2) compiling-leg union via gate-side syn, empty-set fallback, miss on compiling leg red; (3) secret-scan skips exactly `target/agent-run/chunk.diff` + upload excludes it; (4) `commit_exists` removed. Rationale: measured `-G` hazard, run 36165685381 replay, the `?t=` residue hit, leg-side annotation declined (would widen the unscanned upload's content class), witness CI run 36270173848 (base fcca1ce both legs, 79/74/0, gate ok). Impact: §3, §6, §9, §10.
