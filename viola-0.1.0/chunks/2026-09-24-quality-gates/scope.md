# Scope — 2026-09-24-quality-gates

**Working entry (`working-route.md:23`, Epoch 1 — Foundation):** Quality gates: fatal fmt/clippy lint, MSRV 1.96 job, per-OS coverage floors, seeded property and fuzz corpus replay, zero retries, per-job gate verdict.

This chunk turns test-plan §10 (Quality Gates & Coverage Targets) and the matching §9 CI rows into mechanical gates: the jobs that are missing from `ci.yml`, the harness verbs they call and one gate verdict per job. The gates are the subject. Product and test changes are only what the gates need to pass honestly. It is the last test-side gate chunk in Epoch 1. Ahead of it is only the workspace-tree / code-graph chunk.

## What it builds

1. **Fatal fmt/clippy lint: already done, not rebuilt** (CARRY, folded from the entry). Chunk `2026-09-24-observability-gates` already wired fatal `cargo fmt --all --check` and `cargo clippy --workspace --all-targets --features fake-agent -- -D warnings` on all three OSes in the `ci.yml` `lint` job. This chunk keeps them fatal and adds the `lint` job's gate verdict (item 6) only if a suite verdict applies to it. [premise-corrected: out of scope. The rmcp `cargo tree -e features` assertion has no subject at HEAD: `crates/` holds only `viola-core` and `viola-e2e`, and `viola-mcp` lands with its first consumer. `cargo modules` is "on demand" boundary review per architecture §Build system, while test-plan §9's Lint row puts it in CI, and the entry names neither. Both rows are recorded forward at wrap, not built here.]
2. **MSRV 1.96 job** (test-plan §9 MSRV row). A single ubuntu job: `cargo check --workspace` + `agent-run run --unit` on 1.96, with its own rust-cache key and `viola-harness gate --require nextest-unit` as its last step. The workspace `rust-version = "1.96"` is already set (`Cargo.toml:70`).
   - [premise-corrected: the toolchain comes from `rustup toolchain install 1.96`, selected with `RUSTUP_TOOLCHAIN=1.96`, not the dtolnay action. architecture §CI/CD and security-plan §Dependency Security forbid a toolchain action, and research M4 measured that `RUSTUP_TOOLCHAIN=1.96` outranks the `rust-toolchain.toml` pin.]
   - Verified at P3: every locked dependency builds on 1.96 and HEAD uses no newer API. `cargo +1.96 check --workspace --all-targets --features viola/fake-agent` exits 0 with no warnings (rustc 1.96.1, research M3). The unit leg under 1.96 passes 158/158 (M4).
3. **Per-OS coverage floors** (test-plan §10 table + stack adjustments, §3 `--coverage`):
   - `agent-run run --coverage` (cargo-llvm-cov 0.9.1 under nextest, `--profile ci`, LCOV to `target/lcov.info` + `cargo llvm-cov report --json --summary-only` to `artifacts/llvm-cov-summary.json`), with `--ignore-filename-regex '(viola-fake-agent|crates/viola-e2e|tests/support|fuzz/)'`.
   - In the per-OS `test` job, one instrumented nextest run replaces the separate unit / integration / e2e nextest runs, and the doctest run follows it.
   - Enforced floors per OS: `--fail-under-lines 85 --fail-under-functions 95 --fail-under-regions 80`.
   - cargo-llvm-cov 0.9.1 is added to the taiki-e install line.
   - `LLVM_PROFILE_FILE` must survive every test that calls `env_clear()`.
   - Verified at P3: the run does not exist at HEAD. `run.rs` `Selection` has only unit/integration/mutants, and `crates/viola-e2e/tests/cli.rs:61` pins `run --coverage` as usage.
   - Floors, partly verified at P3. The Windows leg is measured at 98.21 % lines, **96.55 % functions** and 98.34 % regions (product files only, research M5), so it passes with a thin function margin. The Linux and macOS legs are unmeasured, and their witness is the CI `test` job on this chunk's push. A shortfall is closed with tests in this chunk, never by lowering a floor.
   - [premise-corrected: the test-plan §3 ignore regex `(viola-fake-agent|crates/viola-e2e|tests/support|fuzz/)` does not match Windows paths. llvm-cov reports `crates\viola-e2e\…`, and the regex returned `False` on it (research M5), so on Windows the exclusion silently fails. The regex takes a separator-agnostic spelling of the SAME four exclusions (`[/\\]`). That is a correction, not a widening.]
   - [premise-corrected: under `cargo llvm-cov nextest`, JUnit stays at `target/nextest/ci/junit.xml`, not `target/llvm-cov-target/…` as test-plan §3 `gate` Inputs state (research M6). The secret-scan root and the `junit-<os>` upload need no change.]
4. **Seeded property and fuzz corpus replay** (test-plan §4 proptest, §6 Property suite, §7, §9 Fuzz replay row):
   - proptest 1.11.0 properties at `cases: 512`, with committed `proptest-regressions/` seeds (at HEAD only `crates/viola-core/proptest-regressions/lib.txt` exists);
   - a `fuzz/` cargo-fuzz 0.13.2 workspace (nightly toolchain, `arbitrary` 1.4.2 shared inputs) with `fuzz/fuzz_targets/<parser>.rs`, and committed `fuzz/corpus/<target>/` seeded from the §7 recorded payloads each parser accepts;
   - `agent-run run --fuzz-replay` (`cargo +nightly fuzz run <target> -- -runs=0`, ubuntu only, `tool-missing` exit 1 when cargo-fuzz is absent), plus a ubuntu CI job running it with `gate --require fuzz-replay`;
   - the `nightly.yml` time-boxed run, `-max_total_time=120` per target.
   - [premise-corrected: NONE of test-plan §6's seven parser surfaces exists at HEAD, where `viola-core` holds only `ViolaName` + `MAX_FRAME` (research, Open questions). Owners on the route: vt100 `working-route.md:45`, `agents --json` `:66`, `Last-Event-ID` `:100`, channel framing `:28`/`:34`. Whether the fuzz pipeline is seeded now with the one existing validator (`ViolaName::try_new`) or built verb-only is P4's fork. A missing parser is still never faked with a stub target.]
   - [val-1 intent-incomplete, P5] Operator decision at P4: seed the full fuzz pipeline now with the genuine `viola_name` target. This covers `fuzz/`, the corpus, `run --fuzz-replay`, the ubuntu `fuzz-replay` job and the `nightly.yml` time-box ("ViolaName is a real consumer, so the fuzz pipeline is infrastructure with a target, not an abstraction over emptiness"). test-plan §6 gains it as an eighth target at wrap.
   - Verified at P3 against the arch extract: `fuzz/` is its own workspace (`[workspace]` in `fuzz/Cargo.toml`, `publish = false`) plus the root `exclude = ["fuzz"]`. libfuzzer-sys builds with `cc`, which the `deny.toml` C-build ban forbids in the main graph (architecture §Module Boundaries / §Build system).
5. **Zero retries** (test-plan §10 zero-flakiness budget). nextest `[profile.ci] retries = 0` is already set in `.config/nextest.toml`. This chunk makes it a checked property, not a default: a gate or probe fails if any profile CI uses carries a non-zero retry count. No `#[ignore]` or `test.skip` parking. Playwright `retries: 0` has no subject until the browser chunk (Epoch 8).
6. **Per-job gate verdict** (test-plan §3 `gate`, §9 Quality gates row):
   - a `viola-harness gate --require <suites>` subcommand (forwarded by both `agent-run` shims, like `schema-check` / `secret-scan`) that reads `target/agent-run/artifacts/` (`junit-<suite>.xml`, `run-summary.json`, `llvm-cov-summary.json`, `mutants.out/outcomes.json` via the existing mutants verdict, `perf-<hook>.json`) and exits non-zero on any §10 breach, and on any required suite whose artifact is missing;
   - it runs as the last step of every job with exactly that job's suites: per-OS test `coverage,doctest`, mutants `mutants`, MSRV `nextest-unit`, fuzz replay `fuzz-replay`.
   - Verified at P3: `perf` and `playwright` have no producing suite at HEAD. `ci.yml` has no hyperfine or Playwright job (read in full). `gate` parses their artifacts per §10, but no job requires them until their chunks land.
7. **zizmor `concurrency-limits` decision** (CARRY from chunk `2026-09-24-supply-chain-and-workflow-gates`). zizmor's pedantic-persona `concurrency-limits` finding (help, low) is open on `ci.yml` and `nightly.yml`, and the default persona suppresses it. Decide whether the workflows take a `concurrency:` block. It changes run cancellation, which matters to the mutants chunk-diff base (`github.event.before`) and to the witness reads below. The decision is recorded either way.
8. **`mutants.out/` upload** (CARRY from chunk `2026-09-24-observability-gates`, overseer direction at that chunk's phase review). The `mutants` job uploads `mutants.out/`, which carries test output, unscanned: obs-plan §9's scan roots do not cover it. Either scan it before upload, or stop uploading it.
9. **[inferred] Kill the 2 CI-missed `file_mode` mutants, first** (PREREQ from the entry, resolved at take-up by the overseer; also this chunk's Setup-5a CI fold — CI run `36005608858` on sha `39c3d2b`, job `mutants`, conclusion `failure`). Overseer reading, verbatim in substance: "mutants: 78 tested, 70 caught, 6 unviable, 2 MISSED, both crates/viola-e2e/src/harness/secret_scan.rs:230:5 file_mode -> Some(0) and -> Some(1). The earlier claim that Linux tests kill them was false." The same run's `test (ubuntu-latest)` log carries exactly one PASS line for `wrapper_boot_exiting_before_ready_fails_as_exited`, so the `2834e4d` timeouts are closed.
   - Runner-only bullet, closed against the recorded run: still failing there → the verdict is VERIFIED, and the coordinates are kept.
   - [premise-corrected: the missed pair is NOT the unix reader. `cargo mutants --list` puts `230:5` on the `#[cfg(not(unix))]` stub body `None`, which Linux never compiles, so no Unix test can kill it. The unix reader is `224:5`, and the existing `secret_scan_flags_a_diagnostics_file_that_is_not_owner_only` and `secret_scan_passes_a_clean_tree` already kill its 3 mutants on Linux (research M1). This is cargo-mutants' documented limitation ("does not yet understand conditional compilation … will report functions for other platforms as missed", mutants.rs/limitations.html).]
   - Fix: `file_mode` becomes ONE function whose `#[cfg(unix)]` / `#[cfg(not(unix))]` arms are blocks inside a shared body. Its whole-body replacement mutants are then compiled on every OS and killed on Linux by those two tests. The OS reader stays minimal (testing.md SA #7).
   - Witness: the CI ubuntu `mutants` job on this chunk's push. The restructure TOUCHES the lines, so `--in-diff` regenerates the function's mutants (verification-harness SA), and the job must read them caught. A local Windows survivor on the single function's `-> None` mutant is expected (it is equivalent on Windows, where there is no mode) and is not the verdict.
   - Verified at P3 (research M2, the fetched cargo-mutants limitation): every future cfg-gated body of the OS the mutants job does not run on is unkillable there. At HEAD, `file_mode` is the only instance.
   - [val-1 intent-incomplete, P5] Operator decision at P4: fix the gate now, not in Epoch 2 ("problems are fixed at once, never pushed to the tail"). The `mutants` job gains a `windows-2025` leg, each leg writes a reduced `mutants-verdict-<os>.json`, and a `mutants-verdict` job runs `gate --require mutants --mutants-legs …` as the union: a mutant is red only if no leg caught it. This supersedes removing the `mutants.out/` upload (item 8), since only the reduced verdict files are uploaded.
   - The macOS-only (`target_os = "macos"`) residual is recorded with its owner, as the operator directed: "Unix endpoint and home hardening" (Epoch 7) adds a macOS leg to the union if it adds a macOS-only body. It becomes a wrap route-resolve CARRY.

## Boundaries

- Not in scope: hyperfine perf budgets (no hook verb exists at HEAD); Playwright, a11y lint and the browser job (Epoch 8); the release-build row and the workspace-tree arch listing (the next Epoch 1 chunk); any product behaviour beyond what a gate needs to pass honestly.
- No gate is weakened to reach green: no floor lowered, no ignore-regex widened, no retry added, no mutant excluded, no zizmor persona silenced (security rule: never widen an ignore or silence zizmor).
- Every new CI step keeps the existing discipline: SHA-pinned `uses:`, `permissions: {}` at the top with `contents: read` per job, pinned tool versions from test-plan §9, and uploads that leave CI only behind the secret scan where they carry test output.
