# Report — 2026-09-25-pty-wrapper-on-windows

**Chunk:** PTY wrapper on Windows: viola run over ConPTY via viola-pty, zero own bytes, npm-shim → claude.exe, .cmd/.bat refusal, R8 CLAUDE* strip, process-handle exit, harness supervise on the PTY form; ripgrep install resilient to schannel revocation-offline
**Date:** 2026-09-25T17:43:18Z (wrap run `.andromeda/runs/2026-09-25T17-43-18-wrap/`)
**Commits (since last_wrap 2026-09-25T13:25:14Z):**
- `17ea8c7` chore(2026-09-25-pty-wrapper-on-windows): operator pre-CI commit, for the run this chunk's verdict reads
- `c05e6e2` fix(2026-09-25-pty-wrapper-on-windows): unix exit-no-eof holder leaves the foreground group
- `7681c73` fix(2026-09-25-pty-wrapper-on-windows): scope the held-output check off macOS, measured in run 36166907442
- Chunk base `0253507` (the parent of the one operator pre-CI commit `17ea8c7`). Changed-file set: `git diff --stat 0253507` = 85 files, +10867 / -244 (incl. phase/implement run dirs and evidence) + the uncommitted `evidence/operator-pass.md` (+30 lines) and `.claude/session-handoff.md` (end-status line).

## Changes (structured — detectors read this)
- **Files:**
  - New product: `crates/viola-pty/{Cargo.toml,src/lib.rs}` (1159 lines), `crates/viola-agent-claude/{Cargo.toml,src/lib.rs}` (459), `src/run/env.rs` (268).
  - Modified product: `Cargo.toml`, `Cargo.lock`, `src/run/mod.rs`, `src/cmd/run.rs`, `src/obs.rs`, `src/bin/viola-fake-agent.rs`, `schemas/diag-line.v1.json`.
  - Harness / scripts / CI: `crates/viola-e2e/{Cargo.toml,src/harness/supervise.rs,src/harness/secret_scan.rs}`, `scripts/{sync-crates.txt,orphans-check.sh,install-ripgrep.sh}`, `.github/workflows/ci.yml`.
  - Tests: new `tests/{tui_pty_seam,tui_passthrough,tui_env_strip,cli_program_resolution}.rs`, `tests/support/outer_pty.rs`; modified `tests/support/{home,mod}.rs`, `tests/run_cli.rs`, `tests/cli_fake_agent.rs`, `tests/contract_diag_schema.rs`.
  - Route/ledger: `viola-0.1.0/verification-matrix.json` (v1-25), `viola-0.1.0/working-route.md` (the phase's marker stamp), `.andromeda/master-route.md` (the phase's pending record).
- **Symbols / APIs:**
  - `viola-pty` (pub): `PTY_BACKEND: &str` (`"conpty"` / `"openpty"`, a `const`), `Size {cols, rows}` + `Size::DEFAULT` (80×24), `SpawnSpec` (absolute program, args, cwd, names to remove, size), `ExitSource::{HandleWait, KillFallback}` + `as_str`, `Exit`, `PumpEnd`, `PtyError` (hand-written `Display`/`Error`, fixed messages; the portable-pty anyhow error kept only as a boxed source), `trait Pty` (spawn · read · write · resize · wait · kill; `mockall::automock` under test), `PortablePty`, `spawn(&SpawnSpec)`, `pump(..)`, `raw_input_mode(u32)`, `vt_output_mode(u32)`, `size_from_window(..)`, `nonzero_size(..)`, `HostTerminal` (RAII: `enter() -> Option<Self>` — one inline `#[cfg(windows)]` body and one `#[cfg(unix)]` body — restores the saved modes on `Drop`; `None` off a terminal), `host_size() -> Option<Size>`.
  - `viola-agent-claude` (pub): `IDENTITY_FLOOR: [&str; 11]`, `StripPlan` (+ `count`, `removed_names`, `kept_names` — comma-joined), `plan_strip(inherited, persistent)`, `Refusal` (`BatchScriptChild`), `Search<'a>` (+ `HOST_IS_WINDOWS` const), `resolve_program(..)`.
  - Root (crate-private): `src/run/env.rs` (persistent names: Windows `HKCU\Environment` + `HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment` value NAMES via windows-sys Registry; Unix `config.json` `claude_env_keep`), `src/run/mod.rs::{log_self_start, log_child_start(child_pid, &StripPlan), log_child_exit(Exit), log_self_exit(code, detail), resolve_program(program, cwd)}`. `src/cmd/run.rs::run()` is the only caller of the spawn path (research §Graph impact); `spawn_child`'s stdin-pipe body is replaced by the PTY spawn + pump.
  - Run order: obs init → `resolve_program` → strip plan → PTY spawn (cwd = `current_dir()`, size = `host_size()` or 80×24) → `log_child_start` → `HostTerminal` guard → pump (stdin→PTY writer; PTY reader→stdout flushed per read; resize poller every 250 ms) → `wait()` on the child handle → `process-exit{claude-child, exit_source}` → guard dropped → bounded drain → `process-exit{self}`.
  - **The pump keeps the ConPTY input writer until the child exits** (implement measured: dropping it makes conhost send `CTRL_CLOSE` to the child, exit `0xC000013A`; friction `2026-09-25T15:19:15Z-a`). The harness `supervise` writes through the kept writer.
  - Exit-code contract unchanged: 0 when the child ran, 1 on refusal / internal error.
  - `.cmd`/`.bat` refusal: exit 1, no child spawned, no `process-start{claude-child}`, `process-exit{self, exit_code:1, detail:"batch-script-child"}`, exactly two stderr lines `unable: <name>'s command is a .cmd or .bat script` / `hint: pass the real executable, not a .cmd or .bat shim`. It is the root bin's first human stderr site, one fn with a local `#[allow(clippy::print_stderr)]`.
  - npm shim: `claude.cmd`/`claude.bat` (case-insensitive) resolves to `<dir>/node_modules/@anthropic-ai/claude-code/bin/claude.exe` when that file exists; the shim is never read. Windows PATH search over PATHEXT only, never the bare name.
  - Fake agent (`src/bin/viola-fake-agent.rs`): takes a `HostTerminal` guard at start; new receipts `size {cols, rows}` (at start and on change), `cwd`, and `hold {pid}` (the `--exit-no-eof` holder, which on Unix spawns with `process_group(0)` since `c05e6e2`).
  - Harness `supervise`: one outer PTY per instance (master writer + child + a drain thread); `stop` writes `\x03` into each PTY and **presses it again every 500 ms** until the child exits, then `wait_or_kill` 10 s (a Ctrl-C into a child that is not raw yet is swallowed).
  - Env vars: none added. `VIOLA_NAME`/`VIOLA_DIR`/`VIOLA_BIN` NOT set (operator ruling 3 → a CARRY).
- **Crates / modules:** added workspace crates `viola-pty` (sync) and `viola-agent-claude` (sync), both `[lints] workspace = true`, both with a code-free `fake-agent = []` feature so a `--features fake-agent` build scoped to the package (cargo-mutants) resolves. Added root module `src/run/env.rs`. Added test support module `tests/support/outer_pty.rs`. `viola-e2e` gains a `viola-pty` dependency.
- **Dependencies:**
  - Added pins in `[workspace.dependencies]`: `portable-pty = "=0.8.1"` (comment = the v1-25 line record), `libc = "=0.2.189"` (already locked via portable-pty; research fact 10), `mockall = "=0.15.0"` (dev).
  - `windows-sys =0.61.2` features + `Win32_System_Console`, `Win32_System_Registry`.
  - `viola-pty` normal deps EXACTLY: portable-pty; `cfg(windows)` windows-sys; `cfg(unix)` libc (gate `cargo tree -p viola-pty … --depth 1` printed `['libc', 'portable-pty', 'windows-sys']`). No thiserror, no viola crate. Dev: mockall, tempfile.
  - `viola-agent-claude` normal deps EXACTLY: thiserror (no `viola-core`; `crates/viola-agent-claude/Cargo.toml`). Dev: tempfile.
  - Root: + `viola-pty`, `viola-agent-claude`; windows-sys moved to `[target.'cfg(windows)'.dependencies]`.
  - Transitive via portable-pty (research fact 10): anyhow, bitflags 1, downcast-rs, filedescriptor, lazy_static, libc, log, nix 0.25, serial 0.4, shared_library, shell-words, winapi 0.3, winreg 0.10, thiserror 1 — no C build; `cargo deny check` four-green under the unchanged `deny.toml`, RUSTSEC-2017-0008 still the only ignore.
- **Schema / config:**
  - `schemas/diag-line.v1.json` `process-start` gains `env_kept` (string). `pty_backend`, `env_stripped_count`, `env_stripped_known` (already in the schema) are now emitted.
  - `config.json` key `claude_env_keep` (Unix pass-list): array of names each `^CLAUDE[A-Z0-9_]*$`, at most 32, read with `Read::take(MAX_FRAME)`; a bad value is rejected WHOLE through `ConfigRejection` / `log_config_rejection`; a floor name in it is ineffective, never an error. `src/obs.rs` `CONFIG_KEYS` 2 → 3 (`["v", "diagnostics_level", "claude_env_keep"]`).
  - R8 strip rule (operator ruling 1): every inherited `CLAUDE*` name (ASCII case-insensitive on Windows, exact on Unix) is removed from the child EXCEPT names in the persistent set (Windows registry `Environment` keys; Unix `claude_env_keep`) — and an `IDENTITY_FLOOR` name is removed whatever the persistent set says. Floor = the 11 names measured on this host (research fact 7): `CLAUDECODE, CLAUDE_CODE_BRIDGE_SESSION_ID, CLAUDE_CODE_CHILD_SESSION, CLAUDE_CODE_ENTRYPOINT, CLAUDE_CODE_EXECPATH, CLAUDE_CODE_MESSAGING_SOCKET, CLAUDE_CODE_MESSAGING_TOKEN, CLAUDE_CODE_SESSION_ATTENDED, CLAUDE_CODE_SESSION_ID, CLAUDE_EFFORT, CLAUDE_PID`. Names only: no value is read by the strip, the registry reader or any log. The start line names the stripped (`env_stripped_known`) and the kept (`env_kept`) names.
  - Secret-scan table (`secret_scan.rs` `claude-stripped`): + one E2 canary per floor name + `CLAUDE_VIOLA_TEST_NEW_X`.
- **Spec-master edits:** none (no master changed during phase/implement; `git diff 0253507 --stat -- .andromeda/*.md` lists only `master-route.md`).
- **Counts / qualifiers moved:** (basis: implement gate record `.andromeda/runs/2026-09-25T14-10-08-implement/gate-….json` rec 10, 16:45:40Z, whose expect atoms held)
  - workspace crates +2 (`viola-pty`, `viola-agent-claude`); `scripts/sync-crates.txt` +2 (gate `grep -c` → `2`).
  - orphans-check targets 5 → 7 (`orphans-check: 7/7 targets clean`); orphans probe line `1/1 fired` → `2/2 fired, control clean`.
  - new probe line `install-ripgrep probes: 3/3 fired`.
  - root `[[test]]` binaries +4 (`tui_pty_seam`, `tui_passthrough`, `tui_env_strip`, `cli_program_resolution`).
  - `CONFIG_KEYS` 2 → 3; `process-start` fields + `env_kept`.
  - R8 name list: masters' "14-name" literal (test-plan.md:859, 1 hit) → the measured 11-name floor + the prefix rule.
  - deny-probes stays `13/13 banned, control clean`. `run --integration` on the fix: 112/112 (operator-pass.md).
- **Dev-tool versions:** none changed — cargo-modules re-read at 0.27.0, curl re-read at Git-for-Windows 8.18.0 (Schannel), zizmor at the CI pin 1.30.1, cargo-deny 0.20.2, ripgrep 15.2.0 (already installed; the install re-verified its sha256).
- **Harness / gate surface:**
  - `supervise`: outer PTY per instance; `stop` re-presses Ctrl-C every 500 ms; module doc + 4 inline tests on the PTY form.
  - `tests/support/home.rs` `Wrapper::boot/send/stop` and `tests/run_cli.rs::run_viola` on `OuterPty`; readiness waits for the fake agent's `start` receipt (its terminal is raw) before any key — also applied to `tests/cli_fake_agent.rs` and `tests/contract_diag_schema.rs`.
  - `scripts/orphans-check.sh`: each lib/bin analysed under the 3 CI triples via `cargo modules orphans --target`; a module fails only when EVERY triple reports it orphaned; `--probe` adds the never-compiled-target case and a `cfg(windows)`/`cfg(unix)` pair control.
  - `scripts/install-ripgrep.sh`: `--retry 5 --retry-delay 2 --retry-all-errors`, `--ssl-revoke-best-effort` only when `curl --version` names Schannel; sha256 compare + `tool-missing:` byte-for-byte; `--probe` mode (TLS-fail retried then fail-closed; digest mismatch → exit 1 `install-ripgrep: checksum mismatch`; matching digest passes), writes only under `target/tools/ripgrep/probe-*`; unknown argument → exit 2 + usage.
  - `.github/workflows/ci.yml` lint: new step `bash scripts/install-ripgrep.sh --probe` before the install.
  - Mutation union (`gate --mutants-legs`): UNCHANGED this chunk; its rule counts a mutant unviable on the leg that compiles its code and missed on the leg where the code is cfg-compiled out as a survivor (run 36165685381). The fix is routed to the next chunk (overseer direction 2; see Decisions).
- **Cross-project / external claims:**
  - CI run **36165685381** on `17ea8c7` (the full-chunk run; mutation base `0253507`): conclusion `failure` — `test (ubuntu-latest)` + `test (macos-latest)` red on `tui_pty_seam::pty_exit_is_read_on_the_handle_while_the_output_is_held` (355/356); `mutants (windows-2025)` success, `mutants (ubuntu-latest)` success, `mutants-verdict` failure with exactly 2 union breaches: `crates/viola-pty/src/lib.rs:395:9` and `:420:9` `replace HostTerminal::enter -> Option<Self> with Some(Default::default())` (17ea8c7 line numbers).
  - CI run **36166907442** on `c05e6e2` (fix-only mutation base 17ea8c7): conclusion `failure` — only `test (macos-latest)` red (`tests/tui_pty_seam.rs:47:5`, "the output ended although the holder started (hold receipt present)"); ubuntu + windows test green; mutants legs + union success.
  - CI run **36167590761** on `7681c73` (= HEAD; fix-only base c05e6e2): conclusion `success` — every job green incl. the 3 `test` jobs (per-OS coverage floors 85/95/80 inside them) and `mutants (ubuntu-latest)`, `mutants (windows-2025)`, `mutants-verdict`.
  - CI run 36141312209 on `0253507` (research's closed verdict): `lint (windows-2025)` attempt 1 `curl: (35) … CRYPT_E_REVOCATION_OFFLINE`, attempt 2 green — the transient this chunk's ripgrep change absorbs.
  - wezterm#6783 re-checked OPEN on 2026-09-25, portable-pty 0.9.0 still crates.io's newest (v1-25 record). portable-pty 0.8.1 source facts: `bInheritHandles = 0` (`src/win/psuedocon.rs:141`), default cwd `%USERPROFILE%` (`cmdbuilder.rs:560-567`), exact-name-first Windows PATH search (`cmdbuilder.rs:532-556`), `kill` = `TerminateProcess(h,1)`; Unix `src/unix.rs:57-58` FD_CLOEXEC on its own master/slave, `:206-236` child `setsid()` + `TIOCSCTTY` + SIGHUP reset to default.
- **Reverted / negative API facts:**
  - A `SetConsoleCtrlHandler(NULL, TRUE)` "ConPTY Ctrl-C event" fix was coded with a comment claiming measurement, then disproved by a recording handler (the signal was `CTRL_CLOSE` from the dropped input writer, not `CTRL_C`) and reverted with its diagnostics (friction `2026-09-25T15:19:15Z-d`).
  - `tests/cli_fake_agent.rs::fake_agent_exit_no_eof_exits_while_stdout_is_held` lost its `!eof_at_exit` assertion: under the PTY the grandchild holds the child's console, not viola's stdout; the PTY-level no-EOF witness is `tui_pty_seam`.
  - No `Default` impl on `HostTerminal` (overseer direction 1: making the two union-breach mutants buildable would bend the API to the tool).
- **Insufficient fixes (written, kept, not the remedy):** `c05e6e2` (the Unix `--exit-no-eof` holder spawns with `process_group(0)` + a `hold {pid}` receipt) fixed the Linux red of run 36165685381 (ubuntu green in 36166907442) but not macOS, where the output ends at the session leader's exit with the holder alive in its own group. The remainder was closed by `7681c73`: the held-output assertion runs on Windows + Linux only; on macOS exit-on-handle is shown by the handle wait alone. No open owner.
- **Spec claims disproved by measurement:**
  1. test-plan.md:859 "The R8 strip list against a literal 14-name list copied from S6" — no artifact enumerates 14 names; S6 names five and the measured inherited set on this host is 11 (research fact 7). Measured basis: research.md fact 7.
  2. test-plan.md:1304 "the no-EOF fake mode (a grandchild holds the PTY slave open on Unix; native on ConPTY)" — false as stated on Unix: (a) Linux: a holder in the session leader's foreground process group dies by SIGHUP on the leader's exit, so the output ends (run 36165685381 ubuntu red); a holder in its own process group keeps the slave open (run 36166907442 ubuntu green). (b) macOS: the output ends at the session leader's exit even with the holder alive in its own process group (`hold` receipt present, run 36166907442 macos red) — MEASURED, the revoke hypothesis confirmed in outcome (mechanism not observed directly). Overseer direction 4: carry this measurement wherever a master claims output outlives the session leader (sweep: `grep -n -E 'grandchild|exit-no-eof|holds? (the )?(output|slave|pty)|output open|never EOF|no EOF'` over the 7 masters → test-plan.md:1304, :1366, :1784 only; 0 hits in the other six; `session leader`: 0 hits in all 7).
  3. test-plan.md:1366 `--exit-no-eof` "(a bounded re-exec holds the inherited stdout for 10 s)" — under the PTY the holder holds the child's console / PTY slave (not viola's stdout), writes a `hold {pid}` receipt, and on Unix runs in its own process group.
  4. a11y-plan.md:624 (Keyboard test harness → Tooling) "no SGR or cursor control the child did not emit" as a general reading of zero-viola-bytes — unachievable on Windows: ConPTY emits `ESC[?9001h ESC[?1004h ESC[?25l ESC[2J ESC[m ESC[H`, an OSC 0 title holding the exe path and `ESC[?25h` on every spawn, and nested it re-renders (research fact 4). The line already scopes Windows to literal-absence in a later sentence; the "that means" sentence reads as all-OS.
  5. **Overseer-stated, NOT measured in this chunk's artifacts:** "ConPTY swallows focus reports" (overseer direction 4, listed under a11y). a11y-plan.md:624 has the tui outer-PTY harness assert that injected `\x1b[I`/`\x1b[O` do not move the wheel. Search for a basis: `grep -rn -E 'x1b\[I|focus'` over `.andromeda/runs/2026-09-25T14-10-08-implement/` and `chunks/…/evidence/` → 0 hits; the only chunk-side `\x1b[I` is `tests/cli_fake_agent.rs:229` (direct pipe to the fake agent, keys pass byte-exact) — no ConPTY run of a focus report is recorded. Disposition needs its basis at P2.
  6. Plan-internal (not masters): plan step 2 (thiserror `PtyError`) contradicted plan gate "viola-pty deps == [libc, portable-pty, windows-sys]" → hand-written `Display`/`Error`; plan step 3 / expected amendment "viola-agent-claude … viola-core + thiserror" → the crate depends on thiserror only; plan entry `AGENT_RUN_CHUNK_BASE=… run --mutants` in its plan form (no `--leg`) cannot pass on Windows with `#[cfg(unix)]` bodies in the diff (grades them missed by construction).
- **Expected amendments (from plan):**
  1. architecture §Infrastructure Patterns → Crate dependency direction (+ tree, licence inheritance list, code-graph plane member list): **carried** — Dependencies + Crates bullets. Actuals override the plan text: `viola-agent-claude` = thiserror only (no viola-core); `viola-pty` = portable-pty + windows-sys `cfg(windows)` + libc `cfg(unix)`; root + windows-sys `cfg(windows)` (registry); both crates' `fake-agent` no-op feature. Sites: `grep -c viola-pty architecture.md` = 6, `viola-agent-claude` = 23, `libc` = 0.
  2. architecture §Established Decisions [PTY]: raw host terminal (`HostTerminal`), explicit child cwd, viola-side program resolution, kept input writer until child exit: **carried** — Symbols bullet. Sites: `grep -c ConPTY architecture.md` = 2.
  3. architecture §Occupied Resources → Environment variables: the R8 rule per ruling 1 + `config.json` `claude_env_keep`: **carried** — Schema / config bullet. Sites: `R8` = 4 hits, `claude_env_keep` = 0.
  4. security-plan §Secret Management → Storage + §Input Validation: R8 persistent-environment exemption with the identity floor; child executable resolution as built: **carried** — Schema / config + Symbols. Sites: `R8` = 4, `persistent` = 8, `Child executable resolution` = 1, `identity floor` = 0.
  5. obs-plan D-14 + §6 `process-start` catalog: stripped and kept names are named, `env_kept` field: **carried** — Schema / config. Sites: `D-14` = 4, `env_stripped` = 7, `env_kept` = 0.
  6. test-plan §6 E2: the 14-name literal → the measured 11-name floor + prefix rule; `VIOLA_*` and Unix-fds halves re-pinned to their CARRY entries: **carried** — Counts + Spec claims 1. Sites: `14-name` = 1 (test-plan:859), `VIOLA_NAME` = 16, `0/1/2` = 4.
  7. test-plan §3 and a11y-plan §3 Keyboard test harness: the Windows zero-own-bytes oracle per research fact 4: **carried** — Spec claims 4. Sites: `ConPTY` test-plan = 10, a11y-plan = 3.
  - Added by overseer direction 4 (not in the plan list): the macOS/Linux held-output measurement (Spec claims 2, 3; test-plan only); "ConPTY swallows focus reports" for a11y (Spec claims 5; no basis on disk).
- **Coverage of new surfaces:**
  - `viola run` PTY passthrough (stdin→PTY, PTY→stdout, resize poller) → validation n/a (human bytes pass unmodified by contract) · instrumentation `process-start{pty_backend, env_*}` / `process-exit{exit_source}` ✓ · PII names-only ✓ · tests integ (`tui_passthrough` 3, `tui_pty_seam` 2) · a11y tui clauses: zero-own-bytes ✓, keystrokes byte-exact + Ctrl-C ✓, resize ✓, focus/mouse sequences ✗ (no wheel yet — not asserted) · tokens n/a
  - `.cmd`/`.bat` refusal (stderr `unable:`/`hint:`) → validation closed enum `Refusal` ✓ · instrumentation `process-exit{detail:"batch-script-child"}` ✓ · PII no path/pid ✓ · tests integ `cli_program_resolution` (Windows) · a11y n/a · tokens n/a (design-system fixed line)
  - `resolve_program` / npm-shim → validation fixed sibling path, shim never read ✓ · instrumentation n/a (failures → `internal-error`, chain to the detail file) · PII n/a · tests unit tables + integ
  - R8 strip (`plan_strip`, `IDENTITY_FLOOR`) → validation names only ✓ · instrumentation `env_stripped_count/_known`, `env_kept` ✓ · PII values never read; secret-scan 0 canaries ✓ · tests unit + integ `tui_env_strip` (Unix `claude_env_keep` survival on CI)
  - `config.json` `claude_env_keep` → validation `Read::take(MAX_FRAME)` + name regex + ≤32 + whole-reject ✓ · instrumentation `log_config_rejection` ✓ · PII n/a · tests unit (`src/run/env.rs`)
  - Windows registry name reader → validation bounded buffer, names only ✓ · instrumentation n/a · tests unit (absent key → empty)
  - `HostTerminal` raw/VT mode + restore → tests child PTY test reads `ENABLE_LINE_INPUT`/`ICANON` after drop ✓ · a11y (never delay a keystroke) ✓
  - harness `supervise` outer PTY, `install-ripgrep.sh --probe`, `orphans-check.sh` triple rule → tests: supervise inline tests, probe verdict lines ✓ · instrumentation n/a

## Deviations from intent
1. `PtyError` has a hand-written `Display`/`Error` (plan step 2 said thiserror) — the plan's own dependency gate forbids a thiserror dep in `viola-pty`.
2. `viola-agent-claude` depends on thiserror only, not `viola-core` (plan step 3): nothing in it needs a core type.
3. Both new crates carry a code-free `fake-agent` feature — cargo-mutants passes `--features fake-agent` to package-scoped builds; without it the leg swamped unviable (friction `…16:59:16Z-f`).
4. The pump keeps the ConPTY input writer until the child exits (not in the plan): dropping it on stdin EOF killed the child via `CTRL_CLOSE` (measured).
5. `supervise` stop re-presses Ctrl-C every 500 ms (plan: write `\x03` once, then 10 s `wait_or_kill`): a single press was swallowed by a child not yet raw.
6. `tests/cli_fake_agent.rs` and `tests/contract_diag_schema.rs` edited outside the research modify-set (start-receipt readiness; the stdout-held assertion dropped) — research's companion sweep missed them (friction `…15:19:15Z-b`).
7. Fake agent also gained `cwd` and `hold {pid}` receipts (plan named only `size`).
8. `c05e6e2` / `7681c73` post-pre-CI fixes: Unix holder in its own process group; held-output check scoped off macOS (operator option a, run 36166907442).
9. Plan entry `run --mutants` read in the CI form `--leg windows-2025` (ok:true) with the union as the verdict — the plan form cannot pass with `cfg(unix)` bodies in the diff.
10. The 2 union breaches are operator-ratified without a product change (overseer direction 1).
11. Remove-the-guard runs: 11 guards recorded (plan listed 8; + `keep-child-input`, `ctrl-c-resend`, `terminal-restore`) — `evidence/guards/readings.json` (11 entries).

## Decisions & corrections
- **Overseer direction 1 (this wrap) — reasoned ratification, not a deferral:** the 2 `HostTerminal::enter -> Some(Default::default())` union breaches of run 36165685381 are accepted with no product change. Basis: on the leg that compiles each `enter` the mutant is unviable (`HostTerminal` has no `Default`), so it cannot exist; the "missed" on the other leg comes only from that body being cfg-compiled out there. A `Default` impl just to make a mutant buildable would bend the API to the tool. Precedent: L7.
- **Overseer direction 2:** the union-rule fix — "a mutant unviable on the leg that compiles its code is not a survivor, whatever the other leg reads" — folds into the NEXT chunk, "Local Linux pre-push gate" (WSL2 Ubuntu installed 2026-09-25), which the operator inserts at the route head via a 0-pending wrap right after this one. Pinned as a CARRY on that entry once it exists; named in the handoff until then.
- **Overseer direction 3:** the chunk's mutation verdict = run 36165685381's legs (base 0253507) + the fix runs 36166907442 / 36167590761 (their own lines), with 36167590761 all-green on HEAD 7681c73. Base = 0253507 (one pre-CI commit, 17ea8c7).
- **Overseer direction 4:** P2 carries the plan's list, the `VIOLA_*` / Unix fds / fixtures CARRYs, the labelled `^Z` hypothesis, and the macOS revoke measurement wherever a master claims output outlives the session leader.
- **Operator rulings (P4, 2026-09-25):** (1) R8 strip exempts persistent-environment names except the identity floor; each stripped and kept name is logged. (2) Raw tty on Unix too: `libc` `cfg(unix)` in `viola-pty`. (3) No `VIOLA_*` here → CARRY on "Instance state and start order".
- **Operator (operator pass):** macOS option (a) — keep the held-output assertion everywhere; if only macOS stays red with `hold` present, scope it to Windows + Linux in a follow-up citing run 36166907442 (done: 7681c73).
- **HYPOTHESIS (unmeasured, labelled):** Rust std's Windows console stdin may treat a leading `^Z` as end of input, which would stop that key reaching the child. Carry, labelled as a hypothesis, on the next route entry that feeds `viola run` stdin.
- **Correction (retraction `2026-09-25T17:12:00Z-a`):** the 16:44:58 session end was a hand-closed window (founder's account); the `contract_diag_schema` Ctrl-C race is a hang found and fixed, not its cause.
- **Measured learnings:**
  - Dropping ConPTY's input writer sends `CTRL_CLOSE` to the child (exit `0xC000013A`): keep the writer until the child exits.
  - A Ctrl-C written into a ConPTY child before it has set raw mode is swallowed: tests wait for the child's `start` receipt; the harness re-presses.
  - On Linux a PTY holder in the session leader's foreground process group dies by SIGHUP when the leader exits; a holder in its own group keeps the output open. On macOS the output ends at the leader's exit regardless.
  - The session's LSP `rust-analyzer` holds `mutants.out`, so cargo-mutants' rename fails (`os error 5`) until it is stopped.
  - `secret-scan` read `target/agent-run/chunk.diff` residue from a prior `run --mutants` (its own `?t=` pattern line) — scan scope is a harness-owner question.
  - `agent-run boot | <pipe>` never reaches EOF while the session runs: `supervise` inherits boot's stdout (pre-existing; `boot.rs` untouched).
  - cargo-mutants' union reads unviable-on-the-compiling-leg + missed-where-compiled-out as a survivor (routed, direction 2).
  - A remove-the-guard run exposed two vacuous tests (`pump_copies_the_output_it_drains_after_exit`; the terminal-restore check through a fresh `enter()`), both strengthened.
- **Sweep hazard:** the token `14` in "14-name" appears once (test-plan:859); the S6 brief is the only other "14" source — no master enumerates the names, so a sweep keyed on the list's members finds nothing to replace.

## Outcome
**Acceptance criteria (re-asserted against the diff):**
- Outer-PTY keys `ab\r` → `61 62 0d`, no `0a`; `\x03` ends the child; wrapper exits 0 with `exit_source:"handle-wait"`; none of viola's literals in the master stream — **met** (`tui_passthrough`, gate `--filter …` green; CI 36167590761 3 OSes green).
- Unix byte-for-byte wrapped == unwrapped — **met on CI** (ubuntu + macos `test` jobs green in 36167590761; not measurable on this host).
- Exit on the handle (real PTY with the holder; mock seam never-EOF reader) — **met**, with the held-output half asserted on Windows + Linux only (macOS measured to end its output at leader exit; 7681c73).
- PTY resize + host resize reach `size`; child cwd == spawner's — **met** (`tui_pty_seam`, `tui_passthrough`).
- `.cmd`/`.bat` refusal + `claude.cmd` fixture resolution — **met** (`cli_program_resolution`, Windows).
- R8 canaries: 11 floor + unknown stripped; start line carries count ≥ 12 / known / kept; secret-scan 0; Unix `claude_env_keep` survives (CI); unit tables — **met** (smoke on this host logged `env_stripped_count 11` from the live shell's names).
- `viola-pty` deps exactly portable-pty / windows-sys / libc, no viola crate; both crates in sync-crates and pass the sole-root tokio ban; `cargo deny check` four-green, one ignore — **met**.
- `orphans-check.sh --probe` 2/2 + control clean; `orphans-check.sh` 7/7 on all 3 lint legs — **met** (local + CI lint legs green in 36167590761).
- `install-ripgrep.sh --probe` 3/3; real install verifies 15.2.0; zizmor green — **met**.
- Harness lifecycle over outer PTYs (`pty_backend:"conpty"`, `processes_gone:true`) — **met**.
- Mutation gate: 0 missed / 0 timeout / unviable ≤ caught on each leg; union — **met by operator ratification**: legs green in 36165685381 (windows local CI-form: 186 mutants, 151 caught, 30 unviable, 0 timeout, 5 cfg(unix) survivors caught/unviable on ubuntu); the union's 2 breaches ratified (direction 1); fix runs 36166907442 / 36167590761 union green on their own lines. Coverage floors 85/95/80 inside the green `test` jobs of 36167590761. 11 remove-the-guard red/green pairs in `evidence/guards/`.
- v1-25 (by-construction): the portable-pty line record is beside the pin and in the crate doc — **met**; matrix v1-25 `implemented`.

**Gates** (implement's final full run, `gate.py run` rec 10 at 2026-09-25T16:45:40Z; entries by `run` text):
- `cargo fmt --all --check` green · `cargo clippy --workspace --all-targets --features fake-agent -- -D warnings` green · `bash scripts/agent-run.sh run --unit` green · `run --integration --filter 'binary(tui_pty_seam) | …'` green · `AGENT_RUN_KEEP_HOMES=1 … run --integration` green · `schema-check` green · `secret-scan` green (after removing the `target/agent-run/chunk.diff` residue) · smoke `cleanup` / `boot` / `status` / `logs … pty_backend` (`conpty`) / `cleanup` (`processes_gone:true`) green · `grep -c … sync-crates.txt` green (2) · both `cargo deny --config deny-sync.toml --manifest-path crates/viola-{pty,agent-claude}/Cargo.toml check bans` green · `cargo deny check` green · `deny-probes.sh` green (13/13) · `cargo tree -i portable-pty` green (v0.8.1) · `cargo tree -p viola-pty …` green · `orphans-check.sh --probe` green (2/2) · `orphans-check.sh` green (7/7) · `install-ripgrep.sh --probe` green (3/3) · `zizmor --format=plain .github/workflows/` green · `install-ripgrep.sh` green · MSRV `RUSTUP_TOOLCHAIN=1.96 CARGO_TARGET_DIR=target/msrv cargo check …` green.
- `AGENT_RUN_CHUNK_BASE=0253507… bash scripts/agent-run.sh run --mutants` — **red in its plan form** (exit 1: 5 `cfg(unix)` bodies compiled out on Windows grade missed). Treatment: read in the CI form `--leg windows-2025` (ok:true, 186 / 151 caught / 30 unviable / 0 timeout) with the union as the verdict per overseer direction 3; the union's 2 breaches ratified per direction 1.
- `git diff --quiet && … git push origin build/viola-0.1.0` (leg operator) — pushed `0253507..17ea8c7`, `17ea8c7..c05e6e2`, then `7681c73`; branch 0 ahead of upstream at this wrap's Setup.
- `gh api …/check-runs … unique | join` (leg operator) — on HEAD `7681c73`: `success` (run 36167590761). Earlier shas: 36165685381 `failure,success`, 36166907442 `failure,success` — dispositioned above.
- `gh api …/check-runs … startswith("mutants") …` (leg operator) — on HEAD: `success,success,success`. On 17ea8c7: `success,success,failure` → ratified (direction 1).
- Post-pre-CI fix gates (operator-pass.md, Windows): fmt 0, clippy 0, `tui_pty_seam` 2/2, `cli_fake_agent` 32/32, `tui_passthrough` 3/3, `run --integration` ok:true 112/112.

**Outcome basis:** implement's conversation is gone (a `/clear` since), so this rests on: implement's evolve records (`chunk == 2026-09-25-pty-wrapper-on-windows`, `skill == implement`: 12 records incl. the retraction), the implement gate record `.andromeda/runs/2026-09-25T14-10-08-implement/gate-….json`, `evidence/operator-pass.md` (incl. its uncommitted runs 36165685381-final / 36167590761 sections), `evidence/guards/readings.json`, `git diff 0253507`, and the overseer's four directions given at this wrap. Implement's tallies beyond these are `unmeasured — the implement session`.

**Process hygiene:** implement smoke census 0/3/0 (friction `2026-09-25T17:10:29Z-a`). Re-measured at this wrap (`Get-CimInstance Win32_Process`): 0 `viola-harness` / `viola-fake-agent` / `cargo-mutants`; 4 `viola.exe` running, all from `D:\dev\projects\additional\viola-lab\prototype\target\debug\` — outside this repo, not this chunk's, left untouched.
