# Report — 2026-09-26-ci-chunk-base-and-union-verdict

**Chunk:** CI chunk base and union verdict: whole-chunk mutation base that survives the operator pass, compiling-leg union verdict, secret-scan residue scope
**Date:** 2026-09-26T21:00:00Z
**Commits:** `e486544` chore(route): operator-requested adaptation — 0-pending wrap (the adaptation that minted this entry) · `acd08c7` chore(2026-09-26-ci-chunk-base-and-union-verdict): operator pre-CI commit, for the run this chunk's verdict reads

## Changes (structured — detectors read this)
- **Files:** `crates/viola-e2e/src/harness/cfg_legs.rs` (new) · `crates/viola-e2e/src/harness/run/mutants.rs` · `crates/viola-e2e/src/harness/run.rs` · `crates/viola-e2e/src/harness/gate.rs` · `crates/viola-e2e/src/harness/mod.rs` · `crates/viola-e2e/src/harness/secret_scan.rs` · `crates/viola-e2e/src/bin/viola-harness.rs` · `crates/viola-e2e/Cargo.toml` · `Cargo.toml` · `Cargo.lock` · `.github/workflows/ci.yml` (basis: `git diff --stat e486544 -- . ':(exclude).andromeda/runs'`, 34 paths incl. chunk folder and route files).
- **Symbols / APIs:**
  - `resolve_base(repo, from_env)` (mutants.rs, re-exported from `harness::run`): signature unchanged, rule changed. Order: `AGENT_RUN_CHUNK_BASE` (trimmed, non-empty) as an explicit override; else the derived base; else `git merge-base HEAD origin/main`; the result is the full sha (`git rev-parse --verify --quiet {rev}^{commit}`), `None` otherwise. Sole production caller `mutants()` (graph: mutants.rs:239); its test callers updated.
  - Derived base (new private fns `chunk_flip`, `pre_ci_parent`, `pending_marker`, `commit_sha`): read `.andromeda/master-route.md` from HEAD's tree; every `{marker} · pending · ` record line's marker; the OLDEST commit in HEAD's history whose message carries `chore({marker}): operator pre-CI commit` (`git log --reverse --format=%H -F --grep …`); bound = that commit's parent, else HEAD; base = `git log -1 --format=%H -G ' · complete · ' {bound} -- .andromeda/master-route.md`; when that commit IS HEAD (the wrap push, whose own commit flips the record) the base is `HEAD^`.
  - `commit_exists` (pub, re-exported) REMOVED: after `resolve_base` moved to `commit_sha` its only caller was a test assertion and its mutant (`replace commit_exists -> bool with true`) survived the local leg. 0 remaining callers (`grep -rn commit_exists` over `crates/ src/ tests/`: 0 hits after removal).
  - `run --mutants` run document: every form (`counted`, `no-rust-delta`, `test-only-rust-delta`) gains `"base": "<full sha>"` (a commit id, never a path). The leg verdict file `mutants-verdict-<leg>.json` is UNCHANGED (names + outcomes only).
  - `gate(artifacts, root, require, legs)` (gate.rs): new second parameter `root`, the source tree the legs ran on. 1 production caller `gate_cmd` (viola-harness.rs, passes `ws.root`) + 15 test call sites (graph: gate @ viola-harness.rs:194 + gate.rs tests). CLI surface `gate --require … --artifacts … --mutants-legs …` unchanged.
  - `union` (private): a mutant is judged only by the named legs that COMPILE its line (`cfg_legs::compiled_legs`); when none does, by every named leg; red iff no judged leg reads `caught` and some judged leg reads `missed` or `timeout`. A missing leg file stays `artifact-missing` with no mutant judged.
  - `cfg_legs::compiled_legs(root, mutant, legs) -> Vec<String>` (new pub fn, new pub module `harness::cfg_legs`): parses `{path}:{line}:{col}: {description}`; parses the file with `syn`; collects every `#[cfg(…)]` on a covering node (visited: `ItemFn`, `ItemImpl`, `ItemMod`, `ImplItemFn`, `Local`, and the expressions `Block`, `Call`, `ForLoop`, `If`, `MethodCall`, `Tuple`, `Unsafe`); evaluates each per leg three-valued. Leg family by name prefix: `windows-` → windows; `ubuntu-` → unix + linux; `macos-` → unix + macos. Known predicates: `unix`, `windows`, `target_os = "…"`, `target_family = "…"`, `not`, `all`, `any`; anything else (incl. `test`, `feature`) is unknown. A leg is dropped ONLY when a covering cfg is proven false for it; unknown predicate / unknown leg family / unparsable name / a path with a non-normal component / unreadable or unparsable file / an unvisited node form all keep the leg.
  - `secret_scan::scan`: skips exactly `<agent_run>/chunk.diff` (the mutation leg's diff); a `chunk.diff` elsewhere under the capture is still scanned; classes, report shape, `empty-scope` unchanged.
- **Crates / modules:** new module `viola-e2e::harness::cfg_legs`; no crate added or removed; no product crate touched (`viola`, `viola-core`, `viola-pty`, `viola-agent-claude` unchanged — `git diff --stat e486544 -- src crates/viola-core crates/viola-pty crates/viola-agent-claude`: empty).
- **Dependencies:** `[workspace.dependencies]` + `syn = "=2.0.119"` (default-features off; `full`, `parsing`, `printing`, `visit`) and `proc-macro2 = "=1.0.107"` (`span-locations`); consumed by `viola-e2e` only (test-only crate). Both were already resolved in `Cargo.lock` (proc-macro deps); the lock gained only the two dependency-list lines (`git diff --stat e486544 Cargo.lock`: +2). Licences MIT OR Apache-2.0; `cargo deny check` four-green.
- **Schema / config:** none (no JSON schema, config key or redaction shape changed; the run document's new `base` field is harness stdout, no schema file carries the run doc).
- **Spec-master edits:** none yet (this wrap's P2).
- **Counts / qualifiers moved:** the pinned-action set is unchanged (5); `ci.yml` `github.event` references 1 → 0 (`grep -c 'github\.event' .github/workflows/ci.yml`); harness unit tests +18 new (the filtered gate entry: 18 run, 18 passed); workspace-deps pins +2.
- **Dev-tool versions:** none — cargo-mutants 27.1.0, cargo-nextest 0.9.146, zizmor 1.30.1, cargo-deny re-read at their CI pins; rustc 1.98.1 / MSRV 1.96 unchanged.
- **Harness / gate surface:**
  - CI `mutants` job (ci.yml): the `AGENT_RUN_CHUNK_BASE: ${{ github.event.pull_request.base.sha || github.event.before }}` env line REMOVED; only `LEG` stays; step renamed "Mutation leg (whole chunk)"; the job comment restates the compiling-leg union.
  - CI `test` job `harness-<os>` upload: `path` = `target/agent-run/` plus `!target/agent-run/chunk.diff`.
  - `mutants-verdict` job unchanged in shape; its `gate` now reads the checked-out source (same sha) for `#[cfg]`s.
  - `AGENT_RUN_CHUNK_BASE` meaning: from "the base CI sets" to "an explicit override of the derived base" (still read only by `viola-harness`, viola-harness.rs `run_cmd`).
- **Cross-project / external claims:**
  - CI run **36270173848** on `acd08c718c6b25a6e9ea1d0a292e671bf9e5fe6a` (the pre-CI commit; this wrap's commit adds the report/specs on top): completed `success`, 15/15 checks. `mutants (ubuntu-latest)` job 108482499487 and `mutants (windows-2025)` job 108482499511 each printed `"base":"fcca1cebef44778f7475b61535bded1484218289"`, tested 79, caught 74, survived 0 (unviable 5); `mutants-verdict` job 108485375727 `{"cmd":"gate","ok":true,"breaches":[]}`. Independently read by the overseer (same figures). Evidence: `evidence/operator-pass.md`.
  - Run **36165685381** (17ea8c7, the pty chunk): its two recorded leg files (downloaded at phase P3 into `.andromeda/runs/2026-09-26T19-33-34-phase/run-36165685381/`) hold the breach shape `lib.rs:395:9 … Some(Default::default())` windows unviable / ubuntu missed, `:420:9` the reverse; the replay of all 186 × 2 records through the new union against HEAD (lib.rs byte-identical to 17ea8c7) reads `ok:true`, where the previous union printed exactly those two breaches (phase `p5/05.log`).
- **Reverted / negative API facts:** `commit_exists` removed (above). A copy of `mutants.out/outcomes.json` placed in `evidence/` during implement was deleted before any commit — it carries absolute argv paths and test output.
- **Insufficient fixes (written, kept, not the remedy):** none.
- **Spec claims disproved by measurement:**
  - The route CARRY's labelled hypothesis ("`-G` selects any commit whose diff adds or removes a `· complete ·` line, so a mid-pass edit of a complete master record would move the base") is MEASURED TRUE (phase research `base_probe.sh`; unit test `chunk_base_holds_the_flip_across_a_pass` with guard g1). Disposition: the rule this chunk ships bounds the pickaxe; test-plan states it (Expected amendments).
  - test-plan §3 `secret-scan` Scope / §6 Canary state the scan covers every file under `target/agent-run/`; the pty chunk's observation that `chunk.diff` residue hits `?t=` shows that scope includes repository source text by construction (the hit is not staleness: a fresh diff touching `secret_scan.rs` hits too). Disposition: scope narrowed by exactly that file on the scan AND upload side (Expected amendments).
- **Expected amendments (from plan):** (site search: `grep -c -F` per master; hits: `github.event` arch 1 (522) · test-plan 3 (553, 1439, 1767); `AGENT_RUN_CHUNK_BASE` arch 1 (364) · security 1 (340) · test-plan 2 (553, 1439); `no leg` arch 1 (522) · test-plan 5 (655, 796, 1439, 1501, 1803) · obs 1 (1253) · layouts 1 (288, unrelated wording — to be read); `chunk.diff` arch 1 (386) · test-plan 6; `target/agent-run/` arch 1 · test-plan 19 · obs 6; `harness-<os>` arch 1 (522) · security 1 (386) · obs 5; `mutants-verdict` arch 3 · security 3 · test-plan 14 · obs 2)
  - test-plan §3 `run` step 4 Base + §9 Mutation row: the derived base (bounded at the oldest pre-CI commit's parent, wrap push `HEAD^`), the override kept for tests, the `base` field in the run document, CI no longer passing an event value — carried (Symbols/APIs, Harness).
  - test-plan §3 `gate` union bullet + §10 Mutation gate: judged by compiling legs from the source's `#[cfg]`s, empty-set fallback to every leg, missed on a compiling leg stays red — carried.
  - test-plan §3 `secret-scan` Scope + §6 Canary bullet: minus the mutation leg's `chunk.diff` — carried.
  - test-plan §9 Test report format: `harness-<os>` excludes `chunk.diff` — carried.
  - test-plan §12: Decisions Log entry for the four — carried.
  - architecture §Infrastructure Patterns → CI/CD approach: mutants base sentence, union sentence, `harness-<os>` upload — carried.
  - architecture §Occupied Resources → Environment variables: `AGENT_RUN_CHUNK_BASE` = explicit override — carried.
  - architecture §Occupied Resources → Repository: `target/agent-run/chunk.diff` never scanned or uploaded — carried.
  - architecture §Infrastructure Patterns → crate dependencies: `viola-e2e` + syn / proc-macro2 — carried (Dependencies).
  - obs-plan §9 Mutation row + Step order step 3 + §8 integration point 6: union rule; scan scope and upload exclusion — carried.
  - security-plan §Secret Management "Secret scanning in CI": scope sentence where it names `target/agent-run/` — carried (security 1 hit on `harness-<os>` at 386, 1 on `AGENT_RUN_CHUNK_BASE` at 340 — to be read by the detector).
- **Coverage of new surfaces:**
  - `cfg_legs::compiled_legs` → validation mechanism✓ (path components Normal only; conservative on every unreadable input) · instrumentation n/a (harness, no product telemetry) · PII n/a · tests unit (`compiled_legs_*` ×5, `union_by_compiled_legs_*` ×5) · a11y n/a · tokens n/a
  - derived chunk base (`resolve_base`) → validation mechanism✓ (a commit or `None` → `base-missing`) · instrumentation n/a · PII n/a (a sha) · tests unit (`chunk_base_*` ×6 + `resolve_base_needs_a_real_commit`) · a11y n/a · tokens n/a
  - `secret-scan` exact-path skip → validation n/a · instrumentation n/a · PII: the skipped file is never uploaded (ci.yml exclusion) · tests unit (`secret_scan_skips_the_mutation_diff*` ×2) · a11y n/a · tokens n/a

## Deviations from intent
- Plan step 4 listed trait items, fields, match arms and inner `#![cfg]` among the visited nodes; the visitor covers the forms measured in lib/bin code at HEAD (fn-level, impl, inline mod, impl fn, `let`, seven statement-expression kinds). Every unvisited form reads as uncfg'd and keeps every leg — it can never exempt a mutant — and each extra hook would be one more mutant needing its own kill.
- `commit_exists` removed (plan did not list it): the change left it with no production caller and the mutation gate flagged it.
- One test beyond the plan: `chunk_base_reads_only_pending_record_markers`.
- The local windows leg needed rust-analyzer (the session LSP) stopped by exact ExecutablePath before each of three runs (it held `mutants.out`; `os error 5`; it restarts after edits).

## Decisions & corrections
- Overseer directions (phase, verbatim intent): the base is the last master flip reachable from the OLDEST pre-CI commit's parent, proven by a simulated-pass probe; the union's witness on the recorded 36165685381 shape plus a negative (missed on the compiling leg stays red); the scan stops reading `chunk.diff`, planted-secret red kept; remove-the-guard per guard, 0 missed / 0 timeout, fold don't carry; the local Linux gate is the next chunk.
- Decisions (phase P4 leans, approved at P5): the compile-leg knowledge comes from the source via `syn`, gate-side — a leg-side annotation would widen the ratified content class of the unscanned leg upload; `AGENT_RUN_CHUNK_BASE` stays as the override; the wrap push reads `HEAD^`; the scan skips by exact path, and the upload excludes the same file.
- Correction (orchestrator, self-caught): `mutants.out/outcomes.json` must never be copied into a committed evidence folder — it carries absolute argv paths and test output.
- Observation (overseer-requested): two cargo-mutants 27.1.0 windows runs over the IDENTICAL final tree graded differently — caught 73 / unviable 6, then caught 71 / unviable 8, 79 tested both times, both green (`evidence/mutants-leg-windows-local.md`). CI's two legs each read 74 / 5 on the same tree. No artifact froze a number; the per-leg verdict (0 missed, 0 timeout, unviable ≤ caught) held on every run. Cause unmeasured.
- Sweep hazard: `-G ' · complete · '` matches any commit whose diff adds OR removes a complete record line — a mid-pass edit of a complete record moves an unbounded pickaxe (measured).
- Host: rust-analyzer restarts after edits, so a stop before one `run --mutants` does not carry to the next run.

## Outcome
- Acceptance criteria, re-asserted against the diff:
  - Derived base holds the flip across a simulated pass; before any pre-CI commit it is the last flip; the wrap push reads HEAD's parent; the override wins; no flip and no `origin/main` → `base-missing` — MET (`chunk_base_*` + `resolve_base_needs_a_real_commit`, run by `run --unit` and the filtered entry).
  - Local windows leg without `AGENT_RUN_CHUNK_BASE` prints `"base":"fcca1ce…"`, counted, 0 missed, 0 timeout, unviable ≤ caught — MET (final block: tested 79, caught 71, unviable 8); CI legs and union success on the final HEAD with the same base on both legs — MET (run 36270173848).
  - Recorded-corpus replay `ok:true`; recorded shape clears; control without cfg keeps both; missed on the compiling leg stays red; shared unviable+missed stays red; a body no leg compiles is judged by all — MET.
  - `secret-scan` green over kept homes plus a live `chunk.diff` with `?t=`; same bytes elsewhere hit; planted-secret tests green; upload excludes exactly that file — MET.
  - `ci.yml`: 0 `github.event`, no `concurrency:`, five pinned actions, zizmor clean — MET.
  - Only `viola-e2e`, the two manifests, `Cargo.lock`, `ci.yml` changed; no product crate; deny four-green; MSRV check green — MET.
  - Guards g1–g7 red/green pairs recorded — MET (`evidence/guards/summary.ndjson`, 7 × pair ok).
- Gates (implement's final block, `.andromeda/runs/2026-09-26T19-53-13-implement/gate-full-2.out`): `cargo fmt --all --check` green · `cargo clippy --workspace --all-targets --features fake-agent -- -D warnings` green · `bash scripts/agent-run.sh run --unit` green · `run --unit --filter 'test(/chunk_base_|compiled_legs_|skips_the_mutation_diff/)'` green (18 run) · `gate --require mutants --mutants-legs ubuntu-latest,windows-2025 --artifacts …run-36165685381/artifacts` green · `AGENT_RUN_KEEP_HOMES=1 … run --integration` green · `run --mutants` green · `grep -c -F '?t=' target/agent-run/chunk.diff` green · `secret-scan` green · `grep -c 'github\.event' …ci.yml` green (exit 1, last line 0) · `grep -c -F '!target/agent-run/chunk.diff' …ci.yml` green · `zizmor --format=plain .github/workflows/` green · `cargo deny check` green · MSRV `cargo check` green. Operator entries: push `e486544..acd08c7` exit 0; check-runs `success`; mutants `success,success,success` (run 36270173848; `evidence/operator-pass.md`). Smoke: harness lifecycle cleanup/boot/status(ready)/cleanup(processes_gone:true) ✓.
- Outcome basis: implement's P4 report (this conversation) + the operator pass run by the session on the overseer's word + the overseer's independent CI read.
- Process hygiene: all harness/cargo/nextest/cargo-mutants processes terminated (host list read at implement P3 and after the pass); `impl-smoke` session cleaned; rust-analyzer stopped by this chunk's runs and left to restart on demand; other sessions' `claude.exe` and a `viola-lab` prototype `viola.exe` not this chunk's.
